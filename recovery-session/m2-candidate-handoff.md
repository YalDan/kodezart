# Actual M2 Organize candidate

Candidate `73cc5def12a2daa4e0ba319660db76cdad781882`, tree `17c6bb565eaefa0f1a11232b77c9b1490e7b6f19`, branch `codex/v03-m2-organize`, clean writer `/private/tmp/kodezart-v03-m2-organize`. Maintained M4 `dddbcbb0df1e0171e8a770063958f85d16c0d917` is an actual ancestor. Donor `36083f83f42c03240ebb5861fe284da2c9f04180`. No foundation branch or PR118 destination.

## Reviewable scope

The maintained diff has103 files,13426 insertions and62 deletions. It contains actual admission, authoring, fresh independent verification, bounded admission/convergence/verification halts, description/criterion/graph/split writes, phase markers, explicit scope approval checks, and scheduled native owner composition. Four role prompts in both shipped sets, exact registered typed schemas, explicit no-default owner budgets, operation bindings, config and docs are included. M4 classification/escalation/ruling/source-reader/write-back prerequisites are inherited without duplicated source diff. M1 GitSettings is inherited; the provisional config.git_remote compatibility difference is gone.

Machine-readable independent source map: `m2-final-donor-hunk-map.json` compares every final changed file and source symbol to donor, recording exact candidate/donor line locations and AST equality.59/103 files byte-identical to donor. `m2-extracted-hunks.json` retains extraction history. `m2-final-test-oracle-map.json` compares238 changed test functions: all assert and pytest.raises/fail call ASTs match donor. This proves retained function oracle expressions, not completeness of all later milestone test migration.

## Explicit packaging differences and ownership

- Scope-label constructor input is the exact required `Mapping[str,str]`. Production uses actual OperationConfig.scope_labels;8 existing constructor fixture calls explicitly pass `{}`. No implicit optional fallback remains. Shared scope-input read/labels belong to M1 when root serializes ownership; currently materialized once in this consuming tree.
- Pure domain/topology and types/domain/topology belong to M2 as first real cycle-validation consumer, superseding prior file-level M3 ownership. Existing domain.dispatch.blocker_keys was already available. Planner/walker/current readiness runtime remains M3.
- Partial fire_spec contains only unchanged criterion field parser/constants/helper/error used by criterion creation. No native-fire agent union, field defaults, state/effect workflow or audit closure.
- tests/chains/test_organize_owner.py and tests/integration/test_organize_scheduler.py omit unused later-fire criteria_stage_label_key fixture input. Original assertions retained.
- test_organize_ownership uses explicit existing GitWorktreeProvider fixture identity instead of relying on the donor later signature's optional inputs; valid under maintained M1 and no oracle change.
- tracker_over fixture supplies criterion and approved scope mappings; linear_over_fake_mcp takes explicit optional fixture mapping forwarded as{}. Actual production adapter remains required.
- FakeTrackerPort now implements the exact needed Organize methods and effect counters. Fake-only upsert helper supports fake split creation; no public production upsert API imported.
- FakeMcpIssue adds native project/milestone fields, and FakeLinearMcpServer.save supports donor graph/inverse/initial native state. Later state-history recording remains outside M2.
- tests/tracker/test_milestone_approval excludes only actual runtime scope-resolution test `test_actual_milestone_scope_uses_current_project_approval`, owned by M3. Other2 actual adapter approval tests retained.
- test_native_approval_aliases preserves approval arm assertions while selecting approval instead of the donor approval+spec parameter census. Spec/readiness arm belongs to M3; this is explicit migration scope, not retired oracle.
- test_issue_identity_boundary exercises the public upsert workflow and is not imported. Existing original split identity/native-field controls are imported unchanged. Public upsert migration remains M3.
- Existing Makefile strict typing excludes tests. Source uses typed dataclasses/Pydantic contracts and existing exhaustive dispatch; no Any/cast/ignore escape added to source.

## Evidence

Selected actual Organize/native boundary control command and log: `m2-fifth-actual-tests.log`,687 passed77.08s. Selection includes all test_organize modules across chain/core/domain/integration/tracker/prompts/types, criterion creation, native artifact fields/split identity, body revisions, scope/milestone/native-alias approval, topology and portability.

After required scope-label correction, `make format-check lint type-check` passed465 formatted/Ruff-clean files and214 strict source files (`m2-freeze-static.log`). Premerge source snapshot `daae8df4c4e09499f0401c9406251ec88af9a8dd` handed to independent reviewer before any retry or prompt correction.

First maintained-base full check caught a duplicated AppConfig.write_back annotation introduced by auto-merge before pytest (`m2-maintained-base-full-check.log`). Mechanical successor73cc removes only duplicate; inherited M4 declaration remains. Current immutable `UV_PYTHON=3.12 UV_LOCKED=1 PATH=/Users/kodezart/.local/bin:$PATH make check` at73cc is running, log `m2-maintained-base-full-check-corrected.log`; format468/Ruff/strict215 already passed. Do not mutate writer while it runs.

All earlier failed selections remain preserved in m2-first/second/third/fourth-actual-tests.log. The first two exposed missing extraction fixtures/config/namespace/surface closure. The next two exposed deferred runtime test imports and missing fixture constants. No timeout or source oracle weakened. `m2-composed-mypy.log` was an accidental read-only donor check and is NOT candidate evidence.

## Finite remaining acceptance work

1. Settle current immutable full gate and repair any exact census or collaborator extraction omissions without weakening tests.
2. Fix independently reproduced native criterion known-unsent retry lease expiry using complete retry-attempt revalidation and preserve readback outside resend scope. Original expired control fails/live control passes on pre-correction source; frozen probe `test_m2_criterion_retry_independent.py`, log `m2-independent-criterion-retry-daae.log` (1fail/1pass16.08s). Original criterion source/oracles remain unchanged pending settled gate.
3. Resolve independent criteria-author prompt/schema mismatch after concrete baseline proof; preserve original prompt bytes until diagnostic completes. No new runtime capabilities inferred.
4. Independent all-lens review plus root publication of actual maintained M2 PR, naming acceptance pending until above closes. Source/proposal/schema controls do not alone accept whole L2 lane.
5. Root serializes shared scope-label/input hunk ownership into M1 and remaining milestone migration; no duplicate implementation or unassigned exclusion.

Backend leases and rereads are observed consistency, not atomic backend fencing. M3 walking, readiness and M6 terminal aggregation remain separate owned work. Requested Astra/high effective runtime cannot be independently verified here.

Linear contract references: https://linear.app/duckburg/issue/KOD-74#comment-eba027b8-8907-4c41-aaa8-5fde6520016b ; https://linear.app/duckburg/issue/KOD-74#comment-201c264e-7ee0-47e2-9617-a6689003a99d ; https://linear.app/duckburg/issue/KOD-74#comment-f227405a-5fb3-400a-83f1-2861991b9d5c . Parent owns external comments/PR/initiative/Notion.

## Settled baseline and finite successors

Original immutable73cc full Makefile result:70 failed,4752 passed,16 skipped590.06s; strict215/Ruff468 passed. Completed correction history in one isolated branch, no source/test edits during any test run:

- a15b33484bc0f88975c910c1706edf20805d98f8: independent criterion retry+prompt correction,130author controls and44fresh peer controls green; accepted https://linear.app/duckburg/issue/KOD-74#comment-d5557112-901c-486f-bfbe-b483d142d91a .
- 4ecd7c72d155fb9acedc832c32ee016db7ef6749:5fixture-only collaborator lines;69 passed19.62s;95 original assert/raises/fail ASTs identical, peer accepted.
- 25377b4:4file prompt/config/cutover census. Exact19→23 literal prompt count,3 actual OperationConfig field names,6 exact donor placeholder rows and actual native organize_scopes consumer, exact donor input-tag tuple/four role cases.128 passed3.14s (`m2-remaining-prompt-censuses-corrected.log`). Tests retain closed totality assertions; expected inventories now cover actual capabilities.
- fadf6efe29c7addcc608b22e8345cd9008ce4bab:4file native schema/surface census plus remove one undefined later-state-history fake assignment accidentally extracted with create_issue. Adds exact two original donor schema model mappings and forwarding caller rows; two supported graph/split surface census entries and removes those newly supported surfaces from unavailable-surface parametrization. Original actual positive graph/split/native-readback controls retained and green.309 passed6.74s (`m2-native-censuses-corrected.log`), Ruff/format/diff-check green.

Latest clean branch `codex/v03-m2-organize-boundaries` tree `73b6570a22879e20a8da3c068cc74fa95375f0fe`. Root review/integration owns maintained M2. Subsequent census changes deliberately update expected declaration values, so the initial238-function donor assertion equality evidence describes the extraction snapshot, not every later corrected inventory. No property is dropped: closed-set equality stays closed; actual new capabilities gain existing positive controls.

Remaining original fullgate category:34 shared boot tests blocked by absent scope-label bootstrap mappings/instatement and corresponding owned/configured mapping inventories. Actual source owner is M1; `services/tracker_boot.py::OWNED_REF_BUILDERS` and configured mappings lack scope labels even though OperationConfig.CONFIG_OWNERSHIP declares scope_labels OWNED. Root was notified and serializes this shared extraction; author has not edited that source. Composed fullgate remains required after root integrates that shared closure and reviewed M2 successors.
