# KOD368 bounded implementation proposal

Read-only starting SHA: 4c8b876828de95e9ee56262a0867b7cc488fbe70. No source edits or tests run during reconnaissance. Requested High; effective runtime unverified. Implementation paused for the root-assigned ten-failure CI follow-up; shared contracts below remain proposals until root approval.

Live KOD368 Check and latest comments were read alongside KOD74. Direct parent, blockedBy, relatedTo, priority, milestone and splitting are settled Organize responsibilities. Existing UnavailableProposal graph capabilities and author prompt refusals are implementation gaps, not founder-owned choices. Only evidence-backed unresolved human choices may use the existing decision path. No approval, criterion Done, FIRE or merge authority is added.

Source findings: TrackerIssue already has every graph field required, including project_id and milestone_key. No TrackerIssue or OperationConfig schema addition is required. OrganizeAuthor and OrganizeAdmission currently see issue bodies but no structured graph facts; their freshness checks use a body-only digest. Metadata-only graph changes therefore require a graph context bound to admission and author evidence, including the phase-marker check. Existing plan_topology owns dependency cycle detection. Existing IssueIdentity and LinearIssueIdentityCarrier own scope/deliverable idempotence; upsert_issue cannot be reused directly as create-only because it overwrites existing title/body. Existing RulingRecordReader reads native ruling occurrences; do not invent another parser or ruling identity.

Proposed closed proposal variants: GraphProposal contains a nonempty tuple of discriminated SetParent(nullable native parent), SetBlockedBy(native keys), SetRelatedTo(native keys), SetPriority(existing IssuePriority), SetMilestone(nonblank native milestone). Reject repeated properties, self/duplicate/unknown identities and contradictory changes before writes. SplitProposal contains nonempty children with stable deliverable_key/title/body; new ordinary children inherit actual source team/project and have the source as parent. Existing children are never overwritten on creation replay; later edits address their own minted surfaces.

Shared surface approval requested: ISSUE_GRAPH owns one issue's parent, priority, milestone and outbound dependency/related facts; acquire peer ISSUE_GRAPH surfaces when inverse relations or parent membership also change. ISSUE_SPLIT_SET is source-parent-addressed creation membership plus initial contents, with no existing-child edit authority. No alias to issue description or criterion membership. Proposed TrackerPort additions: update_issue_graph(issue_key, expected: IssueGraphSnapshot, changes: tuple[GraphChange, ...], holder) -> TrackerIssue; create_split_if_absent(source_key, deliverable_key, title, body, holder) -> TrackerIssue; read_split_children(source_key) -> tuple[TrackerIssue, ...]. Split creation uses IssueIdentity(ScopeRef(ISSUE, source_key), deliverable_key), duplicate identities refuse and replay returns the actual existing child unchanged. Adapter checks its declared grant immediately before every save. Creation gets exactly one native UNSTARTED state using the existing criterion lookup factored into _unstarted_state_id(team_id: str) -> str, coordinated with the native amendment owner before touching it.

One L2 graph-context reader will give admission and author current scope members, graph fields, complete dependency/parent closure and actual recorded rulings, with deterministic identity validation. The typed admission result binds the exact context snapshot/digest, and is_live re-reads it. The author returns its exact source context. Immediately before each write, recheck current membership, source body and graph context, phase and final approval. Validate proposed parent ancestry and dependency cycles using existing topology logic; related links do not manufacture blockers. Validate milestone identity through container_metadata and its owning project. Do not treat aliases or prose as native identities.

The owner selects the validated proposal's surface before entering existing WriteBackVerifier; repair dispatches a fresh author, stays on that surface, and uses the existing configured bound. Extend existing tracker_artifacts with deterministic real ISSUE_GRAPH and ISSUE_SPLIT_SET readback, preserving native IDs, parentage and actual fields. No reconstructed save response, second verifier or private audit loop. Splitting does not authorize parent-body rewriting under its creation surface; any parent-body amendment uses the existing separate body proposal/verification path.

Exact proposed files: new types/domain/organize_graph.py, domain/organize_graph.py and services/organize_context.py; existing types/domain/organize_owner.py proposal section, types/domain/organize.py admission context binding, chains/organize.py, chains/organize_author.py, services/organize_owner.py, services/tracker_artifacts.py; narrow core/protocols.py TrackerPort additions and adapters/linear_mcp_tracker.py/fake methods; root-owned types/domain/surface.py enum dependency and narrow composition/organize.py context-reader constructor wiring; domain/prompt_variables.py and both prompt sets' organize_assess/verify/author/criteria_author inputs, their golden/census tests. No scheduler, operation settings, RunShape, RulingId, CriterionId or unrelated adapter sections. L8 alarm owner confirms only new alarm read/write methods overlap the adapter file; upsert_issue is untouched. Native owner has not yet frozen the UNSTARTED helper and requests coordination.

Current callable Linear save_issue schema inspected without issuing writes: parentId accepts string|null; priority accepts number; blockedBy/relatedTo accept arrays with separate removeBlockedBy/removeRelatedTo arrays; milestone accepts string only; create uses title/team/description plus supported assignment fields. Use native IDs and exact deltas, never infer replacement semantics from an empty relation array. Milestone clearing has no documented nullable argument: do not invent null or an empty-string sentinel; record this precise backend capability residual and typed-refuse that operation until supported. Official references: https://linear.app/docs/mcp, https://linear.app/docs/issue-relations, https://linear.app/docs/parent-and-sub-issues, https://linear.app/docs/project-milestones. Tool metadata supplies the actual callable argument contract; product docs establish semantics, not conditional mutation guarantees.

Counterexample plan: reproduce graph UnavailableProposal against actual configured factory; metadata-only author drift and admission drift must refuse; wrong output identity, unknown native keys, duplicate properties, self/parent/dependency cycles, wrong-project milestone and moved scope members refuse before writes. Use real production adapter with transport boundary doubles to verify exact add/remove/parent/priority/milestone payloads and fresh readback. Replay unchanged complete scope produces zero save calls, including split identity replay; duplicate persisted split identities refuse without overwrite. Approval during awaits, peer grant conflict, cancellation, expired lease and delayed create must retain established behavior and characterize the known in-flight backend limit. Fresh independent writeback must see actual graph/children, preserve refutation citations and configured rounds, and stop without dry spinning. Criterion creation remains preparation only; approved labels and workflow Done are absent from payloads.

Eight lenses: SOLID keeps semantic author, deterministic validators, adapter transport and canonical verifier distinct; DRY reuses identity, ruling, topology and verification infrastructure; Hexagonal keeps vendor fields inside adapter; KISS uses closed operations and existing bounds; typed-agent calls extend the existing registered proposal schema, no heuristics; official framework/backend references are checked above (Pydantic remains locked 2.12.5); type safety binds source graph evidence and forbids malformed identities; hygiene requires focused before/after tests, unchanged independent oracles and strict/Ruff gates. Risks: no atomic backend uniqueness or fencing is claimed, sequential read/check is not a transaction, and unsupported milestone clearing remains an explicit capability limit. Integration verdict: proposal only, no implementation to integrate yet.

Owning links: https://linear.app/duckburg/issue/KOD-368 and https://linear.app/duckburg/issue/KOD-74. No public comment posted during this reconnaissance.
