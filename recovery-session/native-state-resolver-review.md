# Native state resolver prerequisite

Author freeze: 53b3ca89efec37b23570bf6cfd78780cc2acddef, parent68187b03, clean `/private/tmp/kodezart-v03-recovery-native-state-resolver`. Four files: adapters/linear_mcp_tracker.py, adapters/linear_mcp_types.py, tests/fakes.py, tests/tracker/test_criterion_creation.py. No graph methods or reset writer included. Native worker consumes `_unstarted_state_id(self, *, team_id: str, issue_key: str) -> str` after independent/serialized integration.

The actual team-state listing supplies native id, type and name. The old implementation selected the unique unstarted name. The new shared helper requires the actual nonblank id, retains typed invalid-payload and zero/multiple-state refusals, and the criterion creator sends that exact ID. No guessed Todo, new lifecycle enum, setting or caller fallback. Official current Linear MCP list_issue_statuses/read-only response was checked: native id/type/name present, unique unstarted per observed team; save_issue accepts state ID or name. Private native values are not embedded in source.

Before proof: unchanged source from parent with new boundary controls, `uv run --locked pytest -q tests/tracker/test_criterion_creation.py -k 'requires_actual_nonblank_native_id or uses_the_selected_team_id'`: 4failed16deselected0.51s, native-state-resolver-before.log. Original source accepted missing/empty/blank native ID and sent the display name. Candidate selected command `uv run --locked pytest -q tests/tracker/test_criterion_creation.py tests/tracker/test_linear_wire_shapes.py tests/tracker/test_linear_tool_arguments.py`:60passed4.13s, native-state-resolver-tests.log. Strict mypy two changed adapter source files passed, native-state-resolver-mypy.log. Ruff check and format check fourfiles passed; diffcheckclean. Before source restoration ran in this exclusive donor and restored exact tested candidate bytes.

Eight lenses: SOLID cohesive backend state lookup; DRY shared helper used by creation and reusable by reset; Hexagonal backend IDs stay in adapter, neutral port untouched; KISS one small extraction and native field requirement; typed calls no agent/schema semantic change; official tool schema/current backend response verified; type safety missing/malformed native field refuses and zero/multiple unstarted remains typed; hygiene exact payload/state/identity/no-replay assertions retained, no Any/casts/ignores or new model_copy. Boundary fake now resolves the actual native ID supplied by the production adapter, rather than assuming state arguments are names.

Risk/dependency: shared ruling-reader681 prerequisite is ancestry, not a semantic dependency of state lookup. Root owns separate native comment provenance corrections; these must remain intact on integration. Backend no conditional fencing remains unchanged; the existing delayed-create characterization is retained. No full KOD368 or KOD97 completion claimed, no issue-state edits/push/Notion/integration. Requested High/effective runtime unverified. Logs are in /private/tmp/kodezart-recovery-session/.
