# M3 independently reviewable source/hunk ownership

Actual base fadf6efe29c7addcc608b22e8345cd9008ce4bab, donor da39c439898aec1233aa8b6157b35e961df1e453. Destination /private/tmp/kodezart-v03-m3-plan-walk, codex/v03-m3-plan-walk. The machine-readable final map records each destination hash, exact-donor equality and shared-test assertion AST census. m3-extracted-hunks.json records source symbol and donor line spans; m3-current-donor-map.json is the independently recalculated current census, not an assertion that every copied donor module is owned in full.

## Whole actual modules

The source modules listed as whole in m3-extracted-hunks.json retain exact donor bodies: five fire phases, shared Ralph loop/fire graph, authored delivery/publication/checks, native execution/amendment graphs and services, current criteria reader, scope planner/readiness/dispatcher/membership/resolution, source/workspace/criterion/amendment typed states, Git persister/workspace adapter, executor/AgentService invocation carriage, native prompts. No native delivery coordinator, scope-runtime controller or audit runtime is transferred.

## Partial source ownership

- core/protocols.py: FireCriteriaReader/Source, NativeWriteGuard, TrackerContextReader; Git worktree identity; Workspace capture/resume; actual run identity/native guard/current criteria arguments; CI typed observation/rerun/declared-check methods; tracker read_fire_spec/reset_criterion_pending. Existing M2/M4 contracts remain their accepted source. No delivery graph protocol added.
- types/domain/operation.py: CheckPrerequisite/CheckStep and RepoEntry checks/runner_environment/forge_exempt, owned once by first actual M3 authored-check consumer. Existing unrelated Initiative/config fields retained pending actual M1 migration.
- types/domain/agent.py: current native/amendment/criterion payload classes and four actual schemas; required invocation/session event; authored completion and delivery-free fire completion carriers. RaiseSite gains only actual amendment roles; unrelated audit roles excluded.
- types/domain/run_event.py: exact unchanged 18-member RunEventKind primitive only, assigned M4 by root and pending inherited ancestry. No event-state806 table, effect, publisher or lifecycle source.
- types/domain/delivery.py: only CheckRedClass and CheckRedObservation used by authored checks. No NativeDeliveryState or delivery graph.
- types/domain/branch.py: WorkRefLanding plus WorkRef.landing; types/domain/dispatch.py: exact criterion_keys report field. No future coordinator state.
- adapters/github_api.py + adapters/github_types.py: exact typed check read/rerun implementation and consumed wire models, existing PR methods preserved. Existing types/domain/github.py is relocated once to the adapter boundary; no alias/duplicate. No M5 PRStateReader/ForgeQuery runtime copied.
- composition/forge.py: exact RetryPolicy construction for existing actual GitHub adapter. Current flat M1 settings retained; no private defaults or settings shim.
- adapters/subprocess_git_service.py: exact Git worktree identity/owned subprocess cleanup and neutral domain error translation required native workspace receipts. No M5 scratch-head merge.
- adapters/linear_mcp_tracker.py: required criteria_stage_label_key parameter/field; exact read_fire_spec/reset_criterion_pending. Existing M2 retry correction and required scope_labels retained. composition/tracker.py binds stage key from actual criteria mandate terminal marker; no invented stage default.
- core/config.py: exact two donor authored CI bounds and validations. Existing explicit write-back settings inherited. M1-owned unrelated config packaging remains unchanged.
- composition/engine.py: actual donor authored/native graphs and guard source; only final M5 scoped-arm imports/assembly omitted per root ownership correction. main.py supplies actual repositories/tracker criteria/operation, preserving existing lifecycle assembly.
- adapters/asyncio_job_queue.py: actual WorkflowEngine invocation identity/scope arguments only. No M5 API event serialization or alternate queue.
- adapters/_mcp_mapping.py: exact optional run identity/fire-record prompt arguments in existing knowledge-map helper. No recording runtime is configured or claimed.
- domain/errors.py, pr_body.py, prompt_variables.py, stall_report.py: only exact symbols consumed by above actual M3 modules.
- prompt manifests: exact native_writer_contract utility and amendment_judge/amendment_author role entries; preserve existing M2 prompt correction and omit unrelated M6 audit/fire-record roles.

## Test packaging corrections

- All destination test functions shared with donor retain original assertion/pytest.raises/pytest.fail ASTs, unless a later final report explicitly records an inventory-only delta. Shared helper/caller updates bind current typed ports and preserve original behavioral oracles.
- Every old LinearMcpTracker construction declares criteria_stage_label_key=None where no stage is configured; actual native tracker fixture declares FIRE_STAGE_KEY/FIRE_STAGE_LABEL and native approval input. This is fixture routing, not an optional production boundary.
- GitHub client fixture/test implementation is exact donor's required RetryPolicy and typed check observations; the initial 50 CI failures were one stale fixture call and are preserved as diagnostic evidence.
- Git persister admission test remains exact base because donor's whole diff is M1 admission replacement. Fire-context admission helper retains current actual gate and adds the exact stronger donor tracker-prefix assertions. No compatibility admission runtime introduced.
- tests/domain/test_amendment.py excludes only the unchanged API scope-egress test assigned with M5 scope binding. tests/tracker/test_criterion_resolution_consumers.py is wholly M6 audit execution, mapped there after inspecting all four consumer branches.
- test_forge_origin_selection receives only actual authored/typed-CI fixture helpers and explicit scope driver/forbidden arm; M5 PR-state reader and native delivery origin assertions remain M5 integration obligations.

No reset, force, donor mutation, maintained-tree edits or PR creation performed by this worker. Root integrates accepted ancestry and publishes the single destination.
