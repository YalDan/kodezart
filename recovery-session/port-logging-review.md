# Independent bounded port and logging review

Verdict: **integrate 219d132 and 5b4ae92 with their declared dependencies; no blocking source finding**. This does not approve broader KOD-163 completion, surface-lease enforcement, repository composition, full queue liveness, or release gates.

Exact reviewed HEAD: `5b4ae925b5d7543d64dd25c3f1d9ffd7a003a7ef`; source comparison `c96895e18d3137544ea30d18188006118ee8a6e5`. Worktree remained clean at completion. Author commits reviewed: `219d132` and `5b4ae92`. Preceding `ff7fc9e` / `2574358` domain error definitions are source-only dependencies; the actual surface implementation belongs to another worker. Review-authored source commit SHA: **none**. Repository files changed by reviewer: **none**. Effective runtime model/effort was not independently verifiable.

Current KOD-163, KOD-61 and KOD-146 issue descriptions and comments were fetched live. Contract review covered neutral consumer vocabulary, safe retry/cancellation behavior, explicit queue concurrency and original ordering, and formatted chain-level exception frames. Procedural limitation: the author evidence index was accidentally opened before the live contracts/source, contrary to the requested ordering. All conclusions below were subsequently checked against source and independent execution; author pass counts were not treated as my runs.

## Source findings and oracle assessment

No blocking source defect found. Every `LinearMcpTracker` tool call funnels through `_call`; its credential arm now raises `TrackerAccessDeniedError` with the original cause and does not consume retries. The existing `_may_resend` decision still permits harmless unanswered reads and refuses unanswered writes. Exhaustion raises `TrackerUnavailableError` without authorizing replay. Cancellation and programming exceptions are outside both adapter catches. Adapter-local catches downstream of `_call` were migrated, including capability/criteria reads, roster resolution, and marker cleanup. The five consumer modules migrated are `claim_heartbeat`, `escalation_records`, `lane_records`, `pass_gate`, and `ruling_records`. Source searches found no MCP exception imports left in service/handler/chain/composition/API consumers. Existing broad service policy, such as heartbeat logging unexpected renewal failures, is unchanged; the programming-error escape claim applies to the repaired adapter boundary, not every historical application catch.

Both concrete record sink public methods encompass their validation/read/publication paths in the adapter-owned shared boundary. It classifies concrete MCP failures, retains the original cause and destination/run fields, and includes the diagnostic message. Existing typed mapping/identity failures pass through unchanged. `RunRecorder` now knows only the sink contract; its verify-before-write control flow is unchanged and no retry loop was introduced. Source search showed that it is the only production caller of the two record methods and composition wires the two inspected concrete implementations.

The legacy `RefusingRecordSink` fake now invokes the production mapping helper. Therefore those consumer tests alone are a circular mapping oracle and are counted only as consumer behavior checks. The submitted actual-adapter matrix explicitly asserts expected failure values and cause identity, so it independently checks mapping. A narrower limitation exists: its Notion `write=True` branch fails at the initial schema read, before publication. I closed that evidence gap with external controls that drive the real recorder and real sinks, answer verification/schema reads successfully, then raise at `save_document` or `API-post-page`. They assert a single publication and retained remedy/cause/destination for unanswered, session-closed, credential-refused and transport failures. These controls are ready for root to retain as regression tests; the reviewer made no repository edit.

Logging changes only pretty-mode formatter configuration and direct dependency declaration. JSON still uses the chain's `format_exc_info`; pretty mode still receives the exception triple and renders source frames, now with locals disabled. The live-local test uses actual `configure_logging`, not `capture_logs` or a fake renderer, and checks zero calls to the object's stateful `__repr__`. Existing frame tests exercise direct, queue and scheduler JSON emissions and the pretty renderer. Queue source and the two original integration controls are byte-unchanged in this diff: 10-second deadline, log-before-error sequencing, scheduling and concurrency knobs are preserved.

## Actual independent execution

All behavioral runs used `/Users/kodezart/.local/bin/uv run --locked` because this shell did not have uv on PATH.

1. `python -m pytest -q tests/adapters/test_record_failure_boundary.py tests/tracker/test_port_failure_boundary.py tests/tracker/test_linear_mcp_tracker.py tests/core/test_logging_chain.py 'tests/integration/test_issue_key_carriage.py::test_the_appended_identity_can_block_the_entire_pr_write'` — **100 passed in 19.56s**, exit 0. Log: `/private/tmp/kodezart-recovery-session/independent-port-logging-focused.log`. Includes the actual HTTP MCP caller against a boundary endpoint that drops a call, read recovery, unanswered-write no-resend, credential refusal/backoff, both `[False]` and `[True]` original queue cases, and all five configured logging controls.
2. `python -m pytest -c pyproject.toml -q /private/tmp/kodezart-recovery-session/test_independent_port_publication.py` — **10 passed in 9.93s**, exit 0. Log: `/private/tmp/kodezart-recovery-session/independent-port-publication-configured.log`. Eight publication-phase controls use production recorder/adapters and only a boundary MCP caller double. Two tracker controls prove cancellation/KeyError identity propagates under a retry budget.
3. `git diff --check c96895e..HEAD` — clean. Final HEAD and clean worktree reconfirmed. No full tracker directory or full suite was run.

External ready-to-copy regression file: `/private/tmp/kodezart-recovery-session/test_independent_port_publication.py`.

Invalid independent attempts: an initial bare `uv` invocation failed command lookup before execution; the first external pytest invocation omitted `-c pyproject.toml`, so async functions were not executed (10 collection/execution-configuration failures). That diagnostic is preserved at `/private/tmp/kodezart-recovery-session/independent-port-publication.log`; it is not a product failure or counted evidence. Corrected configured run above is the result.

Inspected author evidence, separately attributed: baseline adapter regression log ends with the expected two failures; logging-before log shows expected zero repr calls versus observed one; frozen broader queue/logging log reports 101 passed in 14.48s; `root-frozen-mypy-5b4ae92.log` reports success across 290 source files. I did not rerun that broad selection or mypy. The repository Makefile explicitly excludes tests from typing; no claim of whole-repository type cleanliness is made.

## Eight lenses, limited to this diff

| Lens | Assessment |
| --- | --- |
| SOLID | Adapter substitution improves: consumers handle port failures; recorder loses concrete transport responsibility. No new interface hierarchy. Wider TrackerPort decomposition remains outside this review. |
| DRY | One adapter-owned record mapping replaces service classification and serves both sinks; retry implementation remains unchanged. Shared test helper does not serve as the independent mapping oracle. |
| Hexagonal | MCP taxonomy and transport-specific retry decisions stay in adapters; neutral failures cross consumer ports with original causes preserved. Existing transport protocol types in core are not redesigned here. |
| KISS | Two tracker failure classes, one narrow context manager, and one formatter option. No retry subsystem, queue rescheduling, or additional configuration introduced. |
| Typed agent calls | No agent-call protocol, output schema, prompt, or judgment path changes; existing deterministic MCP calls retain their typed boundary. This lens is unchanged, not a broader validation claim. |
| Official framework/version match | Lock resolves structlog 25.5.0 and Rich 14.3.3. Official matching sources confirm supported `RichTracebackFormatter(show_locals=False)` and conditional traversal of locals. Rich is now a direct requirement while the resolved package stays 14.3.3. |
| Type safety | Source improves exception vocabulary with no new Any/cast/ignore bypass. Shared helper has typed arguments and `Iterator[None]`. Inspected strict source-only mypy passes 290 files; new test functions remain under the existing excluded test policy. |
| Hygiene | Clean frozen checkout and diff whitespace check; scope limited to adapter/error/consumer/logging changes plus regression controls. No production queue or timeout changes. No repository edits, commits, integration, pushes, Notion work, or issue-state updates by reviewer. |

Official sources checked: [structlog 25.5.0 formatter implementation](https://www.structlog.org/en/25.5.0/_modules/structlog/dev.html) and [Rich 14.3.3 traceback implementation](https://github.com/Textualize/rich/blob/v14.3.3/rich/traceback.py). The former defaults locals to True and forwards the option; the latter gates local-value traversal on that option.

Residual boundaries: disabling locals does not make arbitrary log handlers or exception stringification nonblocking; this proves the identified local-repr mechanism and observed queue regressions. Tracker protocols could document the new shared exception vocabulary more explicitly, but the current adapter/consumer behavior is consistent. Surface lease concurrency/loss enforcement and full integration remain separately owned. KOD-146 lifecycle restoration is unchanged and not regraded.

Published own conclusive review evidence, public-safe and without private paths: [KOD-163 independent review](https://linear.app/duckburg/issue/KOD-163#comment-2243da2d-11dd-4319-9850-76d5d1dd8165), [KOD-61 logging review](https://linear.app/duckburg/issue/KOD-61#comment-dc4e3fd0-b88f-4df4-a422-60534edd31e1). No state changes made.
