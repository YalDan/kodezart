# Independent M1 correction review

Accepted bounded correction **57dcc8c3ae4836207307a715812fe389c301e130**, parent **d6e40fdcccdaaed5e6fb52860a29e0811e9cf3a4** (90aea37 composed onto root's b0def20 ancestor-corrected M1). New detached tree `/private/tmp/kodezart-v03-m1-lease-corrective-independent-review`; production source unchanged; only own prompt-probe module untracked. Root separately reviews our original90 authoring; this independent review covers only root's9-file19+/8− correction.

Source: six-line existing donor `_bind_absentable` marker_prefixes call, three exact configured repository-marker prompt substitutions; table/census/recursive-settings helper and four expected terminal-comment prefix migrations. No new public port or type. The configured binding call is inserted among existing map bindings; its keyed consumers are unchanged. No later milestone source enters this correction.

The four lifecycle controls still assert exact one-element complete body lists, with existing queue/state/outcome behavior retained. Exact field-set, placeholder-to-field map equality and template reachability assertions remain. Shared settings census now resolves nested fields instead of pretending only top-level names ship. No strip/substr/containment substitutes for those original oracles.

Independent commands/results:

- `uv run pytest -q tests/services/test_lifecycle_watcher.py tests/prompts/test_operation_config.py tests/docs/test_setup_guide.py`: **105 passed10.91s**, m1-marker-corrective-independent-57dcc8c.log.
- `uv run pytest -q tests/services/test_run_surface_lease.py tests/services/test_tracker_lifecycle.py`: **44 passed4.43s**, m1-marker-corrective-owner-independent-57dcc8c.log. These are the other44 of the root's149 selection; no duplicated tests counted.
- Own three real configured-prefix renderer probes: **3 passed0.27s** on57, m1-prefix-render-independent-57dcc8c.log. Same copied bytes executed externally with original90 source via its uv project/PYTHONPATH: **3 failed5.23s**, m1-prefix-render-independent-before-90aea37.log. Each fails the required distinct-prefix assertion on90; no source edit needed to reproduce.
- `uv run mypy src/kodezart/core/prompt_namespaces.py`: success, one changed source module; m1-marker-corrective-mypy-57dcc8c.log.
- Ruff on changed Python files and own probe: clean. `git diff --quiet`: clean source. Only own test untracked.

Eight bounded lenses: SOLID existing configuration binding owner; DRY shared recursive config census; Hexagonal actual registry/renderer and owner consumer checks; KISS small exact donor substitutions; Typed agent calls instead of semantic heuristics configured marker values; Official framework practices current repository renderer/Pydantic/pytest; Type safety neutral, no widened public types; Repository hygiene separate immutable review and retained failed oracles.

Integrate57 only atop composed d6e40fd after root's independent90 review. Do not reset/rewrite original90 or erase its full9-red diagnostic. Corrected full make check remains a separate integrated gate. No full L1, universal write migration or backend CAS/fencing claim.
