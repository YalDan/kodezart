exec(open('/private/tmp/kodezart-recovery-session/extract_m1_scope.py').read().split("p='src/kodezart/types/domain/operation.py'")[0])
copy('tests/tracker/test_linear_tool_roster.py')
p='tests/tracker/test_linear_tool_arguments.py';replace(p,'\n\n@dataclass(frozen=True)','\nfrom tests.tracker.connected_app_label_contract import (\n    CONNECTED_APP_LABEL_ARGUMENTS,\n    CONNECTED_APP_LABEL_REQUIRED,\n)\n\n\n@dataclass(frozen=True)')
d=source(p);a=d.index('#: Original service capture');b=d.index('async def sent_arguments',a);replace(p,'async def sent_arguments',d[a:b]+'async def sent_arguments')
replace(p,'            MappingRef(\n                kind=MappingKind.DOCUMENT,\n                name="a document nobody holds",','            MappingRef(\n                kind=MappingKind.SCOPE_LABEL,\n                name="approved",\n                identifier="scope:brand-new",\n            ),\n            MappingRef(\n                kind=MappingKind.DOCUMENT,\n                name="a document nobody holds",')
s=(R/p).read_text();a=s.index('async def test_the_sweep');s=s[:a]+s[a:].replace('LIVE_INPUT_SCHEMAS','DECLARED_INPUT_SCHEMAS');(R/p).write_text(s)
p='tests/tracker/test_tracker_boot.py';replace(p,'                "issue_labels": {"criterion": "Configured Check"},','                "issue_labels": {"criterion": "Configured Check"},\n                "scope_labels": {\n                    "triage": "scope:triage",\n                    "proposed": "scope:proposed",\n                    "approved": "scope:approved",\n                },')
p='tests/prompts/test_operation_config.py';replace(p,'        "queue_states",\n        "issue_labels",','        "queue_states",\n        "scope_labels",\n        "issue_labels",');replace(p,'    assert isinstance(config.queue_states, dict)','    assert isinstance(config.queue_states, dict)\n    assert isinstance(config.scope_labels, dict)');replace(p,'        *example_config().queue_states.values(),','        *example_config().queue_states.values(),\n        *example_config().scope_labels.values(),');replace(p,'    assert native == {"issue_labels": "composition/tracker.py::build_tracker"}','    assert native == {\n        "issue_labels": "composition/tracker.py::build_tracker",\n        "scope_labels": "composition/tracker.py::build_tracker",\n    }')
p='docs/cutover_mapping.md';edit(p,lambda s:s.replace('| scope_labels.triage | scope_labels |\n| scope_labels.proposed | scope_labels |\n| scope_labels.approved | scope_labels |\n',''));replace(p,'| issue_labels | composition/tracker.py::build_tracker |','| issue_labels | composition/tracker.py::build_tracker |\n| scope_labels | composition/tracker.py::build_tracker |')
