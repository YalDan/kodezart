# Native identity cancellation correction — author evidence

Frozen source c579eb9da9425ac4f720d9449a9b0774411e43a6; tree8ec94aa03ee8242ee45df9da0e4772099b9fcc0b; direct parent3508980103cabbb0b28bfe49571293743b42d561. Worktree `/private/tmp/kodezart-v03-native-identity-cancel-correction`, branch `codex/v03-native-identity-cancel-correction`. Root reassigned this worker from independent review to author only after the finding/report was recorded. This is author evidence requiring root independent review; no self-approval.

Before evidence remains `native-identity-cancel-before.log`: the earlier author actually executed the public identity/source-reader cancellation pair at350, **1 failed(identity),1 passed(source) in1.05s**. Independent source/probe review confirmed the owned-child leak before this worker wrote any source. Finding: https://linear.app/duckburg/issue/KOD-105#comment-263d736d-d727-4dd6-b31f-30501c5dd7e3 . This resource finding is separate from previously accepted native checkpoint/preparation repairs at350 and does not prove duplicate committed effects or lost history.

Only two changed files:

- `src/kodezart/adapters/subprocess_git_service.py`: `_identity_git` and imports only. Apply the existing `SubprocessGitSourceReader._owned_command` lifecycle: retain and shield spawning/communication tasks, start an owned session, finish spawn on cancellation, kill its process group, drain/reap through `finish_owned`, then rethrow. Preserve binary return values, Git arguments, current native WorkspaceError path and cancellation. Existing helper reused without modification; no new abstraction, retry, replay state or unrelated Git behavior.
- `tests/adapters/test_native_identity_process_probe.py`: preserve the earlier actual two-arm subprocess probe; remove only its unused pathlib.Path import to satisfy Ruff. All test function ASTs and assertions remain identical. An exact-byte untracked copy `test_native_identity_process_original_peer.py` was actually executed instead of substituting the cleaned copy as the original oracle.

Exact frozen validation command (cwd author worktree):

```
/Users/kodezart/.local/bin/uv run --locked pytest -q tests/adapters/test_native_identity_process_original_peer.py tests/adapters/test_native_workspace_resume.py tests/services/test_git_source_lookup.py tests/core/test_owned_tasks.py
```

**31 passed in133.95s**, `native-identity-cancel-correction-affected.log`. These include the unchanged real-child cancellation pair, real Git serialized-workspace adoption with binary/NUL-delimited filenames, refusal of working/index/mode/symlink/holder/repository/registration/index-flag changes, actual pinned Git source absence/error semantics, and existing repeated-cancellation settlement controls. No native parent replay or complete repository gate rerun is claimed for this local subprocess-only change; those exact35057-test results remain separately recorded.

`uv run --locked mypy src/kodezart/adapters/subprocess_git_service.py` passed strict **1 source** (`native-identity-cancel-correction-mypy.log`). `uv run --locked ruff check` and `ruff format --check` over the two changed files pass (`native-identity-cancel-correction-ruff.log`, `native-identity-cancel-correction-format.log`); `git diff --check` passes. The first prefreeze Ruff diagnostic found only the original probe's unused import; it was removed before freeze. Runtime Python3.12.13, LangGraph1.0.10/checkpoint4.0.1. Source was frozen and tracked diff stayed empty throughout. Proof file `native-identity-cancel-correction-proof.json` records exact source/tree/parent and probe hashes.

Eight scoped lenses: SOLID—the existing Git adapter settles its own operation; DRY—reuse finish_owned and established source-reader lifecycle; hexagonal—real public adapter/OS process and real Git/provider integration; KISS—one local lifecycle, no new framework; typed agent calls—unchanged; official framework practices—shield owned tasks and explicitly terminate/reap subprocesses, consistent with Python3.12 asyncio documentation; type safety—neutral public/type contracts, explicit typed communication task; repository hygiene—two-file source freeze, original oracle preserved, no suppression or unrelated source mutation.

Primary documentation inspected: [Python3.12 subprocess communicate](https://docs.python.org/3.12/library/asyncio-subprocess.html#asyncio.subprocess.Process.communicate) documents stream draining and waiting for child termination; [Python3.12 shield](https://docs.python.org/3.12/library/asyncio-task.html#asyncio.shield) separates cancellation of the awaiting caller from the protected task. Online3.12 documentation currently labels3.12.14; runtime is3.12.13. No exact-tag-source equivalence beyond the previously recorded LangGraph source match is claimed for these online pages.

Limits: the owned process-group lifecycle follows the repository's existing POSIX subprocess boundary; Windows portability and hostile descendants escaping the group are not claimed. No process/host restart, PostgreSQL durability, external-effect transaction or backend fencing guarantee is added. The independently accepted350 correction still only releases workspace ownership before a writer starts, retaining later partial/uncertain evidence. Full L3/L9, KOD814 and KOD806 remain separate.

Integration: after root independent source/oracle review, apply this one commit **after4dd→a198→261→350** in the actual donor composition, retaining canonical shared Git/error hunks. Candidate parent is350; do not import its donor ancestry again. Root owns the actual composed gate, milestone hunk mapping, PR/publication and Notion changes. No source integration or issue-state change was performed here. This worker now resumes unrelated independent M4 ruling/source extraction review at783edbf.
