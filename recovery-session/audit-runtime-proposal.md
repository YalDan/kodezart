# Audit runtime: source-grounded interface proposal

Exact isolated start d14254e1b64b581693fd1032adbdaac57ccc9ce5, `/private/tmp/kodezart-v03-recovery-audit-runtime`, branch codex/v03-recovery-audit-runtime. Shared configuration/roster/scheduler hunks approved by root; implementation started. Read live79/535/537/539/540/518/522/527/531/533/538, latest820 and773 comments, plus direct170 founder95fb593a and amendmentaa6b3c21. HIGH requested/inherited; effective runtime unverified; no delegation.

Source facts: main calls verify_pass_preflight before queue startup, then build_dispatch_runtime; the latter registers only dispatch/FirePrep/Organize-or-Grooming. ScheduledPass already owns timeout/cadence, passes started_at to run, and supports generic run_report. AuditCoverage already owns disposable per-scope delta/full selection, but marks success immediately after successful visitor returns. AuditReadSweep has all existing read arms and deliberately returns partial/unavailable observations without coverage success. No actual production constructor or publication exists. GitSourceReader has a real SubprocessGitSourceReader; PRStateReader/CIMonitor have existing forge collaborators. FreshWriteBackJudge/WriteBackVerifier are restored and are the sole permitted verification mechanism.

Root accepted that additive RunKind.AUDIT is ordinary extension of the actual generic recorder, preserving the old values and operator per-kind destinations. Direct170 forbids duplicate/per-kind recorders and duplicate rows, not all future run kinds. No new SessionType is needed: existing SCHEDULED_PASS owns fresh audit sessions. NEW8203f13d3fc supersedes its old file-scope block; reuse existing judge. NEW77324d09172 reopens raw observations versus emitted verdicts: preserve raw unavailable observations internally, expose only validated complete reports on the publication boundary, and do not claim the whole773/516 fork settled. KOD806 lifecycle owner is explicitly held; no state mutation or inferred lifecycle plan is proposed.

Proposed shared configuration hunks, pending root approval:

- New core/audit_settings.py with frozen AuditSettings: required timeout_seconds positive finite float only. Root owns shared canonical WriteBackSettings(max_verify_rounds strict integer1..10) and optional AppConfig.write_back; Audit consumes that dependency without duplicating the bound. AppConfig.audit:AuditSettings|None=None. Existing audit_sweep_interval_seconds3600 and audit_full_sweep_interval_seconds86400 plus their bounds/cross-validation remain the cadence source. No new default/operator value.
- New frozen AuditScopeBinding in types/domain/operation.py: scope:ScopeRef, repo_url:str declared in existing repos, report_issue_key:nonblank native issue identity. OperationConfig.audit_scopes:tuple[AuditScopeBinding,...]=(). Duplicate/ambiguous scope binding and undeclared repo refuse. Actual native requests must route to this declared repo, or typed-refuse the whole binding; no silent filtering/Cartesian inference and no Organize roster reuse. Explicit per-repo issue scopes remain possible. The report issue is an explicit outbound destination, not inferred from a container key.
- Add RunKind.AUDIT='audit', retain prior values; update exact run-kind registry/census/native consumers. _session_running(AUDIT)->existingSCHEDULED_PASS. Generic run_report(recorder,RunKind.AUDIT,'audit') remains the structural record mechanism; operator records.audit is independently declared or named absent. No new recorder or record-side session.
- Require configured marker_prefixes['audit'] for observation/summary marker composition; reuse configured escalation prefix and issue_labels['decision'] when escalation is enabled by actual evidence. No hardcoded native marker/name/destination.
- composition/passes.py narrow imports/preflight and one ScheduledPass registration for the complete audit roster; main passes actual CI and PR-reader collaborators. Explicit absence emits audit_pass_not_wired; partial declared settings/roster/capability emits named reason then typed startup refusal before queue construction. No independent clock or timer.

Proposed local service seams:

- composition/audit.py constructs all actual read arms, one shared request/source reader, existing GitSourceReader, fresh sessions, and canonical FreshWriteBackJudge. Factory returns one AuditScheduledPass service over configured scope targets; `run(started_at:datetime)->PassRun` is the existing scheduler callable. Its interval/timeout are supplied from config by composition/passes.
- Extract AuditReadSweep.observe_target(snapshot:AuditRequestSnapshot,target:AuditRequestTarget)->AuditReadObservation from its existing loop, with the same current-head/source validation and all applicable detectors. Keep its existing zero-arg run API using that same method for existing consumers. Expose a preparation/final-source-validation method so scheduling does not reach private helpers or duplicate request resolution. It must not silently omit missing detector coverage.
- AuditCoverage.cover gains an optional explicit final-completion callback receiving its actual AuditCoverageResult, invoked after visits and before committing marks. The runtime always supplies it. Final freshness, every required publication and summary success are inside this callback. Failure/cancellation/incomplete observations advance no mark; scope failure does not prevent a later independent configured scope from being attempted. No fresh result is reconstructed from cached verdicts.
- Audit publication consumes complete typed report variants, retains current criterion/native record/head identities and actual mandate payloads, and raises permitted escalation at discovery before summary. Each native observation/comment and summary marker is leased/gated, reread and verified through WriteBackVerifier. Existing LaneEscalationWriter may be used only with canonical verification of both comment/label steps and a final source guard; no second escalation or verification algorithm. A damaged/incomplete source never becomes a report claim. Unverifiable ordinary observations produce no issue-state or escalation writes. Held refutation/lapse transitions remain explicit incompleteness and do not advance completed-audit coverage.
- Publish exact already verified native record references; summary failure cannot erase earlier escalation/evidence. Stable criterion-key escalation identity reuses existing occurrence, including repeated observed lapses; CHEAP lapse does not create escalation. No invented decision where the evidence only needs a pending lifecycle owner. Precise current classification comes from existing typed criterion model/record, not prose heuristics.

Concrete unresolved implementation dependency: generic RunRecorder/LinearRecordSink/NotionRecordSink currently use identity/presence verification but have no outbound gate or canonical fresh WriteBackVerifier path. Existing WritableSurface has no tracker-document/knowledge-row address. Simply calling records.audit would create an uncovered writer under531/538. Proposed temporary supported configuration: marker-based audit evidence/summary publishing is fully canonical; generic structural reporter remains wired and supports legal named absence, while declared audit record sinks without a canonical verification capability typed-refuse at preflight naming records.audit. Alternatively root assigns the minimal generic record verification capability separately. This is an implementation gap, not a founder fork; do not fabricate record-surface addresses or claim holds_record is semantic verification.

Exact local ownership: new composition/audit.py, services/audit_runtime.py, services/audit_publication.py, types/domain/audit_runtime.py and focused tests; existing services/audit_coverage.py, services/audit_requests.py and chains/audit_sweep.py narrow extraction/current-completion seams. No graph/native/amendment/state-authority files. Shared hunks pending: config.py import/field, operation.py binding/RunKind/validation/census, core/prompt_namespaces.py actual native consumer roster, composition/passes.py registration/preflight, main.py collaborator forwarding, .env.example optional setting comments and operation example/config docs, narrow existing errors if no suitable typed error. No pyproject/uv.lock changes proposed. Exact generic record capability requires separate design/ownership approval.

Test plan: actual runtime constructor named absent event first (before red), then real lifespan one audit registration at nondefault interval/timeout and one driven scheduler tick. Real production sweep/collection/verifiers with only external boundaries doubled; first/restart full, state-stamp delta, tied stamps, periodic full, independent scopes, failed/cancelled final publication with no mark, replay no duplicates. Complete report source/head/native identity and required mandate checks; raw incomplete observations never appear as complete emitted claims. Assert gate invocations at each actual outward seam, canonical fresh judge(session_id=None), actual citation/bound retention, escalation-before-summary even summary fails, report refs resolve at publication, no ordinary-unverifiable transitions and no held806 state writer. Preserve original read/forge/cancellation/foreign-head tests. Strict type/census/output-description/locked controls and independent review required.

Framework evidence: exact FastAPI0.135.1 lifespan documentation https://raw.githubusercontent.com/fastapi/fastapi/0.135.1/docs/en/docs/advanced/events.md ; Pydantic2.12.5 models https://raw.githubusercontent.com/pydantic/pydantic/v2.12.5/docs/concepts/models.md . Existing scheduler is plain asyncio and this proposal does not modify LangGraph1.0.10 graph semantics or checkpointing; do not assert unrelated framework coverage. The original FastAPI lifespan/shutdown ownership remains the production entry.

Integration verdict: proposal only; no candidate source to integrate yet. Independent review required after implementation. Owning issue https://linear.app/duckburg/issue/KOD-79 ; public evidence comment pending executable counterexample.
