# L8 read-only harvest proposal

Baseline: exact 249dde2fb9dfa917743582dd861971a7dc9f2968 via immutable git objects in the integration repository. No L8 worktree, source edits, tests, integration, PR, issue-state or initiative writes. Requested Astra ultra; effective runtime identity unverified. This research was interrupted for L5 corrections and independent L3/gate reviews; the baseline was not silently advanced.

Live PR116 rechecked at the end of this read: OPEN, head 9899fe6f23ead2194fd709fdac6a4fe5b1b69b7e, https://github.com/YalDan/kodezart/pull/116. Four commits: 1e369933 alarm persistence; d897e29d event stream; 2ff16d39 subtree reach; 9899fe6f scope-at-rest tests. Do not cherry-pick the whole PR.

## Useful accepted delta

KOD-493's accepted/provisional P1 behavior and KOD-775's Option 1 address ruling are implemented by 1e369933: an alarm is addressed by the complete canonical (subject, signal), not a lane key; identical repeat writes nothing; reading returns equal six-field RunAlarm with reading order; two surfaces under one signal stay distinct; scope subject needs no lane key; missing is None, damaged/duplicate records refuse. The useful donor is that behavior/codec/adapter conformance delta, not its old signatures or current completion claims.

At final PR head, domain/run_alarm_record.py is 65 lines and shares a 37-line domain/fenced_record.py helper introduced in its event work. It uses required configured run_alarm marker purpose, full-subject canonical key then signal, strict typed body read and canonical serialization comparison. A body with another subject/signal is refused, not repaired. Candidate extraction should preserve duplicate-key/framing/address/refusal and exact repeat tests.

Required adaptation: PR116 record_run_alarm calls upsert_comment without holder. Baseline249dde2 universally refuses missing holders. New record_run_alarm must accept/pass the real queue holder, and the actual publisher must use existing RunSurfaceLease over the full addressed alarm set with config.tracker.surface_lease_seconds, explicit renewal after awaited gating and before write, no reacquisition after loss, and settled release on every exit. Do not fork a lease implementation or exempt alarm upserts. Error translation must use the current neutral tracker boundary; the donor's old TrackerProtocolError/tool details are not copied into services.

The event delta should not be harvested: current domain/run_event_stream.py already owns LaneRunEvent plus post/read codec, and TrackerPort already has post_run_event and lane_run_events. Donor final head uses a separate RunEventRecord and run_events spelling; copying would create a second event model/reader. The scope reach commits are separately inherited/L3-owned, not alarm persistence.

## Current source gaps

- No record_run_alarm/read_run_alarm port methods, alarm codec, run_alarm configured prefix, or composed supervisor exists at249dde2. Existing services return observations; composition references none of them.
- Twelve AlarmSignal members exist. Nine have implementation somewhere: seven in domain/run_shape.py (record superseded, missing write-back, commit count, escalation age, surface contention, barren growth, scope tally) and two in domain/mandate_graph.py (ruling growth, structural milestone). TALLY_REGRESSED, LAPSE_UNDISCHARGED and COMPOSITION_SUBSTITUTED have no domain/service implementation. TALLY_UNMOVED has only the scope marker arm; lane accept arm is explicitly refused, and scope execution-entry rung lacks its lane-dispatched reader.
- No complete surface write-provenance reader/producer exists. S6 accepts supplied surface/history readings; it cannot manufacture cross-run holder history from updatedAt. KOD-494 requires every WritableSurface kind, author identity plus edit history, and timestamp-only changes must not create holders.
- AlarmSubject is still kind plus optional lane/issue/member fields, with WritableSurface serialized inside member_id and parsed again. AlarmReading.value is still JSON text; domain _decode reparses typed values. Sep12 KOD-103 comments 7c60c539/7b6966f1 explicitly request closed typed subject variants and evidence payload union. Old passing model tests do not override those current review requests.
- Six configured bounds already exist in AppConfig. No new flag is indicated. TrackerSettings.surface_lease_seconds already supplies the active duration policy.

## Production composition points and unsatisfied metadata

At249dde2, services/ScopeDispatcher.run_pass owns the existing scope readiness call and record_run_outcome delegates exits to FireDispatcher; however build_dispatch_passes still schedules the plain producer. Wiring only that dormant class would not prove actual runtime. New reviewed/pending L3 source at db76a86 provides the concrete request seam: ScopeWorkflowEngine.run loop rereads readiness once per tick and consumes actual final NativeDeliveryState after each lane. Its real queue job is cache_key; the per-lane hash checkpoint key is not the alarm lease holder. composition/scope_runtime.py/build_scope_runtime and composition/engine.py are the actual injection points after L3 integration. A publisher belongs beside that controller's tick and lane-exit calls, not inside individual signal predicates or a duplicated fire graph.

Those seams do not yet supply all supervisor premises. A tick before any lane has run may have no attributable SHA; use a recorded tracker observation or explicit typed absence/refusal, never a repository read or fabricated SHA. Full scope subjects have no lane_key and no inherent issue comment target: PR116's port takes an explicit issue_key separately, so production needs an actual owning-issue/carrier binding. Current LaneRunEvent requires lane_key; using a scope key as a made-up lane to post raised/cleared events would invent identity. Current RunAlarm has exactly six fields and no active/cleared state; editing a clear record while preserving readings and exactly-once transition events requires an agreed existing event/carrier representation. Do not silently add a seventh alarm field or infer transition state from comment timestamps.

KOD-532's marker-only lease and KOD-528's appended transition events also require a complete declared write-set interpretation at the real event owner. Posting through the unfenced post_comment path is not a justified escape from the universal alarm-marker lease. Current event table already has raised/cleared and node-session-started NO_TRANSITION rows, but no sole applying consumer exists. KOD-806's test-backed ec32425c report is explicitly a proposal, not a retained/moved/retired ruling; it continues to reserve KOD-795/796/797/798/401/403 event machinery. No new shared event applier/contract is authorized by this research.

KOD-762 settles criterion-state lapse/regression semantics, but its historical no-live-claim phrase predates KOD-747's replacement of lane claims with marks. Do not revive claim clocks to implement S3; actual mark carrier remains a dependency. KOD-498 demands totality at boot and forbids disabled/degraded arms. A nine-of-twelve publisher cannot silently masquerade as the supervisor.

## Smallest independent implementation proposal

First serialize the type repair and P1 persistence carrier as one bounded review unit, explicitly short of composed supervisor completion. Proposed exclusive source: types/domain/run_alarm.py; the eight existing source consumers that actually construct/decode AlarmReading/AlarmSubject (domain/run_shape.py, domain/mandate_graph.py, services/run_shape.py, services/scope_tally.py, services/lane_record_signals.py, services/barren_record_signals.py, services/escalation_signals.py, services/mandate_graph.py); new domain/run_alarm_record.py. A small shared framing helper may be justified only if current owned codec consumers actually share it; do not migrate the event model merely to reuse donor code.

Proposed coordinated narrow shared hunks: TrackerPort record/read alarm methods; LinearMcpTracker corresponding methods; FakeTrackerPort corresponding methods; configured marker purpose in operation example/fixture only once actual writer composition is chosen. Do not edit other port methods, exception catches, L2 criterion creation or L3 scope runtime concurrently. RunSurfaceLease is reused unchanged. Exact start remains249dde2 unless root supplies a reviewed later integration base.

This P1/type slice is independently testable at the real adapter over external MCP doubles and addresses accepted KOD-493, but it remains a port/carrier repair rather than runtime L8. If the next assignment requires a production supervisor witness, first resolve the explicit event/transition/carrier premises above and compose a total supported service; an empty or partially disabled helper would not satisfy it. The research does not choose those authority forks.

Tests to retain/migrate: all current run-alarm/record-consistency/record-superseded/surface-contention/barren/escalation/scope-tally/mandate domain and tracker suites; new P1 cases over actual Linear adapter plus native fake with explicit real holder. Required adversaries: all six typed subject arms and each malformed region; typed evidence JSON round-trip without domain JSON-in-JSON; two markers/surfaces and scope-without-lane; identical zero-write replay; cold-client native roundtrip; damaged/duplicate/foreign address; missing/foreign/expired holder; concurrent disjoint lane-record/alarm writes; loss during awaited gate and cancellation release. Full supervisor future tests must exercise both actual tick/exit composition paths, all twelve firing/clean/replay pairs, transition-only counts, no tracker-state movement, no agent or git calls, boot totality and current configured duration. No L8 test execution is claimed in this research.

## Requirements read and eight lenses

KOD103 current body and all comments retrieved; current Sep12 architecture/type findings and authoritative prior decisions read. All25 criterion bodies read. Critical later comments for493/497/498/528/532, all775/762 and current806 report read. Historical unverified author narratives are evidence, not new authority. Links: https://linear.app/duckburg/issue/KOD-103, https://linear.app/duckburg/issue/KOD-493, https://linear.app/duckburg/issue/KOD-775, https://linear.app/duckburg/issue/KOD-806.

SOLID: pure predicates, evidence collection and leased publication are distinct actual responsibilities. DRY: reuse native event and lease implementations; no whole PR harvest. Hexagonal: native adapter wire validation stays outside domain/service; holder/carrier addresses explicit. KISS: smallest P1/type delta is measurable; no pretend whole supervisor. Typed agent calls instead of semantic heuristics: no new agent; typed source observations, no prose/timestamp inference. Official framework practices: existing Pydantic discriminated values/strict serialization and actual adapter tests, no speculative framework rewrite. Type safety: current subject/evidence erasure is a confirmed repair target; proposed closed union improves it. Repository hygiene: zero L8 source changes so far, exact baseline/head pinned, exclusive hunks proposed before shared widening. No lane or criterion completion claim.
