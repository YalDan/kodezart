# M2 common application authorization handoff

Frozen source `38956bb04f70b5cbd5d6ffbc5dbc9b08a29e369f`, tree `38e10468599b6c041206d1dfe1f88b2d8312d120`, parent9e387b4; isolated `/private/tmp/kodezart-v03-m2-description-authority`, clean. Seven changed files, source + two original regression modules + fake protocol fidelity. Root author; independent review pending. No maintained branch or test-running tree edited for this source.

Actual Organize `authorize` owns fresh revision, graph context, phase permission and approval checks. Every writing arm now supplies that existing check through `WriteRevalidation` into the adapter's existing complete retry attempt; each native known-unsent retry rechecks it. Body writes also carry the existing `DescriptionWriteAuthority` surface/job token. Native identity, captured source, lease and transactional checks stay in the adapter; issued receipt readback is still outside retry. The optional callback has a legitimate absent case for existing grant-only direct port callers; actual Organize never omits it. No model judgment owns concurrency or authorization. No new policy, ledger, framework, settings, or domain/vendor dependency.

Before/after evidence and per-file hashes: `m2-write-revalidation-proof.json`. Corrected original body probe3red2green at9e; first lease-only binding leaves approval1red43green. Common cause reproduced on graph/split/criteria3red. Final actual-owner/original receipt/lease corpus31green plus broader67green and strict216. Tests preserve original assertions between valid baseline and final repair. Initial uncredited diagnostic accidentally selected live keyword for one control and used generic rather than unanswered transport class for another; preserved separately, corrected before credited baseline. Baseline root review tree is9e with untracked original probe; preserve it.

Type safety neutral: precise async callback extension; runtime authority strengthens. SOLID application policy + adapter enforcement retained; DRY single existing authorization method reused at the retry owner; hexagonal typed callable has no vendor dependency; KISS existing mechanism, no new subsystem; structured author/verify outputs unchanged; framework cancellation/receipt boundaries preserved and original tests rerun; hygiene types remain surface-domain, orchestration in owner and transport in adapter. These are author lenses, not independent acceptance.

Limits: observations cannot fence a backend mutation after its last read or a write already issued. No human approval grant, event806 ruling or native GROOM rubric closure. Next: independent all-eight correctness/type/oracle review, then root integrates into existing PR121 and donor with precise source-delta map; rerun affected/full actual revision. M3 extracts separately and must inherit the new shared signatures at integration. Public source contains no local checkpoint paths or tracker keys from this report.

Owning evidence: https://linear.app/duckburg/issue/KOD-74#comment-37f53f71-46ff-499c-aac1-1961e2e6311e
