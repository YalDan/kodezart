# Independent native canonical gate repair review

Verdict: ACCEPT the bounded two-file correction at 730051660180278c3555a68b67513ec0eaaa363b. No blocking source, race, type or test-oracle finding remains in this slice. This does not accept whole Native/L9, checkpoint restart, KOD814 artifact persistence or KOD96 record adoption.

## Source and requirements

Exact parent: 1f4296c8c36a7498625d2478f8ec7ae6ec187923. Exact tree: 87dec9aa7fb89a4cb3aa9e4839359bd66fb0e415. Independent detached tree: /private/tmp/kodezart-v03-native-gate-independent. Source and tests remained pristine throughout all runs; no new source or test changes were made. Parent reproduction used separate detached native-gate-parent-independent. Requested inherited Astra ultra; effective model metadata is unverified.

Current KOD97 body and all 34 returned comments were read (no next page). The native criterion contract uses tracker sub-issues and actual native keys, with independent amendment judgment and tracker correction before landing; it does not authorize automatic replay of side-effect nodes. Current integration comment203dd299 and checkpoint evidence d5a96897 explicitly retain the separate restart gap. The existing criterion-identity census and graph retry-floor tests are executable repository contracts.

Changed files: services/native_amendments.py (2 added/15 removed), tests/chains/test_retry_floor_wiring.py (29 added). No public port/type/schema changes.

## Source and oracle judgment

_WriterActions.observe_criterion still reads the actual addressed issue, compares the expected body and native source, requires actual UNSTARTED after reset, and updates only the observed matching criterion snapshot. It now delegates obligation construction to the already-injected FireCriteriaReader.read_current(spec=guard._spec), as production TrackerCriteria does elsewhere. The frozen TrackerSpec is retained; duplicate human text does not mint or locate identity. The subsequent require_current remains, comparing current complete native criterion facts, current obligations, ruling records, every registered archive, Git base and HEAD. The new awaited reader does not remove the later drift guard. Operational reader refusal propagates; it does not create an empty or cached successful obligation result.

The retry census still derives actual modules from every production source file with add_node and asserts exact equality to declared modules. Existing retrying graphs retain their same owner/floor/policy checks. The amendment graph is explicitly classified as nonretrying, with assertions over every registered node and a mutation control adding a retry policy independently to each. Eight existing helper/test ASTs are identical to parent. The only changed existing test adds the nonretrying modules to the still-exact census; two functions implement and exercise the absence requirement. This is not an unchecked allowlist exemption.

An additional executable inspection instantiated the real compiled NativeAmendmentGraph. Under installed/locked LangGraph1.0.10, __start__, judge, canonical_write_back and complete each have retry_policy=None; graph.retry_policy is empty. No action implementation was invoked in this constructor inspection. Runtime effect behavior is separately exercised by the actual amendment corpus below. This establishes the current graph's policy, not durable resume semantics.

## Independent execution

Commands used /Users/kodezart/.local/bin/uv run from the exact detached trees. Logs below are in /private/tmp/kodezart-recovery-session.

1. Exact parent original two tests: pytest -q tests/chains/test_retry_floor_wiring.py::test_every_production_graph_with_registered_nodes_is_checked tests/domain/test_criterion_identity.py::test_identity_construction_is_authored_mint_or_exact_tracker_key. Result: 2 failed in4.60s. Log native-gate-original-independent-before-1f4296c.log. The actual failures are the missing native_amendment graph in the exact census and the extra services/native_amendments.py CriterionId construction.
2. The same unchanged two at730: 2 passed in2.67s. Log native-gate-original-independent-7300516.log.
3. pytest -q tests/chains/test_retry_floor_wiring.py tests/domain/test_criterion_identity.py tests/tracker/test_criterion_identity_conformance.py tests/tracker/test_native_criterion_amendment.py tests/services/test_applied_native_amendments.py tests/services/test_native_amendment_archive_independent.py tests/services/test_native_amendment_archive_publication.py. Result:109 passed in387.98s. Log native-gate-affected-independent-7300516.log. This count includes the original two; do not add them as distinct controls.
4. mypy src/kodezart/services/native_amendments.py: strict source clean (1source). Log native-gate-independent-mypy-7300516.log.
5. ruff check and ruff format --check src/kodezart/services/native_amendments.py tests/chains/test_retry_floor_wiring.py: both clean;2 files formatted. Logs native-gate-independent-ruff-7300516.log and native-gate-independent-format-7300516.log.

The runtime selection includes actual AgentService/GitChangePersister/native amendment graph/canonical WriteBackVerifier, real Git commits/worktrees, controlled external tracker/agent data, native criterion reset/body/holder and known-unsent retry controls, authority drift/outage at awaited author/judge boundaries, exact archive loss and commit/publication boundaries. Original assertions were unchanged. No full repository gate was run or claimed in this review.

Evidence artifacts: native-gate-oracle-ast-proof.json; native-gate-compiled-policy-proof.json; native-gate-independent-freeze-7300516.json; parent source/test snapshots native-gate-amendments-parent.py and native-gate-retry-floor-parent.py.

## Eight lenses

- SOLID: criterion obligation construction stays with its existing reader; mutation authority remains with the existing guard.
- DRY: removes the second CriterionId/TrackerCriterionSet reconstruction.
- Hexagonal architecture: consumes the existing injected FireCriteriaReader; actual production components are tested with external boundary controls.
- KISS: one reader call replaces a comprehension; explicit nonretrying census has no new replay mechanism.
- Typed agent calls instead of semantic heuristics: exact native keys and typed TrackerCriterionSet remain; no text-based identity or judgment parsing added.
- Official framework practices (version-matched): actual LangGraph1.0.10 constructor shows no effect retry policies; no framework API changes or restart claim.
- Type safety: improvement in construction ownership and neutral public shape; strict typing passes, with no Any, casts or optional-state widening.
- Repository hygiene: two-file bounded diff, immutable isolated source, preserved original negative/positive oracles and exact logs; no source edits, PR or state changes by reviewer.

## Limits and integration

Fresh sequential source observations are not backend fencing and cannot prevent principal edits inside a backend operation; already completed tracker effects are not rolled back. Existing parent/inner checkpoint-resume gap and full artifact/run-record work remain explicit. The correction neither creates retries nor proves exactly-once effects.

Root may apply only730051660180278c3555a68b67513ec0eaaa363b on the current canonical containing the1f4296c native/archive chain, preserving later unrelated work. Re-run the two original gates and affected integrated checks on the resulting SHA. Root retains all integration/publication authority. This review accepts the tiny native reader/census correction only.

Own KOD97 evidence: https://linear.app/duckburg/issue/KOD-97#comment-93c870d5-a03b-4652-a14d-339e8fd1e79f
