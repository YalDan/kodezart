# Remaining M1 scope input / label / bootstrap inventory at 2bc2375

Read-only comparison of actual M1 `2bc237577da7a3db3101e67c324eea0eaa6e6abd` against accepted donor `36083f83f42c03240ebb5861fe284da2c9f04180`, refining the current-head PR72 audit. No source was changed. This is a finite ownership ledger for the existing milestone-completion workstream, not a new implementation plan. Root priority remains publishing M2; no further extraction starts before its provisional shared contract freezes.

## Current overlap: preserve once

M2 working tree was observed read-only with uncommitted changes; this is **provisional**, not accepted destination evidence. It already carries `ScopeLabel`, `OperationConfig.scope_labels`, its completeness validation, `FIELD_OWNERSHIP["scope_labels"]`, `_scope_label_members`, `_read_scope_issue`, `read_scope_labels`, `execution_approved`, `_read_execution_approval`, `project_milestones`, and `domain/scope_approval.py`. M1 at the pinned SHA lacks this model/port binding. M2 also carries `tests/tracker/test_scope_approval.py`, `test_scope_tool_arguments.py`, `test_milestone_approval.py` and `test_native_approval_aliases.py` provisionally.

Proposed single ownership: generic scope admission vocabulary/configuration, native label/approval/membership reader and bootstrap definition semantics belong to M1; M2 owns Organize's use of those answers, mandate resolution, gate/terminal marker selection and criterion writes. Freeze M2, compare its exact shared units to donor, then root serializes one maintained prerequisite. Do not copy donor whole `operation.py`, tracker adapter, or composition tracker on top of M2's working files; do not create a second scope-label or approval authority.

Concrete provisional difference needing explicit review: M2's adapter constructor currently accepts `scope_labels: Mapping[str, str] | None = None` and stores `dict(scope_labels or {})`; donor requires `scope_labels: Mapping[str, str]`. This is recorded as an observed extraction difference, not an approved compatibility default. No change was made here.

## Exact remaining owning units

| Area | Donor units / current M1 absence | Single owner and active overlap |
| --- | --- | --- |
| Scope vocabulary | `types/domain/operation.py::ScopeLabel` (`TRIAGE`, `PROPOSED`, `APPROVED`); `OperationConfig.scope_labels`; only the scope-label completeness block in `_check_structure` (donor lines 697–704); `FIELD_OWNERSHIP["scope_labels"]` at donor line 1066 | M1 shared; provisionally present in M2. Preserve once. Do not extract `organize_mandates`, `organize_scopes`, `resolve_organize_mandates` as M1. |
| Mapping vocabulary | `types/domain/tracker.py::MappingKind.SCOPE_LABEL` and inclusion in `INSTATABLE_MAPPING_KINDS` | M1. Actual M1 enum currently lacks `SCOPE_LABEL`; ensure scope-label bootstrap must not enter before this contract. |
| Bootstrap refs | `services/tracker_boot.py::configured_mappings` scope-label extension; `_scope_label_refs` whole 16-line helper; `OWNED_REF_BUILDERS["scope_labels"]` | M1. Entire remaining file diff is 21 added lines, inspectable independently of Organize runtime. Definitions are instated at workspace scope; no entity approval is applied. |
| Native definitions | `adapters/linear_mcp_tracker.py` scope label creator/listing constants, `ensure_values` and mapping resolution arms, `_scope_label_definitions`, `_ensure_scope_label`; constructor stores configured mapping | M1; coordinate constructor with M2 provisional reader. Preserve issue/project/initiative namespaces, workspace/team conflicts, native re-listing after creation, and refusal of a scope-qualified scope-label definition. These are mixed-file hunks, not permission to copy the 4,672-line overall adapter delta. |
| Live label/approval reads | Adapter `_scope_label_members`, `_read_scope_issue`, `read_scope_labels`, `execution_approved`, `_read_execution_approval`; corresponding TrackerPort signatures; `domain/scope_approval.py::resolve_execution_approval` | M1 primitive/admission; already provisional M2 prerequisite. Ancestor identity, original issue's project membership and milestone inheritance must remain real native reads. M2's policy consumers stay M2. |
| Project milestone read | `LinearScopeReader.project_milestones` donor 45-line addition and adapter delegation / corresponding protocol | Generic port/read M1; M2 currently needs it. Existing scope reader metadata/approval-parent and typed wire models are already in M1; do not recopy them as completed new work. |
| Tracker construction | Only `composition/tracker.py::build_tracker` → `scope_labels=operation.scope_labels` | M1 shared wiring. Donor `criteria_stage_label_key` from resolved Organize mandates belongs to M2. Do not absorb run-event-table enforcement, writer attribution or unrelated header API changes merely because they share the same function/file. |
| Prompt binding | `core/prompt_namespaces.py::operation_bindings` `_bind_absentable(..., "scope_labels", dict(config.scope_labels), absent=...)` and corresponding namespace documentation | M1 shared config namespace; M2 owns actual Organize/grooming/fire-prep prompt meaning. Shared namespace must be included only once with M2's provisional bindings. |
| Resolution leaf | `services/scope_resolution.py::resolve_scope`, full 15-line file | M1; imports current `TrackerPort.scope_issues` and already-maintained `ResolvedScope`. Byte-identical to PR72 head and donor. `services/scope_membership.py` topology/criterion traversal is a separate native plan/walk/Organize consumer boundary, not this leaf. |
| HTTP scope value | `types/requests/agent.py::ScopeRefRequest`, `WorkflowRequest.scope`, ScopeKind import; `handlers/agent_handler.py::submit_workflow` ScopeRef conversion and submission scope; `WorkflowSubmission.scope` | M1 scope input boundary but not independently end-to-end complete without M3 propagation below. Existing base-branch validation already exists in M1 and must stay. Native `issue_key` semantics are separate M3 data. |
| Queue/engine propagation | `AsyncioJobQueue._consume` passes `request.scope`; WorkflowEngine protocol and actual compiled workflow constructors must accept/address it; native issue identity/run identity travels alongside it | M3 execution contract. M1 must not quietly accept then discard a scope, or import final native execution under the scope-input label. Old PR72's explicit execution refusal was a staged limit; do not revive it as final runtime. |
| HTTP/runtime exclusions | Donor `_queued_event_payload`/`ScopeLaneEvent`, route dependency rewrite, reverse/encoded JobLinks, dataclass and HTTP settings changes, full main/engine scope runtime composition | Separate existing owning units. None is needed merely to define a ScopeRef request and transport it. Scope plan/walk core M3; actual delivery composition M5. |

## Retained tests and docs to relocate with those exact units

M1 missing donor files: `tests/domain/test_scope_label_config.py`, `tests/domain/test_scope_labels.py`, `tests/services/test_scope_resolution.py`, `tests/prompts/test_scope_label_bindings.py`, `tests/tracker/connected_app_label_contract.py`, `tests/tracker/test_scope_label_mappings.py`, `tests/tracker/test_scope_tool_arguments.py`, and `tests/api/v1/test_scope_request_boundary.py`. These were explicitly surfaced by the PR72 current-head audit. M2 already has some native approval/tool-argument tests; compare its frozen content and retain each once rather than reintroducing an old fake signature.

Approval behavior also has donor `tests/tracker/test_scope_approval.py`, `test_scope_ancestor_identity.py`, `test_milestone_approval.py`, and `test_native_approval_aliases.py`. The static helper/fixture inventory alone is not proof that production boot or both existing HTTP routes perform the intended effect. Request-boundary tests must prove invalid inputs reject before enqueue, recorded base precedence, unscoped behavior, and domain scope reaches execution.

Current `tests/tracker/test_scope_reads.py`, `tests/domain/test_scope_container.py`, `tests/domain/test_scope_ref.py` and `types/domain/scope.py` are already donor-identical and inherited by M4. They are retained evidence, not outstanding transfer. Existing scope/lease/read contracts should not be regressed to make a historical PR patch apply.

Documentation/configuration hunks: `[scope_labels]` in `docs/operation.example.toml`, the scope-admission mapping rows in `docs/cutover_mapping.md`, and the scoped request examples in `docs/api.md`. Bind them to actual extracted API/boot behavior; omit stale text claiming blanket unavailable execution once M3 is wired. M2 owns its active Organize namespace/mapping examples.

## Mechanical evidence

`m1-remaining-scope-extraction-2bc.json` records exact donor blob IDs, selected symbol line spans and SHA256, current symbol presence/equality and full pinned per-file diffs for thirteen mixed source files. Large symbol bodies such as `OperationConfig._check_structure`, `FIELD_OWNERSHIP`, `operation_bindings` and adapter constructors are **inspection context**, not a whole-symbol extraction recommendation. This document narrows their actual scope-label hunks. The JSON includes current existence and test names for the listed regression files. The separate `m1-remaining-scope-mixed-deltas.patch` preserves request/handler/workflow/prompt mixed-file evidence.

No standalone runtime test was run for this read-only inventory; the GitSettings full gate proceeds independently at immutable 2bc. Type impact neutral here. Source ownership and actual extraction/review acceptance remain distinct. Next executable action is root's comparison of M2's frozen shared scope contract, then a single serialized owner extraction; no new source work initiated.
