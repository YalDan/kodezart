# Independent L3 correctness review

## Final documentation correction: accepted f632aee

Independently inspected f632aee96063777628577c38af545b03eec18ee1, applied as local9d379f4 after the recorded c47a7f1 compatibility dependency. The two-file delta fixes the actual Workflow count and adds the separately defined Native Delivery event row with its nested scope-only placement. The census discovers AgentEvent subclasses defined in agent, scope_runtime and native_delivery, using exact __module__ ownership so imports do not inflate the census. Exact documented/model name equality, required-field comparison and section-count assertions remain intact; no exempted models or weakened checks.

Command: uv run pytest -q tests/docs/test_api_event_reference.py tests/types/test_scope_events.py. Independent result: 15 passed in0.06s, scope-independent-final-docs.log. Ruff check/format and git diff --check pass. This closes both measured documentation failures above; the independently measured331 runtime correction remains accepted. The separate shared wire-schema mutation-oracle correction remains L2-owned and is not silently waived here. No production source edits were made by this review.

Eight-lens delta verdict: SOLID, DRY, Hexagonal architecture, KISS, Typed agent calls instead of semantic heuristics and Official framework practices are neutral; Type safety is neutral in this documentation delta (the preceding331 closed union improved it); Repository hygiene improves because documentation and exact discovery cover the real definition modules. Integrate the corrective331 and finalf632 commits with their separately reviewed dependencies; this does not establish scheduled/concurrent/cross-job scope operation or resolve KOD777.

## Corrective review of 331cb45538c635b9da63b5de9c2db03b5d2cf72e

Applied only the corrective commit to the existing detached review tree, preserving the original untracked probes unchanged. Local cherry-pick7b9961c and donor331cb4 have the identical tree4ab74967fd592ba4354ce925bbe5bf4de2fead0d. The original three late-readiness counterexamples, unchanged resume and actual main.lifespan control now all pass: 5 passed in4.23s, scope-independent-correction-original-probes.log.

The added final read_scope_ready occurs after awaited probe/checkpoint/work-ref access and before dispatched membership and graph entry. It reuses the existing equality/readiness predicate. Approval placement, open-delivery exclusion, mark/claim policy and base resolution are unchanged. This corrects the measured stale admission seam; no atomic fencing guarantee is inferred.

ScopeLaneEvent now carries a closed discriminated union of actual native progress variants and LaneDeliveryEvent; the controller validates that union before wrapping. Reviewed the SDK message mapper and native producers against the union. Authored artifact and WorkflowComplete events remain outside the native progress envelope. JSON egress preserves required nullable fields inside ScopeLaneEvent, while the existing authored/top-level exclude_none wire stays unchanged. Full strict mypy at donor-equivalent source passes298 files, scope-independent-correction-types.log. All seven changed Python paths pass Ruff/format/diff checks. Type-safety verdict for this corrective runtime delta: improvement; the prior nested-base-model round-trip regression is repaired.

Nine complete affected modules (scope launch/runtime/observations/ready/dispatcher, scope request, origin, scope-event values and event documentation) returned198 passed /2 failures in29.96s, scope-independent-correction-affected.log. Both remaining failures are real documentation inventory omissions: docs/api.md workflow heading says16 for18 rows, and tests/docs/test_api_event_reference.py scans only agent.py, omitting the new real scope event module. No census exemption or assertion deletion is recommended; author/root notified to correct actual count and discovery.

The complete shared wire-schema module independently reproduces the separately owned missing_schema mutation-oracle failure:68 passed /1 failed in12.31s, scope-independent-correction-schema-gate.log. L2 owns that correction. This review did not mask, xfail or amend it.

After the donor-equivalent runs, applied authorized canonical compatibility dependency c47a7f1 as local6a30727 (four known authored schema/fixture files, no331 overlap). Full API jobs+scope-request modules pass96 in7.32s; agent API module passes20 in4.24s. Logs scope-independent-handler-compatibility-corrected.log and scope-independent-agent-api-compatibility.log. The initial compatibility command named nonexistent tests/handlers/test_agent_handler.py and collected zero; retained scope-independent-handler-compatibility.log. Correct actual entry tests are under tests/api/v1. No broader passing claim is made across the remaining doc/schema failures.

Corrective eight lenses: SOLID/DRY/hexagonal/KISS retain the same readiness, coordinator and graph boundaries; Typed agent calls remain schema/native-identity based; Official framework practices use installed Pydantic discriminators and LangGraph checkpoint/resume; Type safety improves closed input/round-trip invariants; Repository hygiene still requires the two doc-oracle fixes and independent shared schema correction. Current runtime correction is accepted on the measured boundaries, with overall gate acceptance pending those explicit outstanding corrections. Reserved KOD777,785,787 and scheduled/concurrent/cross-job/L6 limitations below remain unchanged.

Reviewed immutable db76a865b1e83ff8017bee3614c136f3e08dcac1 in detached /private/tmp/kodezart-v03-scope-review-delivery. Owned deltas only: feb9898, 8f3ea9e, 8ef48ec, db76a86. L5/native/CI dependency cherries were not treated as new L3 code. Requested Astra ultra; effective runtime identity unverified. No source mutation, integration or issue-state write.

## Current verdict: request changes

The actual request constructor reaches native scope execution. A material late-readiness gap remains: a paused native review resumes after A's approval, scope membership, or blocker relation changes during the second awaited DeliveryProbe.open_delivery_exists call. services/scope_runtime.py refreshes readiness before that await, then reads/resumes its checkpoint using only current criterion authority. None of those reads re-establishes current approval/membership/blocker readiness. The real reviewer opens a fresh evaluation session for A after A is no longer ready. Fix the launch boundary without changing approval placement or the open-delivery exclusion policy.

Executable controls in tests/integration/test_scope_runtime_independent_review.py::test_paused_resume_rechecks_readiness_after_awaited_delivery_probe use the actual production builder, controller, native graph and shared reviewer. Only tracker/probe boundaries change. Final corrected result: three intended failures [approval, membership, blocker], one unchanged-resume positive passes, 6.67s; scope-independent-late-readiness-final.log. The first blocker probe additionally admitted B and exhausted its scripted evaluator; corrected fixture keeps B unapproved while its open subtree still blocks A. A temporary subject-text filter was not a sound reviewer oracle and falsely passed; it was removed. Those diagnostic logs remain scope-independent-counterexamples.log, -corrected.log, -final.log. The final no-evaluation assertion is unfiltered and discriminates the authorized unchanged control.

Root separately reproduced ScopeLaneEvent JSON round-trip failure from SerializeAsAny[AgentEvent]; do not accept that boundary before its discriminated-value correction. This review did not duplicate that finding.

## Other measured boundary, reserved rather than a repair request

test_closed_blocker_subtree_without_pr_uses_trunk_despite_parent_state reproduces BaseResolutionError: readiness admits B from A's closed criterion subtree while A's parent remains Todo and no WorkRef exists; BaseResolver still applies its separate ref-less terminal-parent assumption. Fresh full KOD-777 comments, especially e9bd4569 and root 3fdc0d81, explicitly distinguish subtree discharge from landing evidence. This is a preserved unresolved KOD-777/KOD-217 limitation. The test's requested trunk behavior is not implementation authority; no resolver change is proposed. The failed measurement remains recorded, not suppressed or called an ordinary regression.

## Independent execution

Actual main.lifespan + real build_workflow_engine + actual queue + controller + native graph: test_actual_lifespan_builds_native_scope_with_real_queue passed in 0.74s, scope-independent-lifespan.log. The existing cleanup fixture supplies external/boot infrastructure doubles; the test restores the actual workflow builder/checkpointer and runs the actual queue. Exact tracker Check reaches native execution; one queue record; no authored artifact persistence; no error event. This complements the author's separate real HTTP SSE constructor regression.

Six complete affected modules (scope runtime integration, scope observations, scope ready, scope dispatcher, API jobs, forge origin selection): 196 passed, one failed in 39.93s, scope-independent-selected-corrected.log. The failed API paused-authored fixture is the known c47a7f1 quality-gate fixture correction, absent from this isolated ancestry; not an L3 code regression and not called a green selection. Initial command named nonexistent test_scope_walker.py, collected zero; retained scope-independent-selected.log. Correct module is test_scope_ready.py.

## Requirements and source

Fresh full KOD-75/KOD-105/KOD-815 bodies and comments retrieved, latest operative comments read; KOD-425/428/431/449/450/451/456/803 bodies read, KOD-785/787/777 full later comments read. KOD-747's later concurrent-lane/mark ruling supersedes the historical one-fire rule; the request slice explicitly leaves concurrency and mark carrier incomplete. KOD-785 retains open-delivery exclusion; no suggestion to bypass it for a cached PR. KOD-787 placement/partial-approval fork remains unchanged; observations expose facts without selecting an approval refusal policy. KOD-777 does not authorize deriving landedness from subtree closure.

Source traced: main.lifespan -> build_workflow_engine -> OriginRoutedWorkflowEngine -> build_scope_runtime -> ScopeWorkflowEngine.run -> fire.prepare -> NativeLaneWorkflow.prepare -> exact native graph with config. Queue job ID and lane checkpoint key are separate. Metadata is serialized as scalar JSON; saved original RunIdentity survives replay. Current scope/readiness uses existing read_scope_ready and read_scope_plan, not a second topology. Scope emissions suppress inner WorkflowCompleteEvent; final queue outcome remains None and typed scope observations retain unresolved/unapproved/skipped facts.

## Eight lenses and scope

SOLID: controller selects lanes, native graph owns fire/delivery, adapters retain external reads. DRY: existing readiness/base resolver/native graph reused, no queue-within-queue. Hexagonal architecture: actual collaborators tested through external doubles. KISS: one explicit controller per request; scheduled scope selection and concurrency are still missing rather than hidden behind new modes. Typed agent calls instead of semantic heuristics: actual native subject/Check/schema reach shared nodes; no prose-derived target. Official framework practices (version matched): installed LangGraph checkpoint/resume and Pydantic serialization exercised; nested-event input round-trip needs root's typed correction. Type safety: new frozen scope observations improve runtime facts, but inherited AgentEvent-typed nested decoding is currently a regression; cannot call whole type boundary accepted. Repository hygiene: no production edits by this reviewer; independent probes isolated and diagnostics preserved.

This is request-entry evidence, not full L3 or KOD-449 completion. Scheduled configured scope name resolution, concurrent lanes, mark carrier, cross-job/public restart, durable association/evaluator write-back and L6 terminal evaluation remain incomplete. New L5 current-evidence corrections must be integrated separately. Readiness checks are observations, not atomic tracker fencing; the requested correction addresses the known awaited launch seam.
