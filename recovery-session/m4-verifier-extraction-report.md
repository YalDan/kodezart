# M4 first shared verifier extraction — author evidence

Scope: KOD76 / shared KOD527, KOD531, KOD538 and KOD97 prerequisite only. Requested Astra High, effective runtime metadata unverified. No delegation. Root remains sole integrator/publisher of the maintained milestone PRs. This is author evidence, not independent approval or full criterion completion.

## Source and ownership

Isolated branch `codex/v03-m4-verifier-extraction` at `/private/tmp/kodezart-v03-m4-verifier-extraction`, starting `57dcc8c3ae4836207307a715812fe389c301e130`. Published M1 `a4575cd11133c5e631fff454de8cbac682f5a30e` is tree-identical (`92c99fe09310652f7e5e12f33625c28413dff924`). No reset/rebuild is needed. Root's M1 evidence: 3439 passed,16 skipped,75.62s; this is root evidence, not my execution.

Accepted source donor `7892ca1a47adbcfd91fbcfb1e580dfc86f066a99`; historical watermark `d2c6fceab762191d4e40b23c8cd349ef476e4b12`, main `4661a24b599d75503a997f3ce122f3ad2da77048`. Reused the existing extraction overlay/659-path inventory and current initiative manifest `3ae7f771-d30e-43fc-8357-b2892b666abc`; this bounded manifest does not replace that inventory.

`m4-verifier-provenance.json` records all 27 changed files, exact SHA256 bytes, baseline/donor hashes, whole-file identity and hunk responsibility. Existing verifier, models and both actual prompt templates retain current accepted source. `judge_in_workspace` is the exact historical7042032 helper using the existing EVAL_PERMISSION_MODE/list(EVAL_TOOLS) boundary, explicitly authorized by root; final6994552 ToolPreset consumer migration remains pending. The artifact reader is exact historical7042032 initial three-surface implementation. These are disclosed interim source choices, not fabricated compatibility layers.

Minimal shared closure: AuditVerdict/TrackerArtifact only; WriteBackFinding/Result; WriteBackVerifier/FreshWriteBackJudge; generic judge helper only; owned_workspace; read_workspace_head only; GitService.has_replace_refs and its actual adapter/fake method; WriteBackReadError; exact schema/RaiseSite/PromptKey/registrations and one-forwarder caller census. No FreshAuditSession, Audit runtime/models, Organize admission/authoring, native workflow binding, delivery, supervision or state application was copied.

## Findings and actual evidence

Base absence is proven by `m4-verifier-baseline-absence.log`: the five newly required production modules are absent on57;57/a457 tree hashes agree. This is absence evidence rather than a fictitious failing runtime test.

The production sequence is actual write → native reread → independent structured judge → bounded repair. The concrete step retains lease/content-gate responsibility; the shared verifier does not grant write authority. Actual refutations and cited references drive repairs and remain in ordered results on exhaustion. Unsupported surfaces refuse before any step/write/agent dispatch.

Actual tests exercise AgentService, FreshWriteBackJudge, GitWorktreeProvider and SubprocessGitService at native Git refs; replacement refs before/during judgment in default and configured namespaces; repeated cancellation during acquire/session/release; six in-flight subprocess read phases; fresh session_id=None; exact output schema/ref/artifact; wrong semantic outputs and blank refutations refused. The production Linear adapter plus RunSurfaceLease and canonical loop are exercised above an external MCP double, including real reread/repair, exact issue/container identities, unsupported surfaces, cancellation and programming-error escape. Semantic path-checking doubles derive verdicts from artifact content and ref-specific source facts, rather than scripted round numbers. They do not prove live model judgment quality.

First focused run: **14 failed,818 passed,110.66s**. Twelve were the copied later FakeTrackerPort constructor's marker_prefixes argument, unsupported by the existing M1 fake; removed only that argument and preserved every original behavioral assertion. Two were the existing explicit prompt census18 after exactly one new actual role; updated to19, retained enum/complete-set assertions. No test was relaxed or skipped. Log `m4-verifier-focused-first.log`.

Corrected selection plus new actual adapter controls: **2 failed,79 passed,3.68s**. New adapter fixture had server comment timestamps at its default January epoch while adapter lease time was March; the protected native writer correctly refused that incoherent lease. Aligned only the external server clock to the already declared fixture instant. Log `m4-verifier-corrected-controls.log`; then8 adapter controls passed0.32s (`m4-verifier-adapter-second.log`). Added actual issue and container readback controls: **10 passed0.53s**, `m4-verifier-adapter-final.log`.

Initial strict:185 source files clean (`m4-verifier-mypy-first.log`). **Full make check passed:3518 passed,16 skipped,444.63s**; format368/Ruff/strict185 and literal guard passed (`m4-verifier-make-check.log`). All27 mapped files were hash-verified unchanged throughout that run.

After full gate, only the new external failure oracle was strengthened to match the artifact comment's first-line marker, assert exact raised exception identity and count exactly one failed artifact save. The initial text-edit script accidentally added MARKER to the unrelated description tuple (1 failed/45 passed16.97s, `m4-verifier-final-strengthened.log`); restored its original exact three-field tuple. **Final strengthened verifier/judge/actual Git/actual adapter selection:46 passed15.80s**, `m4-verifier-final-strengthened-corrected.log`. Ruff/format passed again. No production source changed after full make check; the final focused run covers the sole changed test file and affected mechanism. No expanded repeated full run was needed after that test-only correction.

Commands: `/Users/kodezart/.local/bin/uv run --locked pytest -q tests/chains/test_write_back_verifier.py tests/chains/test_fresh_write_back_judge.py tests/chains/test_write_back_workspace_ownership.py tests/types/test_wire_schemas.py tests/prompts`; corrected selection additionally `tests/chains/test_write_back_tracker_boundary.py`; `uv run --locked mypy src`; `uv run --locked ruff check src tests`; `uv run --locked ruff format --check src tests`; `git diff --check`; `PATH=/Users/kodezart/.local/bin:$PATH UV_LOCKED=1 make check`. Full log `m4-verifier-make-check.log`.

Initial extraction script diagnostics and provenance are retained in `extract_m4_verifier.py`, `m4-verifier-initial-provenance.json`, `m4_verifier_tests_and_census.py`, `m4_git_controls.py`, `m4_finish_fixture.py`, and `m4_provenance_final.py`. Initial Ruff E501/import reports were corrected by formatting/import ordering before execution. No suppressions were introduced. Initial worktree discovery incorrectly queried the user's distinct repository object database; object lookup failed and no files there were edited. Worktree creation used the actual tmp common repository afterwards.

## Eight lenses and type impact

- SOLID: the step owns write authorization, the canonical reader owns actual artifact identity, the independent judge owns claim evaluation, and the loop owns bounded sequencing. Existing narrow step/judge protocols retained; the broader preexisting TrackerPort remains unchanged except no new tracker method at all.
- DRY: one existing verifier and one shared judge helper; no second audit loop or reconstruction from write return values. Existing task settlement and port/adapter boundaries reused. Exact schema forwarding census permits only the actual helper and exact caller.
- Hexagonal: application services depend on ports/domain models; MCP representation and native Git invocation remain adapters. Actual production collaborators exercised with only external boundaries doubled.
- KISS: existing source extracted at named symbols and hunks. No new scheduler, settings default, enum alias, compatibility wrapper or speculative phase added. Unsupported surface capabilities refuse explicitly.
- Typed agent calls: actual registered WriteBackFinding schema, independent session, validated output, nonblank evidence/citations; admission/buildability cannot masquerade as landed-claims judgment. Identity/ref/authority remain deterministic guards.
- Official practices: locked Python3.12.13/Pydantic2.12.5/FastAPI0.135.1/LangGraph1.0.10 inspected. This slice changes no FastAPI lifespan or LangGraph state/reducer/retry/checkpoint behavior. Pydantic model_json_schema/model_validate and Python cancellation/finally semantics checked against official versioned source/docs. Pydantic versioned documentation URL was unavailable, so exact v2.12.5 source used: https://raw.githubusercontent.com/pydantic/pydantic/v2.12.5/pydantic/main.py . Async ownership guidance: https://docs.python.org/3.12/library/asyncio-task.html#task-cancellation . Live executor/provider compatibility is tested through the existing production classes.
- Type safety: **improvement** over absent component; frozen validated findings/results with required citation on refutation and final verdict consistency; exact native artifacts; no Any/casts/ignores/model_copy shortcuts introduced. Some cross-field invariants intentionally remain Pydantic validation rather than JSON-Schema-expressible constraints, matching accepted source; strict gate and hostile outputs cover that actual boundary.
- Hygiene: coherent existing files;27-file bounded extraction, source-only hunks exclude later owners; prompt descriptions/current templates retained; original donors/canonical untouched; exact provenance and every interim deviation recorded. No source/test mutation during any running test.

## Remaining dependencies and integration

Full accepted161ca93 label/criterion/child-set reader delta remains **M4 after minimal M1 native classification/read_criteria port/model prerequisites**, assigned to root for serialized extraction. It is not an M2 dependency and is not lost. Graph/split artifact arms and their owner/context belong M2. Final6994552 typed tool boundary is pending shared M1 API migration. Required configured write_back.max_verify_rounds1..10 adoption and actual every-lane consumer wiring remain later M4/consumer hunks; the generic loop still accepts explicit caller max_rounds and supplies no default. LaneEscalation and independent semantic/state/ruling/source contracts are later M4 slices. No full KOD76/527/531/538/97 completion claim.

Root-owned neutral GitOperationError/provider correction is a later shared M4 source-boundary dependency after independent review; no error taxonomy edits were made here. Backend in-flight writes remain unfenced; this extraction does not claim compare-and-set, replay uniqueness or completed supervision/state authority.

Integration: root should review this clean bounded commit independently, then cherry-pick onto maintained M1a457 (base57 tree-equivalent); no new PR or temporary donor publication is requested. Retain all pending rows in the seven-milestone hunk inventory and final recomposition proof. Author evidence cannot substitute for fresh independent review.


## Final freeze

Clean commit **40b250c5c032c2a7ef9764c16f85bc7cf8d086ea**,27 files1730 insertions7 deletions. Provenance JSON includes exact frozen tree/commit and all27 final file hashes, verified after commit. Additional root-requested read-only closure proposal: `m4-next-m1-prerequisites.md` and `m4-modern-session-callers.txt`; no source/test changes for that inventory. Official locked implementation inspection retained in `m4-framework-version-source.log`.

Root may cherry-pick this one commit onto maintaineda457 after fresh independent review; shared tree equivalence is proven. Full gate plus final strengthened affected selection establish author execution evidence, not acceptance of later pending surfaces or lane adoption.

Own bounded KOD76 author evidence: https://linear.app/duckburg/issue/KOD-76/v03-l4-tracker-as-live-run-state-evaluator-write-back-validation-from#comment-bdc2b8c0-dfaa-4b54-8d56-753de64f8b33 . No initiative/Notion/status/PR changes.
