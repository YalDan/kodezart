# M1 scope-label, approval read and bootstrap extraction — frozen author handoff

Candidate `ddb8cdef33f90f30aecc4343e36c2293bdf39a27`, branch `codex/v03-m1-scope-bindings-extraction`, isolated worktree `/private/tmp/kodezart-v03-m1-scope-bindings-extraction`. Exact parent maintained M1 `2bc237577da7a3db3101e67c324eea0eaa6e6abd`. Accepted donor `36083f83f42c03240ebb5861fe284da2c9f04180`; frozen M2 comparison `73cc5def12a2daa4e0ba319660db76cdad781882`. No matching tree existed before creation. One committed 37-file slice, clean worktree. No maintained ref, other worker tree, PR, Notion or initiative changed by this author. Root owns fresh independent review and serialized integration.

## Completed source and actual consumer path

`ScopeLabel` and `OperationConfig.scope_labels` retain the donor three semantic members, open configured mapping, absent/empty behavior, complete-key validation and OWNED classification. `operation_bindings` exposes both actual mapping and explicit absent arm; tests prove custom and additional names render and an unavailable member refuses with its exact name. Existing configuration field/type and binding census stays exact equality. Current pass templates do not use this field yet: the mapping table names its actual native `composition/tracker.py::build_tracker` consumer, using the existing native-consumer mechanism. Organize template policy remains M2.

The actual tracker factory passes `operation.scope_labels` into a REQUIRED `Mapping[str, str]` constructor; no production optional default or fallback was added. Every current direct adapter constructor in tests was migrated to explicit mapping. Fixtures for unrelated behavior explicitly declare no configured scope labels; approval fixtures supply custom labels. The constructor is byte-identical to base after removing just the new mapping argument and dict-storage line.

The native adapter reads configured label membership and current execution approval through typed reported issue identity, parent ancestry and the originally addressed issue's project/initiative ancestry. Milestones do not add an approval label level. Missing configuration, missing ancestry, changed identity, unreadable wire data, cancellation and current reparent/revoke controls remain typed failures rather than an empty or guessed answer. `LinearApprovalIssueWire` now inherits existing `LinearAddressedIssueWire`, preserving reported UUID alias validation. No read-fire-spec or execution admission pipeline was imported.

`MappingKind.SCOPE_LABEL`, its instatable classification, `configured_mappings`, workspace-only `_scope_label_refs` and `OWNED_REF_BUILDERS` complete native bootstrap. A configured label is independently defined in issue, project and initiative namespaces. Existing names are adopted unchanged; only missing namespace definitions are created; each creation is freshly re-listed before success. A team-scoped issue copy conflicts with workspace scope and refuses before any namespace write. Bootstrap never applies approval labels to entities. The exact donor `LinearLabelListWire` continuation fields and paginated `_label_entries` helper are necessary bounded dependencies, preserving complete label enumeration and refusal of a missing/repeated cursor. The older service tool capture remains unchanged and separately attributed connected-app declarations extend the public contract; service-credential availability of the project creator remains unverified.

`project_milestones` is the exact pure read port/delegator/native-reader addition with stable membership and detail identity checks. Its two donor oracle functions were split out of mixed `test_organize_graph_writes.py` into `test_project_milestones.py`; they do not import graph writes. The 15-line donor `services/scope_resolution.py` merely returns existing `ResolvedScope` from `TrackerPort.scope_issues`, preserving tracker structural edges without prose parsing or queue propagation.

## Exact provenance and no incidental source changes

`verify_m1_scope_extraction.py` executed on Python 3.12 writes `m1-scope-bindings-extraction-map.json` and `m1-scope-equivalence.log`. The map contains all 37 per-file candidate/donor blobs, exact base-to-candidate hunks, modified/added symbol source spans and donor/M2 equality. Binary patch `m1-scope-bindings-extraction.patch` SHA256 `00f669001892d5b900d6c48e1c6564c4fc8c928fe1577c91688fae8d476844b9`.

Five source files are donor byte-identical: `domain/scope_approval.py`, `services/scope_resolution.py`, `services/tracker_boot.py`, `adapters/linear_scope_reader.py`, `adapters/linear_scope_types.py`. Nine adapter helpers are exact donor AST: `_scope_label_members`, `_read_scope_issue`, `read_scope_labels`, `execution_approved`, `_read_execution_approval`, `project_milestones`, `_scope_label_definitions`, `_ensure_scope_label`, `_label_entries`. Only five existing adapter methods changed: `__init__`, `resolve_mappings`, `ensure_mappings`, `_label_entries`, `_identifiers_of`; every change is the stated mapping/bootstrap dependency. No source Any, cast, ignore or noqa added.

Exact M2 AST overlap at 73cc: six adapter scope/approval/project-milestone helpers, the project-milestone reader, three port methods, generic approval resolver, ScopeLabel values and OperationConfig.scope_labels. Current tracker factory is itself exact M2 AST. Preserve these once by actual M1 ancestry; mixed-file positions and M2-specific surrounding declarations differ and must not be wholesale overwritten. M2's mandates/scopes, issue-identity/author/graph/split/criterion writes and native workflow composition remain excluded.

Test provenance: ten changed test files are donor byte-identical (nine new files plus the existing tool-roster file). Native alias test slicing is described below; exact donor tool-roster attribution and connected-app declarations retained. Fake adapter changes are limited to corresponding protocol methods, label namespace/page fixtures, mapping semantics and existing donor `issue_writes` observability. The latter is incremented by actual fake update_issue and keeps no-write approval assertions meaningful; no empty decorative counter was substituted. Existing constructor fixtures migrated explicitly. No test skip, xfail, marker exemption or assertion containment was introduced.

`test_native_approval_aliases.py` keeps the donor actual approval cases for reported native UUID, canonical human key, missing alias and foreign identity, with explicit local configured approval labels. Donor mixed `read_fire_spec` entry cases remain M3 pending; the M2 snapshot also selected only approval cases. The M1 file removes unreachable spec branches instead of importing execution to satisfy a shared-reader test. `tests/tracker/test_scope_ancestor_identity.py` already matches donor at base and remains unchanged.

## Actual validation

- First focused collection: failed to import donor FIRE_ENTRY_LABELS, exposing the mixed native-spec test dependency. Log `m1-scope-focused-initial.log`; corrected by the explicit approval-only extraction above.
- Focused behavior: 125 passed, 14 failed because current FakeTrackerPort lacked donor issue-write observability, not because approval answers disagreed. Log `m1-scope-focused-corrected.log`; repaired with the exact donor write counter declaration and update append.
- First broader tracker/config/current-consumer selection: 767 passed, 8 failed in 14.73s. All eight were newly reached connected-tool/config/mapping censuses. Log `m1-scope-affected.log`. Migrated complete census data and separately attributed declarations; exact coverage assertions preserved.
- Final `uv run pytest -q tests/tracker tests/domain/test_scope_label_config.py tests/domain/test_scope_labels.py tests/services/test_scope_resolution.py tests/prompts/test_scope_label_bindings.py tests/prompts/test_operation_config.py tests/adapters/test_tracker_self_writes.py`: **775 passed in 12.45s**, log `m1-scope-affected-final.log`.
- Final `uv run mypy src`: **181 source files clean** (`m1-scope-mypy-final.log`). `uv run ruff check src tests`: clean (`m1-scope-ruff-final.log`). `uv run ruff format --check src tests`: **386 files formatted** (`m1-scope-format-final.log`). `git diff --check`: clean.
- Full `PATH=/Users/kodezart/.local/bin:$PATH make check` on immutable ddb8cde: **3866 passed, 16 skipped in 432.31s (7:12), exit 0**, log `m1-scope-full-gate.log`. Formatting, lint and strict mypy passed first. All three existing MCP reopen logging tests passed on this run. Existing pre-extraction MCP reopen logging failures on 241 and 2bc remain separately recorded in GitSettings review; no MCP source/test or timeout change is included and their cause is not claimed resolved. No code changed during the full gate.

## Eight bounded author lenses

- SOLID: configuration vocabulary and pure approval arithmetic have single responsibilities; composition supplies the mapping and the native adapter owns I/O.
- DRY: one generic approval resolver and one configured mapping feed both existing port/factory and future consumers. M2's exact provisional copies should disappear from its final diff through ancestry.
- Hexagonal: pure domain logic takes typed callables, shared resolution uses TrackerPort, vendor IDs/listings/creator tools stay in the adapter. No native execution policy crosses into shared configuration.
- KISS: donor enum/mapping/read helpers and bootstrap cases; no compatibility object, duplicate authority or new scheduling architecture.
- Typed agent calls: no model SDK, prompt output or agent-tool execution contract changed; native MCP calls use existing typed wires and validated tool declarations.
- Official framework practices: existing Pydantic model/Field/model-validator and wire-inheritance patterns retained. No dependency/lock/settings-source changes; label continuation is checked by the existing cursor_pages primitive.
- Type safety: strict production source remains clean; required mapping removes ambiguity at the constructor, reported alias identity is checked through an existing typed wire. No new Any/cast/ignore/default was introduced outside exact existing donor optional wire/config semantics.
- Hygiene: isolated exact-parent single commit, donor/M2 comparison, current constructor/census/docs migration, no source overlap or maintained publication. Immutable affected/full test evidence separated from prior failures.

## Remaining criteria, dependencies and root integration

This closes the shared label/config/bootstrap and pure scope-read extraction obligation locally, pending independent root acceptance. It does not complete M1 or authorize closing PR72. HTTP ScopeRefRequest/WorkflowRequest.scope/handler/submission cannot be accepted end-to-end until M3 carries scope through AsyncioJobQueue and WorkflowEngine into native execution; they remain pending. The donor actual read_fire_spec alias cases remain with that M3 binding. Organize policies and writes belong to M2; source/queue walk core M3; final delivery composition M5.

Root next action: independently review ddb8cde, integrate this exact descendant into maintained PR119, propagate the actual M1 prerequisite into maintained M4, then reconcile M2's provisional shared copies against the map without overwriting its distinct policy code. Current M1 helpers and constructor are the single generic owner; namespace bootstrap is also needed for M2's OWNED mapping boot failures. `domain/bootstrap_mappings.py` exists in neither base2bc nor donor360 and was not invented. Any maintained publication/review gate and the existing full-gate MCP limitation remain root responsibilities. Child performs no PR/initiative/Notion update.

Owning-lane author evidence: https://linear.app/duckburg/issue/KOD-73#comment-3ccfce73-947c-4349-bfdf-9fdcc5e16b35 . No issue status changed. Full gate completed exit0 and final worktree status is clean at ddb8cde.
