# Independent Audit L7 review — REQUEST CHANGES

Reviewed source: `dd11ffd5439472a664ff5fc082ac8c83b11d0ee5` (parent `080fa6c`), then the separate preflight correction `38e848b2fd707df48bfe96cfcf2be42fd8da9ff6`. Review tree: `/private/tmp/kodezart-v03-audit-independent-current`, detached and clean. Original donor trees were not edited. Requested inherited Astra ultra; effective runtime configuration unverified.

I read live KOD79/531/535/538/539/773/170 and current comments, then the actual source diff and executable oracles, before using the author envelope as explanation. This is independent Audit review. The separate native checkpoint report is author-owned framework research and is not independent native acceptance.

## Findings

**P1 — A real Git read outage escapes the per-scope failure contract and aborts the scheduled run.** `SubprocessGitService.remote_branch_sha` calls `_run_with_exit_codes`, whose nonpermitted exit at `src/kodezart/adapters/subprocess_git_service.py:534` is a generic `RuntimeError`. The new Audit owner deliberately excludes generic programming failures from `AUDIT_READ_FAILURES`. With the actual production constructor, real MCP adapter, AgentService, local Git repository and worktree provider, removing the remote directory during the actual `AUDIT_CLAIM_SCHEMA` dispatch causes `git ls-remote` to exit 128. The RuntimeError escapes `audit.run`, rather than producing an addressed incomplete scope and allowing the owner's remaining-scope path to continue. This is operational unavailability from a real subprocess, not a fixture-generated programming exception. Correct the operational error at the Git boundary or an equally narrow typed consumer seam; do not restore a broad RuntimeError catch.

**P2 — Native lane discovery still swallows programmer failures.** `src/kodezart/services/audit_requests.py:155` catches `Exception` around the actual native owner and lane-record reads. An external MCP backend implementation raising one specific `RuntimeError` object from `list_comments` is converted into an unavailable target and ultimately `AuditRunIncompleteError`. The expected original programmer exception does not escape. Paired cancellation preserves its original identity; a declared MCP tool outage correctly becomes addressed unavailability. Narrow this remaining catch to declared operational/validation failures. The existing author test that injects an executor programming failure does not cover this request-reader path.

Both findings reproduce at the original source and the final preflight correction. They are not repaired by adding required criterion/decision mapping preflight. No clean coverage or unauthorized tracker mutation was observed in these two probes; the defects concern truthful failure classification and continuation.

## Exact independent execution

All logs are in `/private/tmp/kodezart-recovery-session`. Commands ran with `/Users/kodezart/.local/bin/uv run` in the detached review tree. Source remained unchanged throughout each command.

1. At `dd11ffd`, `pytest -q tests/integration/test_audit_configuration.py tests/integration/test_audit_report_records.py tests/integration/test_audit_runtime_native.py tests/integration/test_audit_runtime_types.py tests/integration/test_audit_scheduler.py`: **56 passed in 649.38s**, `audit-independent-current-original-tests.log`.
2. At `dd11ffd`, `PYTHONPATH=/private/tmp/kodezart-v03-audit-independent-current uv run pytest -q -c pyproject.toml /private/tmp/kodezart-recovery-session/test_audit_failure_boundary_independent.py`: **2 failed / 2 passed in 35.81s**, `audit-independent-failure-boundaries-corrected.log`.
3. Same exact independent probe at `38e848b`: **2 failed / 2 passed in 19.18s**, `audit-independent-failure-boundaries-38e848.log`.
4. At `38e848b`, with the same PYTHONPATH, `pytest -q -c pyproject.toml /private/tmp/kodezart-recovery-session/test_audit_preflight_capability_probe.py tests/integration/test_audit_preflight_capability.py tests/integration/test_audit_configuration.py tests/integration/test_audit_scheduler.py`: **36 passed in 3.83s**, `audit-independent-preflight-corrective.log`. This includes the three unchanged capability controls and real lifespan/registration paths.
5. At `38e848b`, `mypy src/kodezart/composition/audit.py src/kodezart/services/audit_runtime.py src/kodezart/services/audit_publication.py src/kodezart/services/audit_escalation.py src/kodezart/services/audit_failures.py src/kodezart/types/domain/audit_runtime.py`: **six source files clean**, `audit-independent-types.log`. Ruff over those same six sources passes, `audit-independent-ruff.log`; `git diff --check` and status are clean.

The first external probe invocation omitted PYTHONPATH and failed collection with `ModuleNotFoundError: tests` (one collection error, 14.15s), preserved as `audit-independent-failure-boundaries-first.log`. Only the command was corrected; the probe bytes and oracles were unchanged.

Independent probe: `/private/tmp/kodezart-recovery-session/test_audit_failure_boundary_independent.py`, SHA256 `cb829b08acb578bc7c3ec0d6fb56ed1f369b13023e760acd20e0b3393dcdccb8`. Its four cases cover programmer identity, cancellation identity, declared MCP outage, and actual Git remote loss. All doubles are external boundaries; the production builder/owner/native MCP adapter/Git/AgentService/workspace/WriteBackVerifier components remain actual. No full suite or live external writer was run, and author-reported pass totals are not counted as independent execution.

## Eight scoped lenses

1. **SOLID:** composition owns configuration and real collaborators; the scheduled owner owns coverage, typed observations and publication ordering; existing individual detectors retain semantic responsibility. The request-reader catch violates the advertised failure responsibility and is finding P2.
2. **DRY:** the implementation reuses the existing `ScheduledPass`, `AuditCoverage`, canonical `WriteBackVerifier`, fresh judge, native readers and escalation writer. It adds no second scheduling clock or semantic verification loop. The shared failure tuple is sensible only once the actual participating ports honor it (P1/P2).
3. **Hexagonal boundaries:** the production path is reachable through `main`/passes and native adapters. The corrective preflight validates both declared and actual adapter capability. The tests exercise real Git, MCP decoding and AgentService while replacing external services. The raw Git operational error is the remaining concrete boundary mismatch.
4. **KISS:** explicit scope/repository/report bindings and one tick/full-coverage owner avoid inference from unrelated Organize config. Incomplete reports retain actual partial results rather than inventing state transitions. No new tracker completion authority is introduced.
5. **Typed agent calls:** actual schema dispatch remains through existing typed sessions. Fresh write-back judgments run in independent workspaces; actual repair inputs retain the previous canonical artifact, findings and rounds. Unverifiable observations do not become authorized publication artifacts. Refuted claims still require their actual mandate arm; author rationale is not forwarded as verifier evidence.
6. **Framework match:** installed Pydantic 2.12.5, LangGraph 1.0.10 and mypy 1.19.1 were recorded in `audit-independent-framework.log`. The new Audit owner uses the existing scheduler rather than a new LangGraph scheduling abstraction. Actual Pydantic schema/validation controls and real FastAPI lifespan tests passed. No new framework guarantee, backend transaction, persistent scheduler cache or process-restart proof is claimed.
7. **Type safety:** closed `kind` publication and `status` scope-result unions distinguish current claim, historical forge, removal, overclaim and terminal observations. The historical forge arm binds `graded_sha` to the recorded claim; it is not a claim about current head checks. Complete reports require actual HOLDS receipts and matching scope; incomplete reports preserve raw observations, completed reports and actual repair inputs. This is a stronger typed runtime boundary. Static success does not prove operational exceptions are classified correctly, as both probes demonstrate.
8. **Hygiene and oracles:** no source edits, suppression or acceptance-oracle changes were made by this reviewer. Existing tests prove cancellation/cleanup, current-source and record drift refusal, privacy gating, two windows, actual summary receipt identity, bounded repair and next-scope continuation for declared failures. The prior fixture migration from RuntimeError to GitSourceReadError is not sufficient evidence for the actual GitService port; the real subprocess probe exposes that gap. Exact original tests and red evidence are retained.

## Scope, risks, integration

The candidate materially adds actual production Audit execution and canonical publication, but this review **does not approve integration until the two failure-boundary findings are corrected and the unchanged four probes pass**. The preflight correction itself passes its bounded independent checks. After repair, root should serially assemble `dd11ffd`, `38e848b` and the minimal correction, then rerun the affected integrated selection and static gates.

Known residuals remain explicit: classified lapse/state-transition authority is unavailable; this candidate must not mark criterion or parent state. Configuring generic `records.audit` remains a named refusal because those sinks lack the required canonical verification boundary. KOD531's whole-repository adoption census is not completed by this slice. KOD773's raw-versus-completed publication fork remains held; the source retains the distinction and does not settle it. Backend read/write races have no CAS guarantee. No full L7, initiative or release completion claim follows from these passes.

Relevant live contracts: https://linear.app/duckburg/issue/KOD-79 ; https://linear.app/duckburg/issue/KOD-531 ; https://linear.app/duckburg/issue/KOD-535 ; https://linear.app/duckburg/issue/KOD-538 ; https://linear.app/duckburg/issue/KOD-539 ; https://linear.app/duckburg/issue/KOD-773 ; https://linear.app/duckburg/issue/KOD-170 . Own KOD79 evidence link will be appended after the write is confirmed.

Confirmed own evidence: https://linear.app/duckburg/issue/KOD-79#comment-0d1ccf56-a9cf-40b2-bede-cb758994d6a9 .
