# Independent L2 correctness/concurrency review

## Corrective review accepted: ee1d44b + d52f0c7 + 68eabb5

Independently inspected and applied only the three bounded corrections to the original161 review tree. ee1d44bd540304f5effce4021f547768cfe5d82d is local975e9dc with identical tree6d9b7b15fff2db17f42c6206db1e5c9aa9e4e21b; the copied original probe file was byte-identical before application. Original eight owner probes pass unchanged (two tick cases pending at that stage),0.67s; fourteen complete affected modules438 pass14.59s. Logs organize-independent-correction-original.log and -affected.log. Strict301-source mypy and Ruff pass, -correction-types.log.

Current membership, original proposal body digest and final approval checks now protect the author/criterion paths. Phase completion checks current policy after awaited proof; the canonical escalation writer invokes its supplied same-policy guard before each actual write. Write-back exhaustion retains actual WriteBackResult rounds/evidence. These correct the measured owner defects without claiming atomic tracker snapshots or in-flight fencing.

d52f0c783eeccbff1ecd4399442383fb7bd71dfa is local9535365. The scheduled grooming row now runs the real configured owner; preflight rejects partial declared bindings, and main passes the actual workspace into this construction. The other legacy schedule rows remain intact. Tick remembers the first typed halted report and processes remaining targets before raising. Original10 plus actual scheduler/lifespan and retained typed-halt controls18 pass2.92s, organize-independent-composed-original.log.

The first typed halt version incorrectly inherited issue_key=scope.key for all container kinds. Root corrected that in68eabb5157bbebd82ed181914bd39e32c62ea21c (local7c85975): direct Exception with only actual ScopeRef and OrganizeReport. The only original-probe change is the tick exception import/catch; progress assertions remain identical and owner refusal controls retain their original catch. Final18 plus full prompt-pass and Git-settings modules **77 pass18.70s**, organize-independent-composed-final.log. An additional independent four-container identity control proves scope kind/report preservation and absence of fabricated issue_key: **4 pass0.05s**, organize-independent-halt-identity.log. Final strict301-source mypy and source Ruff pass, organize-independent-final-types.log. Final review source tree matches donor68 tree exactly:6b1f95ca82331548b7263d66c8809ecd05b1788d. Only the additional isolated identity probe is untracked; no source edits.

Correction verdict: integrate these bounded deltas with161. SOLID/DRY/Hexagonal retain the existing owners, canonical verifier and lease; KISS improves first-halt continuation without a second coordinator; typed agent calls and framework practices remain unchanged; Type safety improves with actual report propagation and correct container identity; Repository hygiene improves by measured lifespan wiring and exact unchanged progress oracles. Previously recorded unsupported graph-hygiene/L6/full-L1 and backend nonfencing limitations remain. Do not cherry-pick the review ancestry wholesale: apply owned161, author correctionee1, root compositiond52, then typed identity68, resolving already integrated shared dependencies serially.

Reviewed exact frozen161ca93fd9fb83d64516ee11b8ed53944a9a517e, own49-file delta against its first parent, in isolated detached /private/tmp/kodezart-v03-organize-delivery-review. Donor was clean at161ca93. The existing organize-native-review contained another reviewer's probes and was preserved. No production source, canonical integration, issue-state, initiative or Notion edits. Requested Astra ultra; effective identity unverified. Verdict: request changes before integration acceptance.

## Findings

1. **A subject leaving the configured scope still receives body and phase-marker writes.** services/organize_owner.py::_may_write (278) checks approval and gate but never current target membership. The original subjects roster remains after authorship. _proof_live proves current judgments match the current scope but does not prove the particular marker target belongs to those judgments. Independent test_current_scope_membership_binds_author_write[True] reparents child-subject outside the configured issue subtree during its actual author session; the adapter subsequently sends both description replacement and graph-complete addLabels for that departed issue. The unchanged-membership positive completes normally. This is scope authority leakage, not an in-flight backend race.

2. **A criterion proposal survives a changed parent specification.** _author_write's body branch uses ProposedWrite.revision for expected-body replacement, but its criteria branch (373–444) never checks that revision before creation. test_criterion_author_is_bound_to_current_parent_revision[True] changes the parent body during the actual criteria-author session; one old Check/Do child is created before later verification records an exhaustion. The no-change positive passes. Require the actual proposal parent revision at the protected create boundary; later detection cannot retract the stale write.

3. **Approval can arrive during the last awaited gate read and still permit a body write.** _may_write reads execution_approved first, then awaits read_scope_labels; its caller invokes edit_description afterward. test_approval_is_current_after_last_awaited_gate_read[True] inserts approval after the second post-grant get_issue response (the scope-label read), and observes the description save. The unchanged-read positive passes. A final approval reread after gate/membership validation addresses this measured seam. No finite read sequence can promise atomic fencing against mutation after its last read or an already-issued backend commit; that remaining limit is explicit.

4. **A halted configured binding starves all following targets and drops the typed report at its caller boundary.** services/organize_tick.py:81 immediately raises a reason-string OrganizeWriteRefusalError inside the target loop. test_halted_first_binding_does_not_starve_second_binding[True] uses two real factory-created owners and the real tick; the second owner sees zero executor calls. The first-halt-false control reaches and completes the second distinct scope. Root owns this correction. Smallest suggested contract: carry actual ScopeRef + OrganizeReport on typed OrganizeHaltError, preserve the first halted record locally, process the remaining targets, and raise that recorded halt after the loop. Each report is already logged in full. Do not catch cancellation/infrastructure errors broadly or add a second report coordinator. Aggregate results only if an actual consumer requires them.

## Independent execution

Independent source-preserving probes live at tests/chains/test_organize_delivery_independent.py. Only boundary MCP/executor/workspace/Git doubles are changed; the owners, admission/author/verifier, adapter and leases are actual production components. Tick targets contain actual factory-created owners with distinct valid scope identities; the second-target positive proves the fixture can complete.

`uv run pytest -q tests/chains/test_organize_delivery_independent.py`: final **4 failed /6 passed in1.59s**, organize-independent-counterexamples-final.log. The four failures above are expected review counterexamples, not a passing selection. Initial three-counterexample stage:3 failed/4 passed in3.00s, organize-independent-counterexamples.log. Probe formatting initially found3 E501 lines; formatter corrected them and the final Ruff check passes. No oracle weakening, xfail or source edits.

The two additional independent positive controls establish foreign-holder refusal with zero create writes, same parent+Check replay preserving one native identity, and owner cancellation during an issued create. Cancellation waits for the external response while the lease remains held, releases after settlement, and leaves one ungraded child without criteria-complete marker. This does not claim the backend can retract already-issued creates after expiry; the existing delayed expiry test remains an explicit characterization.

`uv run pytest -q tests/chains/test_organize_owner.py tests/chains/test_fresh_write_back_judge.py tests/chains/test_write_back_verifier.py tests/chains/test_organize.py tests/chains/test_organize_ownership.py tests/tracker/test_criterion_creation.py tests/core/test_organize_settings.py tests/domain/test_organize.py tests/domain/test_organize_routing.py tests/types/test_wire_schemas.py tests/prompts/test_organize_mandate_bindings.py tests/prompts/test_organize_call_bindings.py`: **431 passed in26.44s**, organize-independent-affected.log. This is twelve affected modules, not the full suite.

This corpus exercises fresh assessment/judgment sessions with session_id=None, bounded refutation-driven repair carrying cited_refs, actual reread artifacts, no completion marker on exhausted bounds, current expected-body protection, missing/foreign/ambiguous criterion identities, exact empty Evidence and native initial state, replay without rewriting existing child content, no retry after unknown create response, actual worktree cleanup under repeated cancellation, and the exact schema-dispatch mutation census. The shared missing-schema mutation now removes every occurrence of the selected schema, so it actually tests global absence when a second L5 consumer exists; equality/owner-specific forwarding constraints remain intact.

`uv run mypy src/`: **301 source files pass**, organize-independent-types.log. `uv run ruff check src/ tests/chains/test_organize_delivery_independent.py`, format check and git diff --check pass; **302 files formatted**, organize-independent-ruff.log. Exact installed versions recorded in organize-independent-frameworks.log. Source was not changed or cherry-picked after161ca93.

## Current requirements and measured scope

Read current KOD74/368/820/475 bodies and all comments, including KOD74 Sep1 breadth-first/preapproval/no-ticking/current-body rulings and Sep12 composition/type findings; KOD820 Sep12 3f13d3fc supersedes the historical missing-surface assignment block and authorizes restoration of the one real fresh WriteBackFinding judge. This review does not use the author's freeze narrative as its oracle.

build_organize_owner and build_organize_tick construct actual consumers and are exercised, but this donor has no main/passes callsite to build_organize_tick. Application scheduling integration remains root-owned; no request-entry or running application claim is inferred from a factory. The tick passes scheduled RunIdentity.title as holder; it is not a queue job id. Root must preserve the approved caller ownership contract at actual composition rather than presenting this as full L1 queue-owner evidence.

KOD368 graph hygiene (parent/blockedBy/relatedTo/priority/milestone/split) remains explicitly unavailable via a typed proposal refusal; this patch does not implement that criterion in full. Existing criterion edits, approval placement policy, event-state authority and L6 scope-terminal semantics remain separate. OrganizeBoundEvidence contains actual configured field/value/round arithmetic, but that alone is not a composed L6 ScopeStoppingRule/terminal producer. No criterion completion or human approval write is introduced.

## Eight lenses and integration

SOLID: one owner orchestrates existing admission, author, canonical verifier and adapter boundaries; findings need stronger validation at those same boundaries. DRY: current lease/admission/write-back mechanisms are reused; avoid a second policy helper in corrections. Hexagonal architecture: production components are tested using external doubles, with exact tracker identity readback. KISS: tick's current early raise is too lossy; typed first-halt propagation after remaining targets is sufficient without a new coordinator. Typed agent calls instead of semantic heuristics: discriminated proposal/admission variants and WriteBackFinding schemas drive behavior; no semantic defect substring classifier was added. Official framework practices (version matched): installed Pydantic RootModel/discriminators, registered raw schema output, actual fresh AgentService sessions and asyncio settlement were exercised. Type safety: admission/proposal legal-state partitions improve the agent boundary; WriteBackFinding carries real refutation evidence. Tick's current flattened halt loses typed evidence and needs correction. Runtime freshness findings remain material even though strict typing passes. Repository hygiene: isolated source-preserving review, all diagnostics retained, source checks green; no blanket suite or lane-completion claim.

Do not integrate161ca93 as accepted until the bounded author current-source/membership/approval corrections and root tick report/progress correction freeze and the original probes plus affected corpus are rerun independently. Root should serially integrate only the owned161 delta and reviewed corrections, preserving already integrated shared dependencies and native/L5 schema additions. L3 finalf632 docs correction was reviewed separately and accepted; this report does not reopen it.
