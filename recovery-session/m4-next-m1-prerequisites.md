# Minimal M1 closure for fuller M4 readback and modern judge boundary

Read-only source inventory against accepted7892ca1a47adbcfd91fbcfb1e580dfc86f066a99, compared with M1a4575cd / tree-equivalent57. No source changes or runtime acceptance from this inventory. Existing extraction overlay remains authoritative for later runtime ownership. All following line references are canonical7892, not guessed current working-file positions.

## Native classification and criterion artifact prerequisite

**M1 shared native read contract, smallest extraction:**

1. `types/domain/tracker.py:208-209`: `TrackerIssue.issue_labels: frozenset[str] = frozenset()` and its semantic-key comment. Existing `issue_key`, title/body, state_name/state_kind, queue_states and parent_key already exist on M1. Do not copy `TrackerIssueRevision`, milestone/graph fields or state-history model solely for this reader.
2. `types/domain/operation.py`: only `OperationConfig.issue_labels`569, nonblank/unique semantic-key/backend-label validation689-699 and `FIELD_OWNERSHIP['issue_labels']`1045. This field is absent on M1. Add actual config/example/census rows matching its real native consumer; no Organize mandates or scopes needed for criterion membership. Full current constructor also has scope_labels and criteria_stage_label_key; those are **not dependencies of read_criteria or these artifact rows**. Scope approval and FIRE stage admission consumers keep their later ownership.
3. `adapters/linear_mcp_tracker.py`: constructor `issue_labels: Mapping[str,str]`811 and `_issue_labels = dict(issue_labels)`824; `_to_issue`4312-4316 maps actual reported backend labels to semantic keys. `composition/tracker.py:156` passes only the actual declared `operation.issue_labels`; do not import `resolve_organize_mandates`/MandateKind/criteria_stage_label_key157-160 for a read-only M1 closure. Matching FakeTrackerPort configured classification mapping/field support and fixture constructors must travel with the shared API, not M2.
4. `core/protocols.py:702-714`: existing narrow `TrackerCriteriaReader.read_criteria` returning `Sequence[TrackerIssue]`; integrate existing `TrackerPort` inheritance for that protocol. No TrackerSpec, native FIRE admission, criterion resolver or creation writer belongs to this dependency.
5. `adapters/linear_mcp_tracker.py`: exact `read_criteria`1822-1824, `_read_criterion_family`1850-1868, `_read_criteria`1870-1913. They read full actual children from parentId listing with includeArchived=True; use existing `cursor_pages`/LinearScopeIssuesWire and page-size constant; each native child gets a full get_issue(includeRelations=True), strict membership validation, exact child/parent identity, configured semantic criterion filter and deterministic key order. `_read_criterion_family` needs `CriterionReadError` and preexisting OperationMemberAbsentError / neutral TrackerUnavailableError / TrackerProtocolError. Extract only `domain/errors.py:227-233` CriterionReadError, not native criterion-resolution/amendment errors. Existing child-family reads never need create_criterion_if_absent, _unstarted_state_id or TrackerSpec.
6. `adapters/linear_mcp_types.py:216-224`: exact `LinearCriterionIssueWire(LinearIssueDetailWire)` requires actual labels and nullable-but-present parent_id. Existing base LinearIssueWire labels defaults empty; that default is deliberately insufficient for strict current criterion membership. Do not copy workflow-state history or graph wire fields for this read closure.

**Strict capability pieces available in existing accepted source (M1):**

- `LinearPlanningIssueWire`187-191 requires labels and relations; `LinearMcpTracker.read_planning_issue`1005-1011 + corresponding TrackerPort method818-820. Strict consumers must use this actual path if omission cannot mean empty. It shares `_to_issue`, not a second reader.
- `_classification_label`1013-1019 and `require_scope_plan_reads`1074-1079 require actual criterion+decision mapping, raising OperationMemberAbsentError without backend IO. `require_issue_classification_reads`1081-1088 adds tracker plus explicitly requested keys; latter is needed by other native readers, **not automatically required by a criterion-only artifact read**. Include actual port signatures833-849/fake capability implementations only where selected consumers require them; do not invent a blanket startup requirement for all apps.
- `read_labeled_issues`1021-1072 is the full native classification roster for later scope/native consumers, not a direct dependency of current read_tracker_artifact. If extracted for shared readiness, it also needs `IssueLabelReadError`11-17 and its full strict pagination/readback controls. Keep this distinct from minimum child-family roster.

**M4 reader hunk after M1:** `services/tracker_artifacts.py`: import json, `_SUPPORTED` additions CRITERION_SUB_ISSUE/CRITERION_CHILD_SET/ISSUE_LABEL_SET and only current three-arm block86-151 (exact line positions can be regenerated). It serializes current issue_key,parent_key,title,body,state_name,state_kind,configured issue_labels and queue_states, sorted deterministic rows; wrong-parent/duplicate/classification absence refuses. This is the full accepted161ca93 responsibility, not M2. Current graph_snapshot import and ISSUE_GRAPH/ISSUE_SPLIT_SET arms belong M2 and must remain excluded.

**Source-review caveat for fuller M4:** canonical artifact arm currently calls ordinary `read_issue`, not strict `read_planning_issue`, and does not itself call the capability guards. Thus extracting the strict M1 methods alone does not prove that omitted native labels or absent configuration are refused on every artifact read. A future M4 owner must independently test that exact call path before claiming full label readback; this inventory does not silently substitute a new implementation. The current first slice typed-refuses all three surfaces, so it does not introduce that exposure.

Existing regressions to retain/extract at actual boundary:

- `tests/tracker/test_criterion_reader.py`: exactly direct configured children, successful empty versus failed parent, parent text cannot create membership, fresh labels/parentage, preservation of native child state/evidence. Label-write test in that module belongs its writer closure, not necessary M1 read adoption.
- `tests/tracker/test_criterion_reader_boundary.py`: later pages/full details/page overlap; malformed/non-advancing pagination; child moved during listing; missing config; typed failed listing; required membership keys; descriptionless native criterion remains a member; **actual build_tracker declared label spelling**142ff. Do not weaken these into helper-only checks.
- `tests/tracker/test_scope_planning.py`: strict omitted labels/relations and current classification mapping portions; exclude semantic scope planner runtime.
- `tests/tracker/test_issue_classification_reads.py`: required mappings, blank mappings and additional required phase keys, zero-IO capability validation.
- Current M4/Organize test modules contain reader-specific identity/parentage/duplicate controls; extract only artifact readback assertions and boundary fixtures, not owner/author machinery. New minimal controls should cover all three arms, actual backend labels omitted, wrong child identity/parent, duplicate family identity, deterministic order and changed state/body/label after prior read.

## Modern PermissionMode / ToolPreset boundary

This is a shared M1 application-to-engine boundary, not a new M4 shim. Moving only the enum or generic helper would leave base callers passing vendor strings to an adapter that correctly refuses them.

**Exact M1 contracts:** `types/domain/session.py`: PermissionMode22-34 (interactive,accept_edits,plan,unattended), ToolPreset50-61 and AllowedTools64. SessionFailureKind and ORGANIZE_PASS are independent later responsibility, not required by the judge. `core/constants.py` EVAL_PERMISSION_MODE/UNATTENDED_PERMISSION_MODE become enum members; removal of old EVAL_TOOLS/AUTHOR_TOOLS/etc requires every existing consumer hunk, not premature deletion.

**Exact adapter translation:** `adapters/_permission_modes.py` whole existing map+map_permission_mode; `adapters/_agents_mapping.py` only AllowedTools/ToolPreset imports, _SDK_TOOL_PRESETS139ff and map_allowed_tools160ff. Retain explicit caller lists; preset expansions stay at SDK boundary. `claude_agent_executor.py` and `claude_client_executor.py`: signatures/imports, actual map_permission_mode and map_allowed_tools option construction only. Existing run_identity/event changes in these files remain separately owned; no wholesale file copy.

**M1 live boundary consumers:** `AgentExecutor.stream`, all `AgentRunner.stream/stream_workflow/stream_in_workspace` protocol signatures; AgentService matching methods; existing QualityGate/WorkflowEngine signatures only where they carry the same config; workflow `WorkflowConfig` / `WorkflowSubmission` permission_mode and allowed_tools annotations. Existing HTTP wire remains precisely Literal['plan','bypassPermissions']; `types/requests/agent.py` names HttpPermissionMode and `handlers/agent_handler.py` _HTTP_PERMISSIONS plus its two actual dispatch/submit conversions24-27,114,153. Do not expose internal enum values at HTTP or copy unrelated new scope handlers/state.

All canonical source consumers are enumerated in `m4-modern-session-callers.txt` (git grep over exact7892). Existing-base consumers needing mechanical annotation/preset/constant migration include agent_content_scanner, git_change_persister, ralph_loop, ralph_workflow, remediation, ticket_generation, existing composition/engine, fire_dispatcher and prompt_pass plus fakes and actual SDK tests. They are **shared API migration hunks**, not ownership transfer of whole lane runtimes.

**M4 owned helper consumer:** `services/audit_sessions.py::judge_in_workspace` uses `allowed_tools=ToolPreset.EVALUATION` in current7892, replacing historical704 list(EVAL_TOOLS) only after the shared boundary exists. Its FreshAuditSession neighbor stays M6. M2/M3/M5 later caller modules authored_delivery,fire_implementation,fire_review,fire_specification,scope_runtime retain their milestone ownership; they do not need to be introduced in M1 just to change the boundary. Existing source file census distinguishes absent-base later consumers from already-present application callers. No invented compatibility shim or duplicate preset expansion.

Existing tests: `tests/adapters/test_permission_boundary.py` actual both SDKs, existing HTTP values/defaults, rejection of widened HTTP inputs, untyped adapter input before SDK creation, domain invalid values, checkpoint config restoration and cancellation/cleanup; the opt-in harness probe's mapper preserves interactive alternative. `tests/chains/test_tool_preset_composition.py` actual graph→native session controls must be split by actual already-present graph; new native graph cases remain later. Existing executor/options/subagent/grant/session-policy tests and the extracted actual FreshWriteBackJudge test must run with exact enum/preset oracles. Prompt/token census and SDK mapping behavior remain exact.

## Evidence and limits

Commands used: pinned git show/git grep/git ls-tree reads only; no tests or source changes performed for this inventory. Two mistaken guessed source paths (`adapters/linear_issue_reads.py`, `types/wire/linear.py`) returned absent; actual source was then located as linear_mcp_tracker.py and adapters/linear_mcp_types.py. No missing-file content was assumed. The isolated first-slice full gate remained byte-immutable throughout inventory; later first-slice fault-oracle tightening is separately logged and unrelated to this proposal.

Verdict: this gives an acyclic M1 native-read and shared SDK boundary prerequisite, then fuller M4 artifact/judge consumption. No M2 author/graph dependency is necessary. Root should serialize actual hunk extraction and independently recheck the strict-artifact call-path caveat before claiming complete supported surfaces.
