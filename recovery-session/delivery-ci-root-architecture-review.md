# Root architecture and type review of CI migration

Approve the bounded migration at 2eddd4d0b62220f6a6840b09f442fd04531078cd for canonical integration. Root read the immutable source diff against e4b21ea, then the new actual-adapter regression and author/independent execution reports. No root source changes. This is a separate architecture/type review from the native worker's independent correctness run, and is not approval of the unfinished lane-delivery graph.

The public monitor now returns a coherent observation instead of requiring callers to recover evidence through task-local getters. Completed observations validate commit identity, a nonempty named roster, membership of the failed subset, and verdict consistency. Absent and incomplete observations are distinct; authored consumers refuse incomplete evidence and audit preserves unverifiability. Completed reruns must retain the initial SHA; declared prerequisites and every observed failed subset still feed the one existing classifier. No semantic decision is inferred from summary prose. The internal task binding remains only for selecting an actual backend rerun attempt.

The adapter validates raw check identity cardinality and a single SHA before constructing the portable model. The fake supports that same public contract; the actual HTTP-boundary test, not fake agreement, proves portability after the producing task ends and the absence of a second remote read. Migrated union-isolation guards still forbid actual forge calls, while no longer mistaking the immutable failed-name field for the removed method. Existing authored cleanup watches intentionally observe the published cleanup branch; requiring the earlier feature SHA would change their contract.

Eight lenses: SOLID improves because a watch owns its whole observation; DRY preserves one classifier and removes duplicate evidence readers; hexagonal improves through a vendor-neutral returned value; KISS removes two getter paths and one context registry; typed judgment remains an explicit validated boundary with deterministic SHA/roster/declaration enforcement; Pydantic discriminated union/frozen validation follows the resolved framework mechanism already verified in the framework report; type safety improves from tuple plus implicit task state to three explicit result arms without new Any/casts/ignores; hygiene is neutral/improved across the necessary 38-file consumer migration, with no new config or second classifier.

Execution evidence inspected: author delivery-ci-before.log reproduces the tuple boundary failure; all later failed diagnostic runs and their corrective module reruns are preserved in delivery-ci-review.md. Independent reviewer ran seven actual CI/rerun/classifier/authored/audit modules at the exact detached SHA: 254 passed in11.98s, delivery-ci-independent-native.log. Author strict mypy covered nine changed implementation modules. Root did not repeat those executions; fresh integrated typing and affected tests follow integration. No full-suite, live-forge, new delivery-publication, or release claim.

Current KOD321 literal method-count wording is stale relative to the authorized coherent-return repair on KOD77. This review preserves its substantive structured failed-set/no-prose contract and does not silently edit the acceptance clause or issue state. Exact runtime method migration needs a superseding tracker finding, not a claim that unchanged wording was met.
