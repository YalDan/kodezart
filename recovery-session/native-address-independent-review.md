# Independent addressed-native reader review

ACCEPT the bounded canonical correction through b84d43ba44131c8d43d2f3689844cf928ca2276e, cumulative delta from47af373d8006849cbf773ab3cd3f5ebf5910e297. Exact tree c00364c07cf4bbf642c397edd3e51185903e80ed. Six files, three adapter source files. The final b84 commit is tests-only; source prerequisites0aad6e64891d14c2440bf7a852f0e5633a1a5c4f, a05c169a12c70b5e18961fd3313ba54ce1961427 and7cd8e703b3360c0a2f9255eb66989ed95cf5c5d3 are part of this verdict.

Independent detached source: /private/tmp/kodezart-v03-native-address-independent. Source remained pristine. Only tests/tracker/test_address_identity_peer.py was added as independent probe, not integrated. Current KOD73/76/744 requirements were read; this review applies the addressed native identity and fresh approval requirements, without importing the author's green run as evidence. Requested inherited Astra ultra, effective metadata unverified.

## Source and type verdict

The new LinearAddressedIssueWire retains optional actual UUID evidence at the vendor boundary. It accepts literal canonical id equality, or a requested UUID equal to the UUID in the actual response; an arbitrary display key cannot match through an unrelated UUID. This does not infer a canonical identity from title, label, parent or assignee. The same helper is inherited by LinearApprovalIssueWire; required id, labels, parent_id, attachments and documents remain required. Only uuid is optional, so canonical display requests still work without UUID metadata. Malformed supplied UUID data fails validation.

The criterion family preflight uses the existing nonblank classification guard before backend IO. The raw parent response is checked before listing its children. The supplied-parent arm requires exact canonical key equality. Actual approval entry verifies the raw response before selecting its returned canonical key; its single-read cache lasts only for that invocation. read_fire_spec passes the already-validated canonical subject into the existing family path. The domain ancestry resolver is byte-identical to baseline and retains exact issue equality and cycle checks. No global read_issue weakening, extra approval source, write authority, retry engine or state transition was introduced.

## Actual independent execution

uv run --locked pytest -q tests/tracker/test_criterion_family_aliases.py tests/tracker/test_criterion_family_identity.py tests/tracker/test_native_approval_aliases.py tests/tracker/test_fire_spec_reader.py tests/tracker/test_fire_spec_approval.py tests/tracker/test_fire_spec_approval_ancestry.py tests/tracker/test_scope_approval.py tests/tracker/test_criterion_reader_boundary.py tests/tracker/test_scope_alias_identity.py:173 passed10.18s. Log native-address-independent-b84d43b.log.

New actual adapter/external-MCP controls: swapped display response with unrelated reported UUID across approval/spec/family/labels; omitted required labels and parentId across approval/spec/labels; revocation between repeated UUID alias reads. Corrected final11 passed0.33s. Log native-address-peer-controls-corrected-b84d43b.log.

Original new controls produced2failed9passed2.22s because my initial test required TrackerProtocolError at read_fire_spec, whose unchanged consumer catches that precise error and raises CriterionReadError with cause. Original source/probe diagnostic preserved as test_address_identity_peer_original.py and native-address-peer-controls-b84d43b.log. Corrective oracle changes only the spec arm's expected error to CriterionReadError and additionally requires the TrackerProtocolError cause. All no-membership/no-save assertions remain. This was an oracle correction, not a production defect.

Strict mypy on all3 source files passed. Ruff on6 candidate files plus independent probe passed. Logs native-address-independent-mypy-b84d43b.log and native-address-independent-ruff-b84d43b.log. Source byte hashes and actual required-field inspection: native-address-peer-source-proof.json. All artifact paths are under /private/tmp/kodezart-recovery-session.

## Eight lenses

SOLID: vendor address attestation stays in wire mapping; domain approval arithmetic unchanged. DRY: one shared matches_requested helper for actual reads. Hexagonal architecture: UUID remains vendor-local, TrackerIssue remains canonical and neutral. KISS: explicit equality and standard UUID parsing, no inferred aliases or new observer. Typed agent calls instead of semantic heuristics: no agent schema changed; native identity never uses prose. Official framework practices: standard locked Pydantic2.12.5 UUID conversion and inherited required fields; no dependency update. Type safety: improved optional-evidence representation and preserved strict approval field/identity constraints, no Any/cast/ignore. Repository hygiene: isolated immutable source, exact cumulative commit pins, retained independent controls and corrected diagnostic.

## Limits and integration

184 distinct independent passing controls, no full canonical suite claimed. Snapshot reads are not backend fencing; this is no guarantee of atomic cross-system membership/approval. Existing arbitrary-alias behavior elsewhere is not widened by this bounded path. Root can integrate the four commits in order onto a47af373-equivalent canonical, preserving unrelated work, then rerun the composed gate. M1's extraction already carries the first two source corrections; later canonical approval consumer extraction needs7cd8e70. This accepts the bounded reader/approval identity correction, not all L1/L3/criterion lifecycle behavior.

Own KOD73 evidence: https://linear.app/duckburg/issue/KOD-73#comment-fa096e96-4ca9-41e0-9c51-7dbbbf8df79b
