# M1 scope extraction independent review

**Request changes for one inherited native-parent identity defect.** Extraction equivalence itself passes17 independently derived blob/AST checks at `18113978533fc1c0ed169bfc57ff3945084e48df`, parent `3c477a3489bd0bf555ae7b3a08569c2c5bf049be`. Five complete scope source/conformance modules are byte-identical to `d2c6fceab762191d4e40b23c8cd349ef476e4b12`. This is not an extraction-created identity regression.

Confirmed defect: LinearScopeReader.container_metadata follows a previously returned canonical initiative parent key but discards the actual identity returned by `_metadata(ancestor)`. A project records parent initiative-one; that parent's read responds with id foreign-initiative. The method returns the original project/parent metadata without refusal. This supplies unproven parent-chain evidence. A narrow fix should compare the returned ancestor wire ID with that already canonical ancestor key, preserving the initial opaque address resolver.

The initial root-equality negative is **superseded as an unjustified general oracle**, not a founder fork. ScopeRef says its key is opaque and the adapter resolves it. Live connected-tool evidence on2026-09-12: requesting actual issue UUID65211d3b-bb35-46f7-9ab4-a86577604bb7 returns idKOD-73 and the same uuid. An initial UUID→human-key normalization is valid. A separate actual-adapter positive reproduces that supported shape and preserves the whole canonical subtree. Do not impose root.issue_key == requested_ref.key or a global read_issue equality rule. The original negative remains frozen externally for audit, but must not ship as a failing acceptance oracle.

Unchanged original probe file hash `c09b28add49e8cfc38cdd4eaf015a44382c119f3a1453a63470fb29604e02caa`, retained at `m1-scope-independent-frozen.py`. It contains the superseded root negative, the valid native-parent negative, and3 positive controls. The native-parent function/assertions and3 controls remain unchanged. Positive controls prove exact credential/unanswered exception cause identity, no retry of an unsafe unanswered write (despite4 configured retries), actual Task.cancel propagation during pagination, no partial return, and a fresh successful next traversal.

Actual evidence:

- Original6-module selection at181: **241 passed10.98s**, m1-scope-independent-affected-1811397.log.
- New5-case file at181: **2 failed3 passed2.34s**, m1-scope-independent-probes-1811397.log. One failure is the later-superseded root oracle; only the canonical-parent failure is a required repair.
- The two original negative controls unchanged at exactd2: **2 failed3 deselected1.28s**, m1-scope-independent-donor-d2c6fce.log.
- Same original negatives at exactcurrent5ef89e25649e855b4fe4df7540123774f6d782a2: **2 failed3 deselected0.18s**, m1-scope-independent-current-5ef89e2.log. An extra be9717f run also reproduces them; it is not substituted for this exact5ef evidence.
- UUID normalization positive at181: **1 passed0.06s**, m1-scope-alias-calibration-1811397.log.
- Strict **169 source files clean**, m1-scope-independent-mypy-1811397.log; source + original scope-test Ruff clean, m1-scope-independent-ruff-1811397.log.
- Exact17 equivalence checks, m1-scope-independent-equivalence.json/log. Independent verifier is verify_m1_scope_extraction.py; it reads Git objects and does not execute root's generation scripts.

The equivalence verifier checks all five whole files, both new methods on protocol/real/fake implementations, both neutral errors and ScopeReadError. It reconstructs the actual renamed wire module from baseline executable AST plus only the donor milestone class/field; no later comment/document/history fields are imported. It proves the existing retry AST differs only by the two neutral cause-preserving outward raises, and TrackerIssue differs only by the milestone field. Imports/consumer changes were inspected separately. Source tests retain full list/detail identity, exhaustive pagination, current membership, cycles, configured missing URL and typed-refusal oracles. Fake and real adapters run the same scope contract cases.

Root reports full make check at181 has one stale raw-priority schema allowlist-path failure and3154passed16skip. That broader gate is root evidence, not this reviewer's run. The exact donor path migration is a separate corrective commit a2ee4c6 with whole83-case dispatcher control reported by root; no independent acceptance of that commit is implied here.

Eight lenses: SOLID—scope traversal and adapter boundary are cohesive, canonical ancestor validation missing; DRY—existing parser/ports/reader preserved; Hexagonal architecture—vendor shapes are beside adapter, shared ports remain neutral; KISS—one bounded native-parent check is sufficient; Typed agent calls instead of semantic heuristics—no agent calls/identity regex added; Official framework practices (version-matched)—Pydantic and cursor behavior preserved; Type safety—scope/neutral-failure boundary improves versusmain, extraction itself neutral, ancestor provenance still requires runtime validation; Repository hygiene—exact main/donor/candidate, original controls and diagnostics retained, no source changes during review.

Review role ends here. Root separately authorized a minimal canonical ancestor correction on a new isolated writer branch from8fc655d, with fresh independent review before canonical/M1 cherry integration. No whole L1 acceptance, no inferred approval or issue-state change, and no claim that the new extraction is ready to merge.

Own73 evidence: https://linear.app/duckburg/issue/KOD-73#comment-9e42f079-c55d-43be-b250-73de829e0598
