# M1 native read/configuration extraction — author handoff

Candidate fec7f28292975f0b4635a06ba84e4e35fdc3c2e8; base a4575cd11133c5e631fff454de8cbac682f5a30e. Isolated worktree `/private/tmp/kodezart-v03-m1-native-read-extraction`, branch `codex/v03-m1-native-read-extraction`. Full locked make check passed on this immutable candidate:3493passed16skipped597.09s, strict178 sourcefiles, Ruff/format363files. Log `m1-native-read-make-check.log`. This is a bounded author handoff, not independent approval or full L1 completion.

## Scope and provenance

The 28 changed files and every patch hunk/hash are indexed in `m1-native-read-provenance.json`; exact copied symbol locations are in `m1-native-read-initial-hunks.json`. Accepted canonical source pin is7892ca1a47adbcfd91fbcfb1e580dfc86f066a99. Extraction6346911 preserves the current methods, including two subsequently reproduced defects; it is not an independently acceptable standalone revision. Source-only f3c3dd3dbd8aa06ae4a97811d85db43c89f2b3c0 repairs blank classification membership before IO. Source-only fd19b9a validates the actual parent response against its canonical key or reported UUID. Final test-only fec7f28 preserves additional actual alias controls. Root alone integrates.

The material closure is TrackerIssue semantic issue_labels; declared OperationConfig mapping/validation/OWNED boot reconciliation; actual build_tracker mapping; narrow TrackerCriteriaReader plus strict planning/capability reads; required child labels/parent wire; CriterionReadError; adapter/fake/test configuration. No Organize owner/author, graph changes, Fire runtime, approval policy, classifier writer, evaluator or lifecycle authority is extracted. Existing ordinary read_issue compatibility remains unchanged.

## Findings and actual evidence

* The real build_tracker path at the M1 base cannot accept issue_labels at all: the unchanged production-constructor controls fail twice before the extraction. `m1-native-read-before-executed.log`:2 failures3.63s. The two earlier files named before/before-corrected are collection/async harness diagnostics, not red behavioral proof.
* Direct configured adapters with a blank/whitespace criterion mapping silently return empty on canonical7892. The original two controls fail there and pass after the narrow _classification_label reuse, before any backend IO.
* A substituted get_issue parent silently redirects family membership on canonical7892. The original foreign-parent control fails there. Additional UUID controls independently show one legitimate alias positive and three foreign/malformed UUID refusals missing on canonical7892 (3failed/1passed3.76s). `native-read-original-probe-digests.json` preserves all original external probe hashes.
* Corrected extraction original3 + alias4 + inherited scope UUID positive:8passed0.46s in `m1-native-read-identity-corrected.log`. Blank-only2passed0.59s in `m1-native-read-blank-corrected.log`.
* Existing read/configuration/constructor/boot/prompt selection:131passed2.61s, excluding the then-known three additional defects. `m1-native-read-focused-baseline-excluding-corrections.log`. Strict178 source files passed in `m1-native-read-mypy-first.log`.

Before-probe commands use `PYTHONPATH=.:src /Users/kodezart/.local/bin/uv run --locked pytest -c pyproject.toml -q <external original probe>` in the exact base/canonical tree. Corrected selected command: the same locked pytest invocation with tests/tracker/test_criterion_family_identity.py, external test_criterion_family_aliases.py, and tests/tracker/test_scope_alias_identity.py. Complete command: `PATH=/Users/kodezart/.local/bin:$PATH UV_LOCKED=1 make check`, log `m1-native-read-make-check.log`.

The first extraction focused diagnostics were not disguised: absent later DescriptionEditResult writer import caused collection failure; its entire parent-rewrite preservation writer test stays with the pending M4 edit_description dependency. Then8failed/126passed included the3 actual new defects and5 fixture/census mismatches. Fixture migration explicitly supplies issue_labels={} to existing constructors, uses existing fake zero-write/read collections, includes a real configured label in the all-category equality oracle, and extends the exact native/template consumer census. No assertion was relaxed to permit a wrong result.

## Eight lenses and type impact

1. SOLID: shared native read port is narrow; boot remains the existing owner of declared mapping reconciliation. Reader and writer authority are distinct.
2. DRY: canonical cursor pagination, adapter mapping validation, boot ensure and wire decoding are reused. The small addressed wire shares the attested identity check with the separately corrected canonical approval consumer.
3. Hexagonal: application callers see TrackerIssue/CriterionReadError and the narrow port; vendor UUID and MCP calls stay in adapters. Real constructor tests use only external MCP doubles.
4. KISS: one parent fetch, deterministic native identity/classification/parent checks, existing sorted membership; no heuristic criterion inference or alternate reader authority.
5. Typed agent calls: no agent call/schema/preset changes in this slice. SDK PermissionMode/ToolPreset migration remains separately owned.
6. Official matched framework: locked Pydantic2.12.5 typed UUID parsing is checked against its exact tagged official source; FastAPI0.135.1/LangGraph1.0.10 unchanged. See m1-native-read-framework-evidence.log.
7. Type safety: child labels and nullable parent remain required wire facts; reported UUID is validated rather than guessed from the display key; configuration rejects blank/duplicate labels. No Any/cast/ignore/model_copy is introduced. Unexpected RuntimeError and actual cancellation escape; declared tracker failures retain typed cause. Strict178 source gate passed in the final full verification.
8. Hygiene/oracles: all source/hunks/donor differences are indexed; original external probes are immutable and exercised against real adapter/constructor paths. No source/test changes occurred during running gates; fake-only contract tests are supplemental, not the native identity oracle.

## Dependencies and integration

Full M4 label/criterion/child-set artifact readers now have their native read prerequisite available for independent review. They remain M4 extraction, not an M2 dependency. M2 graph/admission authoring, M3 runtime, classification writers, SDK typed tools and any milestone-wide acceptance remain pending.

Do not cherry-pick fd19b9a alone onto the full canonical implementation: its existing read_fire_spec supplied-subject call must pass an explicitly normalized canonical key after original raw approval identity validation. Root approved that paired adapter-only correction, under a separate canonical worktree; its exact commits and before/after controls are supplied separately. The M1 extracted tree has no such M3 consumer and stays immutable.

Configured boot can create declared owned labels idempotently; validation alone refuses missing backend labels. This is the existing boot contract, not semantic issue-classification write authority. Native reads are observations and provide no transactional snapshot/fencing guarantee.

Owning issues: [KOD-73](https://linear.app/duckburg/issue/KOD-73) and [KOD-386](https://linear.app/duckburg/issue/KOD-386). Own bounded73 evidence comment recorded after the final gate; link added below. Requested Astra High/inherited; effective runtime metadata unverified.

## Final frozen return

Full M1 command exited0:3493passed16skipped597.09s. No files changed during or after that gate. Candidate `fec7f28292975f0b4635a06ba84e4e35fdc3c2e8` remains clean. Canonical corrective is separately frozen clean `b84d43ba44131c8d43d2f3689844cf928ca2276e`; its173 focused tests and strict3/Ruff6 passed. Details and actual commands: `native-read-address-corrective-review.md`.

After independent acceptance, maintained M1 can receive6346911→f3c3dd3→fd19b9a→fec7f28 as a single reviewed stack. Canonical receives only0aad6e6→a05c169→7cd8e70→b84d43b, preserving its existing source. These corrections are authored post-donor engineering fixes backed by actual red/green controls, not silently presented as unchanged donor extraction. Root alone performs integration and publication.

Own bounded author evidence: [https://linear.app/duckburg/issue/KOD-73/v03-l1-scope-input-scoperef-tracker-port-container-vocabulary#comment-9d4de14d-7647-4a6f-a487-2f266d4aa3c6](https://linear.app/duckburg/issue/KOD-73/v03-l1-scope-input-scoperef-tracker-port-container-vocabulary#comment-9d4de14d-7647-4a6f-a487-2f266d4aa3c6). No issue state changed.
