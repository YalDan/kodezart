FFFFFFFFFFFF..FFF                                                        [100%]
=================================== FAILURES ===================================
______ test_external_change_during_authority_wait_refuses[body-identity] _______

boundary = 'identity', change = 'body'
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x10b1775c0>

    @pytest.mark.parametrize("boundary", ["identity", "lease"])
    @pytest.mark.parametrize(
        "change", ["body", "missing", "key", "author", "reply", "created"]
    )
    async def test_external_change_during_authority_wait_refuses(
        boundary, change, monkeypatch
    ):
        server = fixture_server()
        tracker = linear_over_fake_mcp(server)
        original = await leased_comment(
            tracker, target=APPROVED_ISSUE, marker=MARKER, body="private original answer"
        )
        async with lease_for_comment(
            tracker, target=APPROVED_ISSUE, marker=MARKER
        ) as holder:
            actual_call = server.call_tool
            reads = 0
            mutated = False
    
            async def call(*, name, arguments):
                nonlocal reads, mutated
                result = await actual_call(name=name, arguments=arguments)
                if name == "list_comments":
                    reads += 1
                due = (
                    name == "get_user"
                    if boundary == "identity"
                    else (name == "list_comments" and reads == 2)
                )
                if due and not mutated:
                    mutated = True
                    row = next(c for c in server.comments if c.id == original.comment_key)
                    if change == "body":
                        row.body = f"{MARKER}\nconcurrent answer"
                    elif change == "missing":
                        server.comments.remove(row)
                    elif change == "key":
                        row.id = "replacement-native-key"
                    elif change == "author":
                        row.author = "another-principal"
                    elif change == "reply":
                        row.parent_id = "another-thread"
                    else:
                        row.created_at += timedelta(seconds=1)
                return result
    
            monkeypatch.setattr(server, "call_tool", call)
            writes = len(server.tool_calls("save_comment"))
>           with pytest.raises(Exception) as caught:
                 ^^^^^^^^^^^^^^^^^^^^^^^^
E           Failed: DID NOT RAISE <class 'Exception'>

tests/tracker/test_comment_expected.py:77: Failed
________ test_external_change_during_authority_wait_refuses[body-lease] ________

boundary = 'lease', change = 'body'
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x10b18bd70>

    @pytest.mark.parametrize("boundary", ["identity", "lease"])
    @pytest.mark.parametrize(
        "change", ["body", "missing", "key", "author", "reply", "created"]
    )
    async def test_external_change_during_authority_wait_refuses(
        boundary, change, monkeypatch
    ):
        server = fixture_server()
        tracker = linear_over_fake_mcp(server)
        original = await leased_comment(
            tracker, target=APPROVED_ISSUE, marker=MARKER, body="private original answer"
        )
        async with lease_for_comment(
            tracker, target=APPROVED_ISSUE, marker=MARKER
        ) as holder:
            actual_call = server.call_tool
            reads = 0
            mutated = False
    
            async def call(*, name, arguments):
                nonlocal reads, mutated
                result = await actual_call(name=name, arguments=arguments)
                if name == "list_comments":
                    reads += 1
                due = (
                    name == "get_user"
                    if boundary == "identity"
                    else (name == "list_comments" and reads == 2)
                )
                if due and not mutated:
                    mutated = True
                    row = next(c for c in server.comments if c.id == original.comment_key)
                    if change == "body":
                        row.body = f"{MARKER}\nconcurrent answer"
                    elif change == "missing":
                        server.comments.remove(row)
                    elif change == "key":
                        row.id = "replacement-native-key"
                    elif change == "author":
                        row.author = "another-principal"
                    elif change == "reply":
                        row.parent_id = "another-thread"
                    else:
                        row.created_at += timedelta(seconds=1)
                return result
    
            monkeypatch.setattr(server, "call_tool", call)
            writes = len(server.tool_calls("save_comment"))
>           with pytest.raises(Exception) as caught:
                 ^^^^^^^^^^^^^^^^^^^^^^^^
E           Failed: DID NOT RAISE <class 'Exception'>

tests/tracker/test_comment_expected.py:77: Failed
_____ test_external_change_during_authority_wait_refuses[missing-identity] _____

boundary = 'identity', change = 'missing'
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x10b18b200>

    @pytest.mark.parametrize("boundary", ["identity", "lease"])
    @pytest.mark.parametrize(
        "change", ["body", "missing", "key", "author", "reply", "created"]
    )
    async def test_external_change_during_authority_wait_refuses(
        boundary, change, monkeypatch
    ):
        server = fixture_server()
        tracker = linear_over_fake_mcp(server)
        original = await leased_comment(
            tracker, target=APPROVED_ISSUE, marker=MARKER, body="private original answer"
        )
        async with lease_for_comment(
            tracker, target=APPROVED_ISSUE, marker=MARKER
        ) as holder:
            actual_call = server.call_tool
            reads = 0
            mutated = False
    
            async def call(*, name, arguments):
                nonlocal reads, mutated
                result = await actual_call(name=name, arguments=arguments)
                if name == "list_comments":
                    reads += 1
                due = (
                    name == "get_user"
                    if boundary == "identity"
                    else (name == "list_comments" and reads == 2)
                )
                if due and not mutated:
                    mutated = True
                    row = next(c for c in server.comments if c.id == original.comment_key)
                    if change == "body":
                        row.body = f"{MARKER}\nconcurrent answer"
                    elif change == "missing":
                        server.comments.remove(row)
                    elif change == "key":
                        row.id = "replacement-native-key"
                    elif change == "author":
                        row.author = "another-principal"
                    elif change == "reply":
                        row.parent_id = "another-thread"
                    else:
                        row.created_at += timedelta(seconds=1)
                return result
    
            monkeypatch.setattr(server, "call_tool", call)
            writes = len(server.tool_calls("save_comment"))
            with pytest.raises(Exception) as caught:
                await amend(tracker, original, holder)
            assert mutated
>           assert type(caught.value).__name__ == "StaleCommentWriteError"
E           AssertionError: assert 'TrackerUnavailableError' == 'StaleCommentWriteError'
E             
E             - StaleCommentWriteError
E             + TrackerUnavailableError

tests/tracker/test_comment_expected.py:80: AssertionError
______ test_external_change_during_authority_wait_refuses[missing-lease] _______

boundary = 'lease', change = 'missing'
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x10b23f380>

    @pytest.mark.parametrize("boundary", ["identity", "lease"])
    @pytest.mark.parametrize(
        "change", ["body", "missing", "key", "author", "reply", "created"]
    )
    async def test_external_change_during_authority_wait_refuses(
        boundary, change, monkeypatch
    ):
        server = fixture_server()
        tracker = linear_over_fake_mcp(server)
        original = await leased_comment(
            tracker, target=APPROVED_ISSUE, marker=MARKER, body="private original answer"
        )
        async with lease_for_comment(
            tracker, target=APPROVED_ISSUE, marker=MARKER
        ) as holder:
            actual_call = server.call_tool
            reads = 0
            mutated = False
    
            async def call(*, name, arguments):
                nonlocal reads, mutated
                result = await actual_call(name=name, arguments=arguments)
                if name == "list_comments":
                    reads += 1
                due = (
                    name == "get_user"
                    if boundary == "identity"
                    else (name == "list_comments" and reads == 2)
                )
                if due and not mutated:
                    mutated = True
                    row = next(c for c in server.comments if c.id == original.comment_key)
                    if change == "body":
                        row.body = f"{MARKER}\nconcurrent answer"
                    elif change == "missing":
                        server.comments.remove(row)
                    elif change == "key":
                        row.id = "replacement-native-key"
                    elif change == "author":
                        row.author = "another-principal"
                    elif change == "reply":
                        row.parent_id = "another-thread"
                    else:
                        row.created_at += timedelta(seconds=1)
                return result
    
            monkeypatch.setattr(server, "call_tool", call)
            writes = len(server.tool_calls("save_comment"))
            with pytest.raises(Exception) as caught:
                await amend(tracker, original, holder)
            assert mutated
>           assert type(caught.value).__name__ == "StaleCommentWriteError"
E           AssertionError: assert 'TrackerUnavailableError' == 'StaleCommentWriteError'
E             
E             - StaleCommentWriteError
E             + TrackerUnavailableError

tests/tracker/test_comment_expected.py:80: AssertionError
_______ test_external_change_during_authority_wait_refuses[key-identity] _______

boundary = 'identity', change = 'key'
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x10b23e240>

    @pytest.mark.parametrize("boundary", ["identity", "lease"])
    @pytest.mark.parametrize(
        "change", ["body", "missing", "key", "author", "reply", "created"]
    )
    async def test_external_change_during_authority_wait_refuses(
        boundary, change, monkeypatch
    ):
        server = fixture_server()
        tracker = linear_over_fake_mcp(server)
        original = await leased_comment(
            tracker, target=APPROVED_ISSUE, marker=MARKER, body="private original answer"
        )
        async with lease_for_comment(
            tracker, target=APPROVED_ISSUE, marker=MARKER
        ) as holder:
            actual_call = server.call_tool
            reads = 0
            mutated = False
    
            async def call(*, name, arguments):
                nonlocal reads, mutated
                result = await actual_call(name=name, arguments=arguments)
                if name == "list_comments":
                    reads += 1
                due = (
                    name == "get_user"
                    if boundary == "identity"
                    else (name == "list_comments" and reads == 2)
                )
                if due and not mutated:
                    mutated = True
                    row = next(c for c in server.comments if c.id == original.comment_key)
                    if change == "body":
                        row.body = f"{MARKER}\nconcurrent answer"
                    elif change == "missing":
                        server.comments.remove(row)
                    elif change == "key":
                        row.id = "replacement-native-key"
                    elif change == "author":
                        row.author = "another-principal"
                    elif change == "reply":
                        row.parent_id = "another-thread"
                    else:
                        row.created_at += timedelta(seconds=1)
                return result
    
            monkeypatch.setattr(server, "call_tool", call)
            writes = len(server.tool_calls("save_comment"))
            with pytest.raises(Exception) as caught:
                await amend(tracker, original, holder)
            assert mutated
>           assert type(caught.value).__name__ == "StaleCommentWriteError"
E           AssertionError: assert 'TrackerUnavailableError' == 'StaleCommentWriteError'
E             
E             - StaleCommentWriteError
E             + TrackerUnavailableError

tests/tracker/test_comment_expected.py:80: AssertionError
________ test_external_change_during_authority_wait_refuses[key-lease] _________

boundary = 'lease', change = 'key'
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x10b259010>

    @pytest.mark.parametrize("boundary", ["identity", "lease"])
    @pytest.mark.parametrize(
        "change", ["body", "missing", "key", "author", "reply", "created"]
    )
    async def test_external_change_during_authority_wait_refuses(
        boundary, change, monkeypatch
    ):
        server = fixture_server()
        tracker = linear_over_fake_mcp(server)
        original = await leased_comment(
            tracker, target=APPROVED_ISSUE, marker=MARKER, body="private original answer"
        )
        async with lease_for_comment(
            tracker, target=APPROVED_ISSUE, marker=MARKER
        ) as holder:
            actual_call = server.call_tool
            reads = 0
            mutated = False
    
            async def call(*, name, arguments):
                nonlocal reads, mutated
                result = await actual_call(name=name, arguments=arguments)
                if name == "list_comments":
                    reads += 1
                due = (
                    name == "get_user"
                    if boundary == "identity"
                    else (name == "list_comments" and reads == 2)
                )
                if due and not mutated:
                    mutated = True
                    row = next(c for c in server.comments if c.id == original.comment_key)
                    if change == "body":
                        row.body = f"{MARKER}\nconcurrent answer"
                    elif change == "missing":
                        server.comments.remove(row)
                    elif change == "key":
                        row.id = "replacement-native-key"
                    elif change == "author":
                        row.author = "another-principal"
                    elif change == "reply":
                        row.parent_id = "another-thread"
                    else:
                        row.created_at += timedelta(seconds=1)
                return result
    
            monkeypatch.setattr(server, "call_tool", call)
            writes = len(server.tool_calls("save_comment"))
            with pytest.raises(Exception) as caught:
                await amend(tracker, original, holder)
            assert mutated
>           assert type(caught.value).__name__ == "StaleCommentWriteError"
E           AssertionError: assert 'TrackerUnavailableError' == 'StaleCommentWriteError'
E             
E             - StaleCommentWriteError
E             + TrackerUnavailableError

tests/tracker/test_comment_expected.py:80: AssertionError
_____ test_external_change_during_authority_wait_refuses[author-identity] ______

boundary = 'identity', change = 'author'
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x10b25aae0>

    @pytest.mark.parametrize("boundary", ["identity", "lease"])
    @pytest.mark.parametrize(
        "change", ["body", "missing", "key", "author", "reply", "created"]
    )
    async def test_external_change_during_authority_wait_refuses(
        boundary, change, monkeypatch
    ):
        server = fixture_server()
        tracker = linear_over_fake_mcp(server)
        original = await leased_comment(
            tracker, target=APPROVED_ISSUE, marker=MARKER, body="private original answer"
        )
        async with lease_for_comment(
            tracker, target=APPROVED_ISSUE, marker=MARKER
        ) as holder:
            actual_call = server.call_tool
            reads = 0
            mutated = False
    
            async def call(*, name, arguments):
                nonlocal reads, mutated
                result = await actual_call(name=name, arguments=arguments)
                if name == "list_comments":
                    reads += 1
                due = (
                    name == "get_user"
                    if boundary == "identity"
                    else (name == "list_comments" and reads == 2)
                )
                if due and not mutated:
                    mutated = True
                    row = next(c for c in server.comments if c.id == original.comment_key)
                    if change == "body":
                        row.body = f"{MARKER}\nconcurrent answer"
                    elif change == "missing":
                        server.comments.remove(row)
                    elif change == "key":
                        row.id = "replacement-native-key"
                    elif change == "author":
                        row.author = "another-principal"
                    elif change == "reply":
                        row.parent_id = "another-thread"
                    else:
                        row.created_at += timedelta(seconds=1)
                return result
    
            monkeypatch.setattr(server, "call_tool", call)
            writes = len(server.tool_calls("save_comment"))
>           with pytest.raises(Exception) as caught:
                 ^^^^^^^^^^^^^^^^^^^^^^^^
E           Failed: DID NOT RAISE <class 'Exception'>

tests/tracker/test_comment_expected.py:77: Failed
_______ test_external_change_during_authority_wait_refuses[author-lease] _______

boundary = 'lease', change = 'author'
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x10b25b410>

    @pytest.mark.parametrize("boundary", ["identity", "lease"])
    @pytest.mark.parametrize(
        "change", ["body", "missing", "key", "author", "reply", "created"]
    )
    async def test_external_change_during_authority_wait_refuses(
        boundary, change, monkeypatch
    ):
        server = fixture_server()
        tracker = linear_over_fake_mcp(server)
        original = await leased_comment(
            tracker, target=APPROVED_ISSUE, marker=MARKER, body="private original answer"
        )
        async with lease_for_comment(
            tracker, target=APPROVED_ISSUE, marker=MARKER
        ) as holder:
            actual_call = server.call_tool
            reads = 0
            mutated = False
    
            async def call(*, name, arguments):
                nonlocal reads, mutated
                result = await actual_call(name=name, arguments=arguments)
                if name == "list_comments":
                    reads += 1
                due = (
                    name == "get_user"
                    if boundary == "identity"
                    else (name == "list_comments" and reads == 2)
                )
                if due and not mutated:
                    mutated = True
                    row = next(c for c in server.comments if c.id == original.comment_key)
                    if change == "body":
                        row.body = f"{MARKER}\nconcurrent answer"
                    elif change == "missing":
                        server.comments.remove(row)
                    elif change == "key":
                        row.id = "replacement-native-key"
                    elif change == "author":
                        row.author = "another-principal"
                    elif change == "reply":
                        row.parent_id = "another-thread"
                    else:
                        row.created_at += timedelta(seconds=1)
                return result
    
            monkeypatch.setattr(server, "call_tool", call)
            writes = len(server.tool_calls("save_comment"))
>           with pytest.raises(Exception) as caught:
                 ^^^^^^^^^^^^^^^^^^^^^^^^
E           Failed: DID NOT RAISE <class 'Exception'>

tests/tracker/test_comment_expected.py:77: Failed
______ test_external_change_during_authority_wait_refuses[reply-identity] ______

boundary = 'identity', change = 'reply'
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x10b27deb0>

    @pytest.mark.parametrize("boundary", ["identity", "lease"])
    @pytest.mark.parametrize(
        "change", ["body", "missing", "key", "author", "reply", "created"]
    )
    async def test_external_change_during_authority_wait_refuses(
        boundary, change, monkeypatch
    ):
        server = fixture_server()
        tracker = linear_over_fake_mcp(server)
        original = await leased_comment(
            tracker, target=APPROVED_ISSUE, marker=MARKER, body="private original answer"
        )
        async with lease_for_comment(
            tracker, target=APPROVED_ISSUE, marker=MARKER
        ) as holder:
            actual_call = server.call_tool
            reads = 0
            mutated = False
    
            async def call(*, name, arguments):
                nonlocal reads, mutated
                result = await actual_call(name=name, arguments=arguments)
                if name == "list_comments":
                    reads += 1
                due = (
                    name == "get_user"
                    if boundary == "identity"
                    else (name == "list_comments" and reads == 2)
                )
                if due and not mutated:
                    mutated = True
                    row = next(c for c in server.comments if c.id == original.comment_key)
                    if change == "body":
                        row.body = f"{MARKER}\nconcurrent answer"
                    elif change == "missing":
                        server.comments.remove(row)
                    elif change == "key":
                        row.id = "replacement-native-key"
                    elif change == "author":
                        row.author = "another-principal"
                    elif change == "reply":
                        row.parent_id = "another-thread"
                    else:
                        row.created_at += timedelta(seconds=1)
                return result
    
            monkeypatch.setattr(server, "call_tool", call)
            writes = len(server.tool_calls("save_comment"))
>           with pytest.raises(Exception) as caught:
                 ^^^^^^^^^^^^^^^^^^^^^^^^
E           Failed: DID NOT RAISE <class 'Exception'>

tests/tracker/test_comment_expected.py:77: Failed
_______ test_external_change_during_authority_wait_refuses[reply-lease] ________

boundary = 'lease', change = 'reply'
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x10b27e570>

    @pytest.mark.parametrize("boundary", ["identity", "lease"])
    @pytest.mark.parametrize(
        "change", ["body", "missing", "key", "author", "reply", "created"]
    )
    async def test_external_change_during_authority_wait_refuses(
        boundary, change, monkeypatch
    ):
        server = fixture_server()
        tracker = linear_over_fake_mcp(server)
        original = await leased_comment(
            tracker, target=APPROVED_ISSUE, marker=MARKER, body="private original answer"
        )
        async with lease_for_comment(
            tracker, target=APPROVED_ISSUE, marker=MARKER
        ) as holder:
            actual_call = server.call_tool
            reads = 0
            mutated = False
    
            async def call(*, name, arguments):
                nonlocal reads, mutated
                result = await actual_call(name=name, arguments=arguments)
                if name == "list_comments":
                    reads += 1
                due = (
                    name == "get_user"
                    if boundary == "identity"
                    else (name == "list_comments" and reads == 2)
                )
                if due and not mutated:
                    mutated = True
                    row = next(c for c in server.comments if c.id == original.comment_key)
                    if change == "body":
                        row.body = f"{MARKER}\nconcurrent answer"
                    elif change == "missing":
                        server.comments.remove(row)
                    elif change == "key":
                        row.id = "replacement-native-key"
                    elif change == "author":
                        row.author = "another-principal"
                    elif change == "reply":
                        row.parent_id = "another-thread"
                    else:
                        row.created_at += timedelta(seconds=1)
                return result
    
            monkeypatch.setattr(server, "call_tool", call)
            writes = len(server.tool_calls("save_comment"))
>           with pytest.raises(Exception) as caught:
                 ^^^^^^^^^^^^^^^^^^^^^^^^
E           Failed: DID NOT RAISE <class 'Exception'>

tests/tracker/test_comment_expected.py:77: Failed
_____ test_external_change_during_authority_wait_refuses[created-identity] _____

boundary = 'identity', change = 'created'
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x10b2a2a20>

    @pytest.mark.parametrize("boundary", ["identity", "lease"])
    @pytest.mark.parametrize(
        "change", ["body", "missing", "key", "author", "reply", "created"]
    )
    async def test_external_change_during_authority_wait_refuses(
        boundary, change, monkeypatch
    ):
        server = fixture_server()
        tracker = linear_over_fake_mcp(server)
        original = await leased_comment(
            tracker, target=APPROVED_ISSUE, marker=MARKER, body="private original answer"
        )
        async with lease_for_comment(
            tracker, target=APPROVED_ISSUE, marker=MARKER
        ) as holder:
            actual_call = server.call_tool
            reads = 0
            mutated = False
    
            async def call(*, name, arguments):
                nonlocal reads, mutated
                result = await actual_call(name=name, arguments=arguments)
                if name == "list_comments":
                    reads += 1
                due = (
                    name == "get_user"
                    if boundary == "identity"
                    else (name == "list_comments" and reads == 2)
                )
                if due and not mutated:
                    mutated = True
                    row = next(c for c in server.comments if c.id == original.comment_key)
                    if change == "body":
                        row.body = f"{MARKER}\nconcurrent answer"
                    elif change == "missing":
                        server.comments.remove(row)
                    elif change == "key":
                        row.id = "replacement-native-key"
                    elif change == "author":
                        row.author = "another-principal"
                    elif change == "reply":
                        row.parent_id = "another-thread"
                    else:
                        row.created_at += timedelta(seconds=1)
                return result
    
            monkeypatch.setattr(server, "call_tool", call)
            writes = len(server.tool_calls("save_comment"))
>           with pytest.raises(Exception) as caught:
                 ^^^^^^^^^^^^^^^^^^^^^^^^
E           Failed: DID NOT RAISE <class 'Exception'>

tests/tracker/test_comment_expected.py:77: Failed
______ test_external_change_during_authority_wait_refuses[created-lease] _______

boundary = 'lease', change = 'created'
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x10b27f470>

    @pytest.mark.parametrize("boundary", ["identity", "lease"])
    @pytest.mark.parametrize(
        "change", ["body", "missing", "key", "author", "reply", "created"]
    )
    async def test_external_change_during_authority_wait_refuses(
        boundary, change, monkeypatch
    ):
        server = fixture_server()
        tracker = linear_over_fake_mcp(server)
        original = await leased_comment(
            tracker, target=APPROVED_ISSUE, marker=MARKER, body="private original answer"
        )
        async with lease_for_comment(
            tracker, target=APPROVED_ISSUE, marker=MARKER
        ) as holder:
            actual_call = server.call_tool
            reads = 0
            mutated = False
    
            async def call(*, name, arguments):
                nonlocal reads, mutated
                result = await actual_call(name=name, arguments=arguments)
                if name == "list_comments":
                    reads += 1
                due = (
                    name == "get_user"
                    if boundary == "identity"
                    else (name == "list_comments" and reads == 2)
                )
                if due and not mutated:
                    mutated = True
                    row = next(c for c in server.comments if c.id == original.comment_key)
                    if change == "body":
                        row.body = f"{MARKER}\nconcurrent answer"
                    elif change == "missing":
                        server.comments.remove(row)
                    elif change == "key":
                        row.id = "replacement-native-key"
                    elif change == "author":
                        row.author = "another-principal"
                    elif change == "reply":
                        row.parent_id = "another-thread"
                    else:
                        row.created_at += timedelta(seconds=1)
                return result
    
            monkeypatch.setattr(server, "call_tool", call)
            writes = len(server.tool_calls("save_comment"))
>           with pytest.raises(Exception) as caught:
                 ^^^^^^^^^^^^^^^^^^^^^^^^
E           Failed: DID NOT RAISE <class 'Exception'>

tests/tracker/test_comment_expected.py:77: Failed
_ test_matching_desired_body_does_not_authorize_a_replacement_identity[fake-port] _

tracker = <tests.fakes.FakeTrackerPort object at 0x10b27e330>
tracker_writes = <function tracker_writes.<locals>.<lambda> at 0x10b22ea20>

    async def test_matching_desired_body_does_not_authorize_a_replacement_identity(
        tracker, tracker_writes
    ):
        original = await leased_comment(
            tracker, target=APPROVED_ISSUE, marker=MARKER, body="authorized answer"
        )
        expected = original.model_copy(update={"comment_key": "previous-native-key"})
        async with lease_for_comment(
            tracker, target=APPROVED_ISSUE, marker=MARKER
        ) as holder:
            writes = tracker_writes()
>           with pytest.raises(Exception) as caught:
                 ^^^^^^^^^^^^^^^^^^^^^^^^
E           Failed: DID NOT RAISE <class 'Exception'>

tests/tracker/test_comment_expected.py:115: Failed
_ test_matching_desired_body_does_not_authorize_a_replacement_identity[linear-mcp] _

tracker = <kodezart.adapters.linear_mcp_tracker.LinearMcpTracker object at 0x10b259730>
tracker_writes = <function tracker_writes.<locals>.<lambda> at 0x10b22ef20>

    async def test_matching_desired_body_does_not_authorize_a_replacement_identity(
        tracker, tracker_writes
    ):
        original = await leased_comment(
            tracker, target=APPROVED_ISSUE, marker=MARKER, body="authorized answer"
        )
        expected = original.model_copy(update={"comment_key": "previous-native-key"})
        async with lease_for_comment(
            tracker, target=APPROVED_ISSUE, marker=MARKER
        ) as holder:
            writes = tracker_writes()
>           with pytest.raises(Exception) as caught:
                 ^^^^^^^^^^^^^^^^^^^^^^^^
E           Failed: DID NOT RAISE <class 'Exception'>

tests/tracker/test_comment_expected.py:115: Failed
_____ test_cancelled_final_read_preserves_cancellation_and_writes_nothing ______

fut = <coroutine object Event.wait at 0x10b239ee0>, timeout = 1

    async def wait_for(fut, timeout):
        """Wait for the single Future or coroutine to complete, with timeout.
    
        Coroutine will be wrapped in Task.
    
        Returns result of the Future or coroutine.  When a timeout occurs,
        it cancels the task and raises TimeoutError.  To avoid the task
        cancellation, wrap it in shield().
    
        If the wait is cancelled, the task is also cancelled.
    
        If the task suppresses the cancellation and returns a value instead,
        that value is returned.
    
        This function is a coroutine.
        """
        # The special case for timeout <= 0 is for the following case:
        #
        # async def test_waitfor():
        #     func_started = False
        #
        #     async def func():
        #         nonlocal func_started
        #         func_started = True
        #
        #     try:
        #         await asyncio.wait_for(func(), 0)
        #     except asyncio.TimeoutError:
        #         assert not func_started
        #     else:
        #         assert False
        #
        # asyncio.run(test_waitfor())
    
    
        if timeout is not None and timeout <= 0:
            fut = ensure_future(fut)
    
            if fut.done():
                return fut.result()
    
            await _cancel_and_wait(fut)
            try:
                return fut.result()
            except exceptions.CancelledError as exc:
                raise TimeoutError from exc
    
        async with timeouts.timeout(timeout):
>           return await fut
                   ^^^^^^^^^

/Users/kodezart/.local/share/uv/python/cpython-3.12.13-macos-aarch64-none/lib/python3.12/asyncio/tasks.py:520: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = <asyncio.locks.Event object at 0x10b258080 [unset]>

    async def wait(self):
        """Block until the internal flag is true.
    
        If the internal flag is true on entry, return True
        immediately.  Otherwise, block until another coroutine calls
        set() to set the flag to true, then return True.
        """
        if self._value:
            return True
    
        fut = self._get_loop().create_future()
        self._waiters.append(fut)
        try:
>           await fut
E           asyncio.exceptions.CancelledError

/Users/kodezart/.local/share/uv/python/cpython-3.12.13-macos-aarch64-none/lib/python3.12/asyncio/locks.py:212: CancelledError

The above exception was the direct cause of the following exception:

monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x10b258f80>

    async def test_cancelled_final_read_preserves_cancellation_and_writes_nothing(
        monkeypatch,
    ):
        server = fixture_server()
        tracker = linear_over_fake_mcp(server)
        original = await leased_comment(
            tracker, target=APPROVED_ISSUE, marker=MARKER, body="original"
        )
        async with lease_for_comment(
            tracker, target=APPROVED_ISSUE, marker=MARKER
        ) as holder:
            actual_call = server.call_tool
            arrived = asyncio.Event()
            reads = 0
    
            async def call(*, name, arguments):
                nonlocal reads
                if name == "list_comments":
                    reads += 1
                    if reads == 3:
                        arrived.set()
                        await asyncio.Event().wait()
                return await actual_call(name=name, arguments=arguments)
    
            monkeypatch.setattr(server, "call_tool", call)
            writes = len(server.tool_calls("save_comment"))
            task = asyncio.create_task(amend(tracker, original, holder))
            try:
>               await asyncio.wait_for(arrived.wait(), timeout=1)

tests/tracker/test_comment_expected.py:149: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
/Users/kodezart/.local/share/uv/python/cpython-3.12.13-macos-aarch64-none/lib/python3.12/asyncio/tasks.py:519: in wait_for
    async with timeouts.timeout(timeout):
               ^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = <Timeout [expired]>
exc_type = <class 'asyncio.exceptions.CancelledError'>
exc_val = CancelledError(), exc_tb = <traceback object at 0x10b1d41c0>

    async def __aexit__(
        self,
        exc_type: Optional[Type[BaseException]],
        exc_val: Optional[BaseException],
        exc_tb: Optional[TracebackType],
    ) -> Optional[bool]:
        assert self._state in (_State.ENTERED, _State.EXPIRING)
    
        if self._timeout_handler is not None:
            self._timeout_handler.cancel()
            self._timeout_handler = None
    
        if self._state is _State.EXPIRING:
            self._state = _State.EXPIRED
    
            if self._task.uncancel() <= self._cancelling and exc_type is exceptions.CancelledError:
                # Since there are no new cancel requests, we're
                # handling this.
>               raise TimeoutError from exc_val
E               TimeoutError

/Users/kodezart/.local/share/uv/python/cpython-3.12.13-macos-aarch64-none/lib/python3.12/asyncio/timeouts.py:115: TimeoutError
=========================== short test summary info ============================
FAILED tests/tracker/test_comment_expected.py::test_external_change_during_authority_wait_refuses[body-identity]
FAILED tests/tracker/test_comment_expected.py::test_external_change_during_authority_wait_refuses[body-lease]
FAILED tests/tracker/test_comment_expected.py::test_external_change_during_authority_wait_refuses[missing-identity]
FAILED tests/tracker/test_comment_expected.py::test_external_change_during_authority_wait_refuses[missing-lease]
FAILED tests/tracker/test_comment_expected.py::test_external_change_during_authority_wait_refuses[key-identity]
FAILED tests/tracker/test_comment_expected.py::test_external_change_during_authority_wait_refuses[key-lease]
FAILED tests/tracker/test_comment_expected.py::test_external_change_during_authority_wait_refuses[author-identity]
FAILED tests/tracker/test_comment_expected.py::test_external_change_during_authority_wait_refuses[author-lease]
FAILED tests/tracker/test_comment_expected.py::test_external_change_during_authority_wait_refuses[reply-identity]
FAILED tests/tracker/test_comment_expected.py::test_external_change_during_authority_wait_refuses[reply-lease]
FAILED tests/tracker/test_comment_expected.py::test_external_change_during_authority_wait_refuses[created-identity]
FAILED tests/tracker/test_comment_expected.py::test_external_change_during_authority_wait_refuses[created-lease]
FAILED tests/tracker/test_comment_expected.py::test_matching_desired_body_does_not_authorize_a_replacement_identity[fake-port]
FAILED tests/tracker/test_comment_expected.py::test_matching_desired_body_does_not_authorize_a_replacement_identity[linear-mcp]
FAILED tests/tracker/test_comment_expected.py::test_cancelled_final_read_preserves_cancellation_and_writes_nothing
15 failed, 2 passed in 1.82s
