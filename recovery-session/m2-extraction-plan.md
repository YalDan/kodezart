# M2 actual extraction map

Donor: `36083f83f42c03240ebb5861fe284da2c9f04180`. Preliminary comparison: classification candidate `25c32f603f9c9cb59852304743e653f70be309ff`. Actual implementation base must be the coordinator's integrated M4 ruling + classification tree. This is an extraction map, not acceptance evidence. No M2 source has yet been mutated.

## Ownership correction agreed with coordinator

M2 is the first real consumer of the existing pure `domain/topology.py` and `types/domain/topology.py`, plus any missing existing `domain.dispatch.blocker_keys` prerequisite. `domain.organize_graph.validate_graph_change` calls `plan_topology` for dependency-cycle validation. Extract these unchanged rather than clone their arithmetic. Actual planner/walker execution, composition, approval/current readiness remain M3. This supersedes an earlier file-level M3 assignment only for this pure dependency.

Criterion authoring consumes the exact existing `domain.fire_spec.criterion_check` and `criterion_field_bodies` parser + local constants/helpers and named error. Reuse the M4 partial module if its ruling-source closure supplies them; do not copy full native fire workflow/schema unions. Do not add a foundation PR.

## Finite source obligations

1. Restore actual Organize vertical path: `chains/organize.py`, `chains/organize_author.py`, `composition/organize.py`, `core/organize_settings.py`, `domain/criterion_creation.py`, `domain/organize.py`, `domain/organize_graph.py`, `services/organize_context.py`, `services/organize_owner.py`, `services/organize_tick.py`, `types/domain/organize.py`, `types/domain/organize_graph.py`, `types/domain/organize_owner.py`. Preserve current donor graph/split, owner reread, bound evidence and discriminated halt corrections; no placeholder/refusing-only owner tranche.
2. Shared tracker contracts and actual adapter/fake implementations for `read_issue_revision`, `read_scope_labels`, `execution_approved`, `project_milestones`, `update_issue_graph`, `read_split_children`, `create_split_if_absent`, `create_criterion_if_absent`, `read_issue_identity`, `edit_description`. These consume already-reviewed classification/escalation, lease and independent verifier primitives. Minimal graph/split helper closure: `_SplitCreation`, `_read_unchanged_graph`, `_update_issue_graph_once`, `_create_split_once`, `_unstarted_state_id`, identity collection/address validation helpers. Existing `LinearIssueIdentityCarrier` and `IssueIdentity` are required for durable split identities. Public `upsert_issue`, full native state/effect and audit bindings do not follow merely because they share the file.
3. Extend actual canonical `tracker_artifacts` supported set and exact `ISSUE_GRAPH`/`ISSUE_SPLIT_SET` arms, including strict native child reread AFTER identity read and addressed identity validation. Keep prior six M4 arms unchanged. Add required `SurfaceKind` values and native issue revision/identity contracts. Carry the source/readback adverse controls without timeout inflation.
4. Add only M2 operation fields `scope_labels`, `organize_mandates`, `organize_scopes`, `ScopeLabel`, `OrganizeScopeBinding`, their validators and `resolve_organize_mandates`; `AppConfig.organize`; narrow scope-label adapter/config wiring and prompt-variable consumers. Restore actual `organize_variables`, prompt namespace/key/role/`RaiseSite` entries, registered admission/proposal schemas. Do not import unrelated audit/event/scope-runtime configuration.
5. Compose actual scheduled owner through `composition/passes.py`, preflight and `main.py` workspace injection, using existing grooming cadence/report identity and `OrganizeTick`. Preserve subsequent configured scope execution after first typed halt; legacy remaining prompt rows remain exercised. Include exact remote-head primitive needed by tick without unrelated Git outcome rewrites.
6. Carry both shipped sets' four actual Organize role templates and applicable native writer fragment/manifest registrations, configuration examples/docs and exact source/schema/dispatch/surface censuses. All accepted M2 source, tests, prompts, config and docs remain in maintained M2; later milestone material is excluded by hunk.

## Validation corpus to preserve

Whole current Organize modules under `tests/chains/test_organize*`, `tests/domain/test_organize*`, `tests/types/test_organize_halt.py`, `tests/core/test_organize_settings.py`, `tests/integration/test_organize_scheduler.py`, `tests/integration/test_organize_tick_halt.py`, `tests/test_organize_portability.py`, and `tests/prompts/test_organize*`.

Actual native modules: `tests/tracker/test_organize_graph*`, `test_criterion_creation.py`, `test_body_revisions.py`, `test_issue_identity_boundary.py`, applicable exact issue/scope approval tests, graph/split portions of `test_artifact_native_fields.py` and unchanged split identity independent controls. Include original configured owner and MCP fault controls; fake implementation parity alone is insufficient.

Run locked Python3.12 through Makefile, first targeted actual boundary/config/prompt/schema controls, then required immutable `make check`. Inventory tests importing non-M2/native delivery helpers before migration; preserve their assertion semantics through the minimum actual collaborator closure. No source/test edits while tests run in a tree.

## Acceptance limits

Existing KOD74 comments distinguish bounded owner/scheduler/typed halt acceptance from complete lane acceptance. Donor now contains graph/split capabilities beyond some earlier comments; their actual controls must be retained and reviewed, not falsely labeled absent. Backend reads/grants are observed consistency, not transactional compare-and-swap or fencing. State/evaluator authority, M3 walking and M6 terminal aggregation are separate. Earliest real M2 PR tranche is the complete configured vertical path above, after independent review and gate evidence, stacked on the selected M4 head.

Linear references:
- https://linear.app/duckburg/issue/KOD-74#comment-eba027b8-8907-4c41-aaa8-5fde6520016b
- https://linear.app/duckburg/issue/KOD-74#comment-201c264e-7ee0-47e2-9617-a6689003a99d
- https://linear.app/duckburg/issue/KOD-74#comment-f227405a-5fb3-400a-83f1-2861991b9d5c
- https://linear.app/duckburg/issue/KOD-76#comment-7d22b9cc-88b6-4b74-a066-ea3755867b14
