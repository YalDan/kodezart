# M2 Organize independent review

Reviewer: `/root/native_final_review`. Role: independent reviewer only for M2;
no production authorship, no testing or mutation of the writer's tree. Root owns
integration, PR publication, milestone state, initiative and Notion updates.
Requested Astra ultra retained; effective model metadata is not independently
observable. This report will identify the exact frozen source reviewed.

## Contract and finite obligations

Read current KOD-74 and all current comments on KOD-74, KOD-750, KOD-751,
KOD-787 and KOD-788. Also read the exact current criteria KOD-340, KOD-358,
KOD-359, KOD-366, KOD-368 and KOD-369. The canonical engineering instructions
are at https://app.notion.com/p/3abf89e34d10812d9b3fd3551cc2208a.

The operational contract is one preapproval, three-phase Organize pass. It
rereads current bodies and graph/ruling context; independently assesses; routes
the typed three-state result; asks a read-only author for a typed proposal;
applies only authorized tracker writes; independently verifies the actual landed
surface; repairs within active configured bounds or stops with typed retained
evidence; and only marks phases after fresh full-scope verification. Criterion
children use parent plus exact Check identity and are prepared with empty
Evidence, never executed. Scope approval ends Organize and cannot be minted by
its machine labels.

Finite review obligations:

1. Three-state admission/refusal construction, live body and context digests,
   fresh sessions, full own/linked/criterion evidence, no author transcript reuse.
2. Current scope/gate/approval and source revision at actual writes, owned
   resources and cancellation, observed lease validity at retries; no claim of
   unavailable atomic backend fencing.
3. Body/criterion/split replay, stable exact identities, no lost existing
   evidence, graph source/peer closure and preservation of unrelated fields.
4. Full-scope dry verification, cumulative defect classes, actual configured
   bounds and retained typed halt/escalation evidence, no marker on failure.
5. Active scheduler/config composition, four named prompt roles and schema/
   consumer agreement, explicit approval without impersonating a human.
6. Donor/source hunk ownership and original test-oracle preservation, with
   migration fixture corrections distinguished from production fixes.
7. All eight architecture/type/framework/hygiene lenses, and clear remaining
   acceptance criteria and M3/M4 dependencies.

KOD-750's configured issue-state writer is preserved. KOD-751's older Option 1
approval-owner acceptance is explicitly reopened by the founder; KOD-787 and
KOD-788 still reserve approval-placement and composed lease ownership decisions.
This review does not select either fork. Pure topology and criterion parsing are
M2 prerequisites by current assignment; native WALK/FIRE remains M3. KOD-366's
dated durable-product parity is a separate evidence obligation; scripted native
adapter tests cannot establish that historical comparison.

## Initial source observations, before candidate freeze

The admission, author and owner use existing hexagonal ports and the existing
judge/workspace/lease/write-back mechanisms. Current source exposes closed
discriminated admission and proposal unions, explicit configured bounds, current
graph/ruling context, and breadth-first scope snapshots. The owner performs a
fresh full-scope verification including criterion bodies before phase markers.
The native tests include an original lost-create-response control that forbids
resending an uncertain criterion creation and checks subsequent identity replay.

Two bounded concerns are retained for exact-candidate validation:

* `create_criterion_if_absent` checks the child-set lease once, then calls generic
  `_call(save_issue)`. Its known-unsent retry repeats only `_send`; graph and
  split writers correctly repeat the complete precondition. A fresh independent
  expired-lease/live-control probe has been authored outside the source tree at
  `test_m2_criterion_retry_independent.py`; no result is claimed until executed.
* Both `organize_criteria_author` prompts say the owner can edit the addressed
  body and instruct unavailable graph/parent/priority/milestone/split proposals.
  The criteria role accepts only `CriteriaProposal`, and the unavailable union
  permits only `criterion_edit`. These are inherited donor prompt bytes, but
  source provenance does not establish prompt/schema/consumer agreement.

## Execution evidence observed from author logs

This is attribution, not independent execution or approval. Preserved first
selection: 197 passed / 116 failed in 27.42s. Second: 286 passed / 27 failed in
64.05s. Author traced the latter to two omitted destination-to-surface mapping
rows. Expanded attempts encountered collection imports outside this M2 slice;
the exact selected/excluded oracle map must be reviewed at freeze. Latest
`m2-fifth-actual-tests.log`: 687 passed in 77.08s. Latest static log: strict mypy
214 source files, Ruff and format 465 files green. These are not a full-repository
M2 gate, and earlier failures remain recorded rather than rewritten.

## Exact baseline and independent execution

Candidate `daae8df4c4e09499f0401c9406251ec88af9a8dd`, tree
`a0881f14bbf76515233bb94a9f2bded8a5fe3c14`, parent
`25c32f603f9c9cb59852304743e653f70be309ff`. Donor
`36083f83f42c03240ebb5861fe284da2c9f04180`. Independent detached worktree
`/private/tmp/kodezart-v03-m2-organize-independent-daae`, clean before and after
execution. Created with `git worktree add --detach` and installed with
`/Users/kodezart/.local/bin/uv sync --frozen --all-groups` (exit 0; sync log retained).
This candidate includes the M4 shared prerequisite files, before its separate
ancestry merge. Root owns final milestone allocation.

Actual commands, all in the detached worktree:

```text
.venv/bin/python -m pytest -c pyproject.toml -q /private/tmp/kodezart-recovery-session/test_m2_criterion_retry_independent.py
PYTHONPATH=src:. .venv/bin/python /private/tmp/kodezart-recovery-session/m2_prompt_contract_diagnostic.py
.venv/bin/python -m pytest -q tests/chains/test_organize_delivery_independent.py tests/chains/test_organize_native_independent.py tests/chains/test_organize_graph_owner.py tests/chains/test_organize_owner.py tests/integration/test_organize_scheduler.py tests/integration/test_organize_tick_halt.py tests/core/test_organize_settings.py tests/tracker/test_criterion_creation.py tests/tracker/test_organize_graph_retry_boundary.py tests/tracker/test_organize_graph_receipt_boundary.py tests/tracker/test_native_approval_aliases.py tests/tracker/test_body_revisions.py tests/types/test_organize_halt.py
```

* Independent native retry pair: **1 failed, 1 passed in 16.08s**, exit 1.
  `m2-independent-criterion-retry-daae.log` records a real native create on the
  second attempt after the original 300-second grant expired. Only the external
  MCP boundary and actual board clock change; the live retry control passes.
* Actual prompt/schema/owner diagnostic: exit 0 with the disagreement recorded,
  not a passing correctness oracle. `m2-independent-prompt-contract-daae.log`
  shows a criteria prompt advertising body edit, supplied schema admitting
  BodyProposal, and the actual owner refusing that response with no children.
  Six graph/split unavailable capability values fail typed validation.
* Selected original controls: **161 passed in 60.16s**, exit 0, in
  `m2-independent-selected-daae.log`. This includes actual owner and native
  adapter boundaries, configured scheduler/lifespan, nondefault bounds, fresh
  remote base, authorization/stale source, existing criterion replay and
  unknown-response preservation, graph retries and readback, typed halt evidence,
  and exact per-surface body revisions. No full gate was duplicated.

All four local execution sessions (sync, provenance, probe, selected controls)
and the prompt diagnostic have completed; no child test runner is left active.

## Findings on the exact baseline

**P1 — known-unsent criterion retry can issue under an expired grant.**
`LinearMcpTracker.create_criterion_if_absent` at lines 3713–3763 checks its grant
at line 3748 then invokes generic `_call(save_issue)`. Retry repeats only `_send`.
The first attempt can be known unsent, the real clock can pass its 300-second
grant, and a second create is still sent. This differs from both an uncertain
receipt (correctly not resent by the existing test) and a write already issued
before expiry (documented backend limitation). Reuse the existing complete
graph/split retry-attempt pattern and keep post-receipt readback outside resend.
No new ownership abstraction or atomic backend guarantee is requested.

**P2 — criteria prompt advertises proposals the typed role cannot handle.**
Both prompt sets' `organize_criteria_author.md` lines 19–25 advertise body editing
and unavailable graph/split variants, while the role check in
`OrganizeOwner._author_write` requires CriteriaProposal and the unavailable
capability literal permits only `criterion_edit`. Actual diagnostic execution
confirms the advertised body response is schema-valid and then refused. Correct
the role text to match the existing capability; do not grow the handler or turn
an unavailable operation into an invented human fork.

Independent KOD-74 changes-requested comment:
https://linear.app/duckburg/issue/KOD-74#comment-cc8a848c-74cc-48d6-b4aa-8da4f46cb39f

## Source and oracle provenance

`m2_independent_provenance.py` reads immutable Git objects. Its complete output
is `m2-independent-provenance-daae.json` with a condensed execution log beside it.
The exact command used the reviewer-owned M4 environment's Python interpreter,
with repository/candidate/base/donor/output arguments stated above. It reports
121 changed files and 292 added test functions; these numbers include shared M4
prerequisites present before ancestry composition.

Admission, author, owner, context, graph, criterion creation, typed admission/
proposal/graph contracts and all eight role prompt files retain donor bytes.
Pure criterion parsing retains exact donor functions and omits the later native
FIRE capture/entry/criterion-amendment consumers. Pure topology retains donor
source. `build_organize_tick` has one intentional configuration seam migration:
`config.git.remote` becomes the actual maintained `config.git_remote`. It does
not introduce a fallback or choose a new remote.

Every baseline test assertion is retained. Two original test functions differ
only by explicit `scope_labels={}` constructor input after the required typed
boundary was restored; other constructor fixtures received the same binding.
The added native admission cancellation function differs from donor only by
explicit committer name/email required by the maintained workspace provider.
Two approval alias tests retain their original bodies/assertions with only the
`spec` parameter arm excluded from M2; that actual native FIRE entry arm belongs
to M3 and must be restored there. The M4 exact native-byte/symlink test was moved
from the donor's larger M6 module, and its new runtime comment-reader control was
already independently accepted in the M4 correction. Imported scheduler helper
removes only the later `criteria_stage_label_key` constructor argument; original
test functions remain exact. The discarded wholesale public-upsert suite is a
later M3 consumer, not an executed M2 assertion deleted to turn a run green.

The initially omitted title/description destination map rows are exact donor
entries and their restoration admits the real production gate flow exercised
by the selected tests; it does not bypass or weaken that gate. Both current
findings are inherited donor defects and remain defects despite byte provenance.

## Eight lenses and type impact

| Lens | Current assessment |
| --- | --- |
| SOLID | One owner orchestrates the phase sequence; admission/author remain read-only and tracker writes stay in the adapter. No new semantic judgment branch in adapters. Criterion retry is the concrete ownership violation to correct. |
| DRY | Reuses the existing judge, owned workspace/task, surface lease, escalation and write-back mechanisms. A single phase-role lookup and one gap call site remain. Fix the retry by reusing the established attempt pattern. |
| Hexagonal | Typed tracker/workspace/git/runner/gate ports isolate core orchestration; native identity and MCP shape checks remain adapter concerns. No new direct tracker write in the agent sessions. |
| KISS | Typed local proposals and configured bounded loops use the existing scheduler. Pure topology/parser extraction does not introduce native WALK/FIRE execution. No new framework is needed for either finding. |
| Typed agent calls | Fresh ORGANIZE_PASS calls use validated discriminated admission/proposal schemas and read-only evaluation tools. The criteria prompt/schema/consumer contradiction is the specific regression to correct. |
| Official framework practice | Uses actual installed Pydantic validation, existing AgentService runner/session ownership and async context-manager patterns. No new LangGraph API, checkpoint, private SDK compatibility layer or framework reimplementation is introduced by this slice. |
| Type safety | Improves over the prior generic grooming pass with frozen typed scope/graph/admission/proposal/halt/bound contracts; preserves three-state refusal and cited write-back evidence. Required scope_labels input and optional subsystem settings are explicit; no new Any or suppression is required. |
| Hygiene | Source/oracles are frozen and independently compared; prior failures preserved. No full gate, live CLI, historical parity, or atomic backend fencing claim. Two bounded fixes are required before acceptance. |

Type impact is **improvement** over base for the active Organize pipeline and
**neutral** against donor for the extracted typed contracts. The two findings are
behavioral/prompt-contract defects, not a rationale to weaken unions or add Any.

## Remaining criteria and dependencies

This executable selection supports current admission/routing/liveness/marker/
bounded-owner/graph/criterion behavior, not automatic closure of every KOD-74
criterion. KOD-366 still requires the dated durable board-product comparison;
KOD-358's typed boot refusal for an adapter lacking digest capability needs its
own recorded proof; KOD-369's all-write gate census must distinguish authored
content from fixed metadata without silently changing its requirement. Existing
criterion-body edit is explicitly unavailable and milestone clearing is refused
when the native schema cannot express it. Those limits must remain visible in
the M2 completion description. Native WALK/FIRE and completion grading remain
M3. Root's M4 shared prerequisite ancestry and final M1/M2 ownership allocation
must be retained once each, then the maintained integrated gate must run.

## Disposition

**Changes requested on daae8df.** The expired retry and prompt contradiction must
be corrected and independently rechecked on a new clean commit. Preserve this
baseline/probe/log and the original oracles. Do not integrate this baseline as
accepted; root will integrate only the reviewed correction and exact prerequisite
ancestry. The previous Native author assignment does not provide M2 approval.

## Corrected candidate — final bounded acceptance

**ACCEPT** exact `a15b33484bc0f88975c910c1706edf20805d98f8`, tree
`c756178d9dc8cb4fd80c9b0eaa6b69343c51590b`, parent
`73cc5def12a2daa4e0ba319660db76cdad781882`. Independent clean detached worktree:
`/private/tmp/kodezart-v03-m2-organize-independent-a15`. This supersedes the two
proved findings above only. It does not rewrite the failing baseline or claim
blanket completion of KOD-74's criteria or the reserved ownership decisions.

The five-file correction uses a frozen private `_CriterionCreation` receipt to
distinguish an existing child replay from a completed new save. The existing
`_retry_call` now invokes the complete original criterion preconditions and fresh
child-set lease check, ending with one `_send`. Subsequent readback occurs after
that retry scope. A known-unsent retry can therefore no longer reuse an expired
grant; the existing returned child remains unchanged. The two role prompts now
accurately require criteria proposals, name `criterion_edit` as the sole
unavailable capability, and reserve unresolved output for actual human choices.
No handler, schema, grant authority or semantic capability was expanded.

Fresh command on the exact correction:

```text
.venv/bin/python -m pytest -c pyproject.toml -q /private/tmp/kodezart-recovery-session/test_m2_criterion_retry_independent.py tests/tracker/test_criterion_creation.py tests/tracker/test_criterion_receipt_boundary.py tests/chains/test_organize_owner.py::test_actual_factory_runs_all_configured_phases_and_reentry_writes_nothing tests/chains/test_organize_owner.py::test_missing_criterion_edit_capability_is_not_invented_as_a_human_fork tests/prompts/test_organize_roles.py tests/core/test_organize_settings.py::test_actual_tick_uses_fresh_remote_trunk_and_existing_run_identity
```

**44 passed in 7.00s**, exit 0; log `m2-independent-correction-a15.log`. The original
external probe is unchanged (SHA-256
`10a31bd94dafc20a8b3ff45cacc5256c25df0e8ac55668be0b0e40ea40cedbe5`) and byte-identical
to the committed probe. Its old result remains 1 fail/1 pass; both arms pass in
this fresh 44-case selection. Original criterion unknown-response/no-resend,
no-overwrite replay, malformed shape, foreign/missing grant and native receipt
controls remain intact and pass.

The exact original prompt diagnostic was rerun unchanged:

```text
PYTHONPATH=src:. .venv/bin/python /private/tmp/kodezart-recovery-session/m2_prompt_contract_diagnostic.py
```

Exit 0; log `m2-independent-prompt-contract-a15.log`. Its injected body response
is still rejected by the unchanged owner and its six unsupported capability
values remain rejected by the unchanged schema, while the actual criteria prompt
now records `advertises_body_edit: false`. The removed advertisement is the fix;
no new runtime capability is hidden behind it. Both prompt files were read
directly, and role boot/render tests passed above.

Immutable `m2-independent-provenance-a15.json` confirms exactly five changed
files against parent 73cc, zero changed/removed baseline test functions and two
added test functions. `git diff --check` and `cmp` of the original probe passed.
Original owner, admission/author, proposal types, criterion tests and owner tests
are byte-unchanged from daae to a15. Intervening
`8c3ccf5d3d8280d49d91640e23480dbcd355d9f3` / 73cc ancestry composes the root's
accepted M4 prerequisites and deduplicates its settings field; its Git settings
seam is exercised by the fresh remote tick control. This review does not replace
the root's M4 assessment or integrated full gate.

All eight lenses above now support this bounded extraction: ownership/retry and
typed prompt consistency concerns are corrected using existing mechanisms. Type
impact remains improvement over the old generic grooming route, neutral for
donor contracts. The private receipt improves the distinction between existing
replay and post-save readback without Any, suppression or public API growth.
Author-only verification stays separately attributed: 129 passing in 8.85s plus
1 receipt test in 0.18s, strict mypy 215 source files and Ruff/format 470 files.
No independent full-gate result is claimed.

Integration directions: preserve the daae/8c3ccf5/73cc/a15 chain and accepted M4
prerequisites, allocating shared scope/config/bootstrap hunks once under the
root's M1/M2 ownership map. If composing onto a different maintained M2 branch,
apply exact a15 once and preserve its original probe and receipt test; inspect
conflicts rather than force/resetting any donor. Run the root's maintained
integrated gate before publishing its M2 PR. Retain the earlier criteria and
dependency limits. Root alone owns integration/PR/initiative/Notion actions.

Both detached review trees remain clean. All execution sessions are settled.

## Fixture-only follow-on

**ACCEPT** `4ecd7c72d155fb9acedc832c32ee016db7ef6749`, tree
`27e691f7316bd4be0bd4fcddaa248a4d4d74bac5`, parent a15. Exactly five added lines
in `tests/core/test_git_settings.py` and `tests/services/test_prompt_passes.py`
supply explicit `FakeWorkspaceProvider()` and `organize=None` to the production
composition functions whose required signatures changed. This supplies actual
collaborators without defaulting production capabilities or changing assertions.

Independently compared immutable Git ASTs: 29 + 66 = **95 original
assert/pytest.raises/pytest.fail oracles exactly unchanged**. Proof is
`m2-independent-fixture-proof-4ecd.json`. The author's fresh
`m2-existing-composition-fixtures-corrected.log` records **69 passed in 19.62s**;
this read-only fixture review did not duplicate that suite. No source changes in
this follow-on. Preserve this commit once alongside a15 when composing M2.

Own final acceptance comment, explicitly superseding only the two proved fixes:
https://linear.app/duckburg/issue/KOD-74#comment-d5557112-901c-486f-bfbe-b483d142d91a
