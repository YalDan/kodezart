[
  "A new private async instance method `_run_quality_gate` is defined on class `RalphWorkflowEngine` in `src/kodezart/chains/ralph_workflow.py`. It is a regular instance method — NOT decorated with `@staticmethod`, `@classmethod`, `@property`, `@cached_property`, or any other decorator. Verify: `grep -B1 'async def _run_quality_gate' src/kodezart/chains/ralph_workflow.py` shows no decorator on the preceding line.",
  "The helper's signature is exactly `async def _run_quality_gate(self, *, prompt: str, repo_path: str | None, repo_url: str | None, feature_branch: str, ralph_branch: str, base_branch: str, permission_mode: str, allowed_tools: list[str], acceptance_criteria: list[str], cache_key: str) -> WorkflowIterationEvent:` — keyword-only after `self` (the `*` separator is present), 10 kwargs in the order/names/types of the `QualityGate.run` protocol at `src/kodezart/core/protocols.py:416–434`, concrete return type `WorkflowIterationEvent` (NOT a tuple, NOT `WorkflowIterationEvent | None`, NOT a dict, NOT a new wrapper class).",
  "The helper's docstring is exactly the single line `\"\"\"Delegate to the quality gate for iterative execution.\"\"\"` — verbatim match against the caller's existing docstring style. Verify: `grep -A1 'async def _run_quality_gate' src/kodezart/chains/ralph_workflow.py` shows that single line and nothing more on the docstring line.",
  "The helper is placed lexically immediately above `_run_ralph_loop_node` in the file, with no other `def`/`async def` declaration between them. Verify: `grep -n '^    \\(async \\)\\?def ' src/kodezart/chains/ralph_workflow.py` shows `_run_quality_gate` immediately preceding `_run_ralph_loop_node` in the listing.",
  "The helper body acquires the stream writer inline as the first executable statement: `writer = get_stream_writer()`. No `writer` parameter is accepted; no `StreamWriter` type is imported or annotated. The 8-call-site inline convention at lines 370, 399, 544, 647, 742, 848, 937, 1037 is preserved; the writer that moved out of `_run_ralph_loop_node` lands here.",
  "The helper body initializes `last_iteration_event: WorkflowIterationEvent | None = None`, then `async for event in self._quality_gate.run(...)` forwarding all 10 kwargs verbatim with `kwarg_name=kwarg_name` spelling (e.g., `prompt=prompt`, `repo_path=repo_path`, …, `cache_key=cache_key`). Inside the loop body it calls `writer(event)` unconditionally, then conditionally captures via `if isinstance(event, WorkflowIterationEvent): last_iteration_event = event`.",
  "If `last_iteration_event` remains `None` after the loop, the helper raises `RuntimeError` with the byte-identical message string `\"Ralph loop completed without emitting an iteration event.\"` constructed via the file-local two-line idiom `msg = \"…\"; raise RuntimeError(msg)`. Otherwise it returns `last_iteration_event` directly.",
  "The helper body contains zero `try`, `except`, `finally`, `logging`, `logger.`, `print(`, retry loop, `asyncio.sleep`, `time.sleep`, telemetry/metrics call, or suppression directive (`# noqa`, `# type: ignore`, `# ruff: noqa`, `# pragma: no cover`). Verify: `grep -E 'try:|except |finally:|logging|logger\\.|print\\(|asyncio\\.sleep|time\\.sleep|# noqa|# type: ignore|# ruff: noqa|# pragma: no cover' ` against the helper body line range returns zero matches.",
  "The helper body contains zero references to `state[...]`, no `ticket`-related identifier, no `ExecutionContext`, no `RunnableConfig`, no `format_ticket_as_task`, no `ctx.` access — all per-call inputs arrive through the 10 keyword parameters; no boundary-precondition logic is buried inside the helper.",
  "`_run_ralph_loop_node`'s signature is preserved byte-identically: `async def _run_ralph_loop_node(self, state: WorkflowState, config: RunnableConfig) -> dict[str, object]:` with the existing docstring `\"\"\"Delegate to the quality gate for iterative execution.\"\"\"`. Its LangGraph binding at lines 225–229 (`graph.add_node(\"run_ralph_loop\", self._run_ralph_loop_node, retry_policy=self._retry)`) is unchanged — `_build_graph` (lines 204–319) is byte-identical in the diff.",
  "`_run_ralph_loop_node` retains `ctx = ExecutionContext.from_configurable(config)` as its first executable statement (the LangGraph-context decoding stays at the node boundary).",
  "`_run_ralph_loop_node` removes the `writer = get_stream_writer()` line that was at pre-refactor line 455 — the caller no longer references `get_stream_writer` or `writer` anywhere in its body.",
  "`_run_ralph_loop_node` retains the `state[\"ticket\"] is None` precondition with the byte-identical error string `\"run_ralph_loop requires a ticket but state['ticket'] is None.\"`, raised via the same `msg = \"…\"; raise RuntimeError(msg)` two-line idiom. The precondition stays at the boundary; it is NOT moved into the helper.",
  "`_run_ralph_loop_node` invokes the helper exactly once as `last_iteration_event = await self._run_quality_gate(prompt=format_ticket_as_task(ticket), repo_path=ctx.repo_path, repo_url=ctx.repo_url, feature_branch=state[\"feature_branch\"], ralph_branch=state[\"ralph_branch\"], base_branch=ctx.base_branch, permission_mode=ctx.permission_mode, allowed_tools=ctx.allowed_tools, acceptance_criteria=state[\"acceptance_criteria\"], cache_key=ctx.cache_key)` — every kwarg-value expression literally matches one of the pre-refactor kwarg values at lines 464–473.",
  "`_run_ralph_loop_node`'s return statement is `return {\"accepted\": last_iteration_event.accepted, \"total_iterations\": last_iteration_event.iteration, \"feature_tip_sha\": None}`. The two-line inline comment `# feature_tip_sha is left None here; _merge_to_feature_node sets it` / `# from the merger's outcome.feature_tip_sha (canonical, post-push).` is preserved verbatim immediately above the return. `accepted` is sourced from `last_iteration_event.accepted` (no hardcoded `True`/`False`); `total_iterations` is sourced from `last_iteration_event.iteration`.",
  "`git diff src/kodezart/chains/ralph_workflow.py | grep -E '^\\+from |^\\+import '` returns zero lines — no new imports are introduced. `git diff src/kodezart/chains/ralph_workflow.py | grep -E '^\\-from |^\\-import '` likewise returns zero lines — no existing import is removed.",
  "`git grep -n '_run_quality_gate' src/kodezart/chains/ralph_workflow.py` returns exactly 2 hits: the `async def _run_quality_gate` definition line and the single `await self._run_quality_gate(` call site inside `_run_ralph_loop_node`. `git grep -rn '_run_quality_gate' src/` returns exactly those same 2 hits — no second caller exists anywhere in `src/`. `git grep -rn '_run_quality_gate' tests/` returns zero hits (no test reaches into the private helper by name).",
  "[sherlock] `git grep -nE '\\b_quality_gate\\b' src/kodezart/chains/ralph_workflow.py` returns exactly 3 hits: the constructor assignment at line 91 (`self._quality_gate: QualityGate = quality_gate`), the call site inside the helper (`self._quality_gate.run(`), and zero others. The new method name `_run_quality_gate` and the attribute name `_quality_gate` are word-boundary disjoint under grep — they do not collide.",
  "[sherlock] The `writer = get_stream_writer()` line count across the file is unchanged at exactly 9 (matches the count established at pre-refactor lines 370, 399, 455, 544, 647, 742, 848, 937, 1037). Net effect: −1 from the removal in the caller, +1 from the addition in the helper.",
  "[sherlock] The error string `\"Ralph loop completed without emitting an iteration event.\"` appears exactly once in `src/kodezart/chains/ralph_workflow.py` post-refactor (relocated from the caller into the helper, never duplicated). The error string `\"run_ralph_loop requires a ticket but state['ticket'] is None.\"` likewise appears exactly once (stays in the caller).",
  "`git diff` shows zero changes to: `src/kodezart/core/protocols.py` (the `QualityGate` protocol at lines 416–434 is unchanged); `src/kodezart/types/domain/workflow.py` (WorkflowContext / ExecutionContext / RalphLoopContext / WorkflowState are unchanged); `src/kodezart/types/domain/agent.py` (WorkflowIterationEvent and the SSE event vocabulary are unchanged); `src/kodezart/chains/ralph_loop.py` (the lines 84–95 site is untouched).",
  "`git diff` shows zero changes to: `tests/fakes.py` (FakeQualityGate at lines 821–873 is byte-identical — its 10-kwarg `run(...)` signature still matches the unchanged protocol); `tests/chains/test_ralph_workflow.py` (no test is edited; no new test is added). The helper is exercised transitively through `engine.run(...)`.",
  "`git diff` shows zero changes to: `Makefile`, `pyproject.toml`, `.github/workflows/*`, `.mypy.ini`, `.ruff.toml`, or any other build/CI/configuration file. The diff satisfies the existing tool configuration as-is, with no rule loosening.",
  "Zero new module/file is created. `git status --porcelain` shows only `M  src/kodezart/chains/ralph_workflow.py` and zero `A`/`??` entries under `src/` or `tests/`. The helper lives on the existing class in the existing file.",
  "Zero re-export shim, backwards-compat alias, or module-level alias is introduced. Verify: `grep -nE '^_run_quality_gate|^run_quality_gate|^_run_ralph_loop_node|^run_ralph_loop_node' src/kodezart/chains/ralph_workflow.py` returns no matches; no `__all__` change; no `from .ralph_workflow import _run_quality_gate` anywhere in `src/`.",
  "Zero new abstraction is introduced: no new class, no new Protocol, no new Pydantic model, no new TypedDict, no new dataclass, no new NamedTuple, no `RalphLoopContext` adoption at the helper boundary, no `StreamWriter` import. Verify: `grep -nE '^class |^@dataclass|RalphLoopContext|StreamWriter' src/kodezart/chains/ralph_workflow.py` shows only the unchanged `class RalphWorkflowEngine` line and no other matches in the diff.",
  "Zero `Any` annotation appears in the helper signature or body. `disallow_any_explicit = true` (pyproject.toml:39–48) is satisfied. The 10 kwargs use only concrete types (`str`, `str | None`, `list[str]`). No `**kwargs: object` catch-all; no `cast(...)`; no `typing.Any` import (and no need for one).",
  "Zero `# noqa`, `# type: ignore`, `# ruff: noqa`, `# pragma: no cover`, `# pylint: disable`, or other suppression directive is added in the diff. Verify: `git diff src/kodezart/chains/ralph_workflow.py | grep -E '^\\+.*#\\s*(noqa|type:\\s*ignore|ruff:\\s*noqa|pragma:\\s*no cover|pylint)'` returns zero lines.",
  "Zero test double appears in the production diff: no `FakeQualityGate`, no `MagicMock`, no `Mock(`, no `patch(`, no `cast(`, no sentinel test string in the helper or caller. Verify: `git diff src/kodezart/chains/ralph_workflow.py | grep -E 'Fake|Mock|patch\\(|cast\\(|sentinel'` returns zero lines.",
  "Out-of-scope nodes are byte-identical: `_generate_branch_node` (323–360), `_generate_ticket_node` (362–388), `_generate_criteria_node` (390–446), `_persist_artifacts_node` (491–527), `_merge_to_feature_node` (531–611), `_resolve_cwd` (619–626), `_review_against_ticket_node` (628–702), `_fix_code_node` (728–825), `_open_pr_node` (841–928), `_monitor_ci_node` (930–963), `_comment_failure_node` (982–1028), `_complete_node` (1030–1075), `run` (116–200), `__init__` (71–114), and all `_route_*` functions. The diff is confined to (a) the new helper insertion above line 448 and (b) the body of `_run_ralph_loop_node`.",
  "`uv run mypy src/kodezart/chains/ralph_workflow.py` exits with code 0 and reports zero new errors under the project's strict configuration (`strict = true`, `disallow_any_explicit = true`).",
  "`uv run ruff check src/kodezart/chains/ralph_workflow.py` exits with code 0 and reports zero violations under the active rule set (includes `ANN`, `B`, `N`, `UP`, `I`, `RUF`). In particular: ANN401 does not fire (no `Any`-wrapping type alias annotated), N802 does not fire (snake_case `_run_quality_gate`), I001 does not fire (imports unchanged).",
  "`uv run ruff format --check src/kodezart/chains/ralph_workflow.py` exits with code 0 (file is already correctly formatted; no formatter rewrites pending).",
  "`make verify-no-origin-literal` exits with code 0 — no new `\"origin\"`/`\"origin/\"` literal is introduced outside `src/kodezart/core/config.py`.",
  "`make test` exits with code 0. In particular `uv run pytest tests/chains/test_ralph_workflow.py::test_workflow_review_fails_triggers_fix` (lines 1080–1147), `uv run pytest tests/chains/test_ralph_workflow.py::test_fix_code_node_divergent_routes_to_comment_failure_when_pr_url_set` (lines 2396–2452), and `uv run pytest tests/chains/test_ralph_workflow.py::test_fix_code_node_divergent_routes_to_complete_when_no_pr_url` (lines 2455–2499) all PASS without modification, asserting the SSE event sequence emitted via `engine.run(...)` is bit-identical to pre-refactor.",
  "`make check` (the full gate: `make verify-no-origin-literal && make lint && make type-check && make test`) exits with code 0 — the build gate of record per `Makefile:11–29`.",
  "[sherlock] Behavior preservation is verifiable end-to-end: a `FakeQualityGate.calls`-introspection assertion (records every call's 10 kwargs at `tests/fakes.py:821–873`) sees exactly one call post-refactor with the same kwarg values as pre-refactor — no extra call, no kwarg rename, no value transformation. Combined with the unchanged SSE event sequence (Watson 1) and the unchanged 3-key return dict (Watson 4), this proves zero runtime semantics change in the protocol-passthrough."
]