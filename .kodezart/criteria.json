[
  "[scope] `git diff --name-only` after the edit returns EXACTLY these two paths and no others: `src/kodezart/prompts/acceptance_criteria.py` and `src/kodezart/prompts/evaluation.py`.",
  "[scope] No file is created, deleted, or renamed: `git diff --name-only --diff-filter=AD` returns empty; `find src/kodezart -name '*.py' | wc -l` matches the pre-edit count.",
  "[scope] These paths are NOT in the diff: `src/kodezart/prompts/ticket_generation.py`, `src/kodezart/prompts/iteration_feedback.py`, `src/kodezart/adapters/git_artifact_persister.py`, `.gitignore`, anything under `src/kodezart/types/`, `src/kodezart/chains/`, `src/kodezart/core/`, `src/kodezart/services/`, `src/kodezart/handlers/`, `src/kodezart/adapters/`, or `tests/`.",
  "[sherlock] Both files are modified within the SAME commit: `git log -1 --name-only` lists both `src/kodezart/prompts/acceptance_criteria.py` and `src/kodezart/prompts/evaluation.py`. Landing them in separate commits is the bifurcation defect resurfacing.",
  "[acceptance_criteria.py Watson 1] The literal closing sentence `Each criterion must be testable by running a command or inspecting output — never by reading source code and nodding.` is REMOVED. Verified by: `grep -F 'never by reading source code and nodding' src/kodezart/prompts/acceptance_criteria.py` returns no matches.",
  "[acceptance_criteria.py Watson 1] The rewritten Watson 1 body contains the literal phrase `the changeset under review` (callsite-neutral). Verified by `grep -F 'the changeset under review' src/kodezart/prompts/acceptance_criteria.py` returning at least one match within the WATSON 1 block.",
  "[acceptance_criteria.py Watson 1] The rewritten Watson 1 body names three out-of-bounds criterion classes by literal phrase: `pull-request body`, `CI / check-run status`, `merge / branch state`. All three substrings are present and live within the Watson 1 block (before the Watson 2 heading).",
  "[acceptance_criteria.py Watson 1] The Watson 1 heading `── WATSON 1: BEHAVIORAL (subagent_type=Explore) ──` and the `Ultrathink. Be extremely thorough.` opener and the hexagonal/outside-in framing are preserved.",
  "[acceptance_criteria.py Watson 3] The runtime-exit ask phrases `type checker passes in its strictest`, `linter reports zero violations`, and `all existing and new tests pass` are absent from the Watson 3 block (verified by three negative greps).",
  "[acceptance_criteria.py Watson 3] The Watson 3 body enumerates ALL of these Python-only suppression tokens as literal substrings: `# type: ignore`, `# type: ignore[<code>]`, `# noqa`, `# noqa: <code>`, `# ruff: noqa`, `# ruff: noqa: <code>`, `pytest.mark.skip`, `pytest.mark.skipif`, `pytest.skip(`, `pytest.importorskip(`, `def test_`, `assert `, and a Pydantic structural check naming `model_config`, `extra`, `forbid` or `ignore`, and `allow`.",
  "[acceptance_criteria.py Watson 3] Ask (d) (typed-model boundary-validation: `external data entering the system is validated through typed models at system boundaries — no raw dictionaries or untyped payloads cross adapter edges`) is preserved VERBATIM.",
  "[acceptance_criteria.py Watson 3] The policy sentence stating the linter is NEVER allowed to be disabled or suppressed survives as a standalone sentence positioned AFTER the four enumerated asks (not embedded inside ask (b)). Verified by grep for `NEVER` within the Watson 3 block.",
  "[acceptance_criteria.py Watson 3] The block explicitly discloses the deliberate trade-off: contains a sentence using phrasing equivalent to `deliberate trade of grading strength for grading determinism` and explaining why negative-shape proxies replace positive runtime claims.",
  "[acceptance_criteria.py Watson 3] The Watson 3 heading `── WATSON 3: TOOLING (subagent_type=Explore) ──`, `subagent_type=Explore`marker, `Ultrathink. Be extremely thorough.` opener, and the line about reading the project's build configuration are preserved.",
  "[acceptance_criteria.py Watson 5] Ask (b) is replaced from `all existing tests still pass after the change — no regressions` to a static-greppable proxy asserting no test file is weakened, skipped, or deleted on changed lines, naming `pytest.mark.skip`, `pytest.mark.skipif`, `pytest.skip(`, `pytest.importorskip(`, removed `def test_`, removed `assert `.",
  "[acceptance_criteria.py Watson 5] Asks (a), (c), (d) are preserved VERBATIM, as is the Watson 5 heading and the `Ultrathink. Be extremely thorough.` opener.",
  "[acceptance_criteria.py SYNTHESIS] The existing numbered steps 1, 2, 3 and the closing `Do NOT short-circuit ... and so you can do the cross-reference pass that is the entire reason you exist.` paragraph are byte-preserved.",
  "[acceptance_criteria.py SYNTHESIS] Insertion (i) — invariant — is present between the existing step 3 closing line and the `Do NOT short-circuit` paragraph, AND names both `.kodezart/ticket.json` and `.kodezart/criteria.json` as literal substrings, AND asserts the criterion set must be JOINTLY SATISFIABLE under the harness's persistence-commit injection between base and head.",
  "[acceptance_criteria.py SYNTHESIS] Insertion (ii) — forbidden-criteria list — is present and names: criteria depending on `pull-request body`, `CI / check-run status`, `merge / branch state`, criteria requiring command execution to grade, criteria pinning literal counts (file count, export count, import count, grep hit count), AND the exact literal phrase `exactly N files modified`.",
  "[acceptance_criteria.py SYNTHESIS] Insertion (iii) — mutual-satisfiability self-check — is present, contains the literal question `is there an implementation that satisfies every criterion simultaneously, under the harness's persistence-commit injection between base and head?`, and routes a NO answer back through one additional drafter pass with the unsatisfiable conjunction inlined as feedback.",
  "[acceptance_criteria.py SYNTHESIS] Insertion (iii) includes a best-effort disclosure sentence stating the self-check `is in-prompt best-effort, not a hard guard`, and explicitly noting it does not introduce a new graph node / round-trip / schema field / state field.",
  "[sherlock] The best-effort disclosure in SYNTHESIS (iii) is explicitly framed as a deliberate disclosed compromise (referencing the user constraint that blocks new graph nodes), NOT as undisclosed graceful degradation. Verified by reading the surrounding context.",
  "[acceptance_criteria.py NO-DEFER RULE] The entire NO-DEFER RULE block (heading `── NO-DEFER RULE ──` + body) is byte-identical to the pre-edit content. Verified by content-diff (NOT by hardcoded line range, since SYNTHESIS insertions shift downstream line numbers): `git show HEAD~1:src/kodezart/prompts/acceptance_criteria.py | sed -n '/── NO-DEFER RULE ──/,/^── /p'` diffed against the same content extracted from the post-edit file produces no output.",
  "[acceptance_criteria.py invariants] All five Watson headings appear in `_CRITERIA_PRELUDE`: `── WATSON 1: BEHAVIORAL`, `── WATSON 2: ARCHITECTURE`, `── WATSON 3: TOOLING`, `── WATSON 4: ANTI-PATTERN`, `── WATSON 5: INTEGRATION SANITY`.",
  "[acceptance_criteria.py invariants] The literal substrings `WATSON 1` and `graceful degradation` are present in the rendered output of `build_prompt('test')`. The `graceful degradation` site at the WATSON 4 ANTI-PATTERN block (around line 79 pre-edit) is byte-unchanged.",
  "[acceptance_criteria.py invariants] Watson 2 ARCHITECTURE block and Watson 4 ANTI-PATTERN block are byte-unchanged: `git diff src/kodezart/prompts/acceptance_criteria.py` shows no +/- lines within those blocks.",
  "[evaluation.py Watson 3] The verb phrase `reason about whether the changes pass` is ABSENT from the entire file: `grep -F 'reason about whether the changes pass' src/kodezart/prompts/evaluation.py` returns no matches.",
  "[evaluation.py Watson 3] The rewritten Watson 3 body enumerates the SAME Python-only suppression-token set as `acceptance_criteria.py` Watson 3 (every token from the canonical set is present as a literal substring in both files).",
  "[evaluation.py Watson 3] The rewritten Watson 3 body instructs the Watson to report each finding with the suppression token, file path, and changed-line evidence (line numbers from the diff). Verified by reading the block.",
  "[evaluation.py Watson 3] The rewritten block uses callsite-neutral phrasing (`the changeset under review` or equivalent) and does NOT use `the ralph branch working tree`, `the ralph tip`, `the merged diff`, `feature_tip`, `working-tree`. Verified by `grep -nE 'ralph branch|ralph tip|merged diff|feature_tip|working[ -]tree' src/kodezart/prompts/evaluation.py` returning no matches inside the Watson 3 block.",
  "[evaluation.py Watson 3] The policy sentence `The linter is NEVER allowed to be disabled or suppressed` is preserved (the verification mechanism changes from `reason about` to grep-the-diff; the policy itself stays).",
  "[evaluation.py Watson 3] The Watson 3 heading `── WATSON 3: LINT & TYPE SAFETY (subagent_type=Explore) ──`, the `subagent_type=Explore` marker, the `Ultrathink. Be extremely thorough.` opener, the project-build-configuration sentence, the per-file-ignores warning, and the closing reporting sentence are preserved.",
  "[sherlock] No forbidden literal substrings leak into `evaluation.py` — none of `git diff`, `git log`, `--name-only`, `--format=%s` appears anywhere in the file. Verified by four negative greps. (Trap: this is pinned by `tests/prompts/test_prompt_wiring.py::test_evaluation_prompt_inlines_changeset_digest`; the rewrite must phrase grep directives against the already-rendered changeset block, not against git commands.)",
  "[evaluation.py invariants] All five Watson headings appear in `_EVALUATION_PRELUDE`. Watson 1 (line 38 pre-edit), Watson 2 ARCHITECTURE (lines 48-66 pre-edit, including `graceful degradation` at line 54), Watson 4 (lines 78-104 pre-edit), Watson 5 (lines 106-118 pre-edit), SYNTHESIS (lines 122-145 pre-edit), and NO-DEFER RULE (lines 147-175 pre-edit) are all byte-unchanged.",
  "[evaluation.py invariants] The literal `graceful degradation` is still present in the rendered output of `build_prompt(criteria=[...], changeset=ChangesetDigest(...))`. Verified by `grep -F 'graceful degradation' src/kodezart/prompts/evaluation.py` returning at least one match.",
  "[evaluation.py contract] The `build_prompt` signature is byte-identical: `def build_prompt(*, criteria: list[str], changeset: ChangesetDigest) -> str:`. The `*,` keyword-only marker, parameter names `criteria` and `changeset`, parameter types `list[str]` and `ChangesetDigest`, and return type `str` are unchanged.",
  "[evaluation.py contract] `_render_changeset` helper (lines 202-218 pre-edit) and `_EMPTY_CHANGESET_NOTE` are byte-unchanged.",
  "[cross-file symmetry] The Python-only suppression-token SET enumerated in `acceptance_criteria.py` Watson 3 is identical to the set enumerated in `evaluation.py` Watson 3 (no token is present in only one file). Verified by extracting backticked token literals from each Watson 3 block, sorting them, and diffing — the diff produces no output. This is the only thing preventing the bifurcation defect from resurfacing as token drift.",
  "[acknowledged compromise] The suppression-token duplication between the two files is acknowledged as a documented scope-bounded compromise (extraction of `SUPPRESSION_TOKENS` to `src/kodezart/types/domain/` is blocked by the user's edit-set constraint). No new constant module is created in this ticket.",
  "[pinned literals] The three sites of `graceful degradation` literal all survive byte-unchanged: `src/kodezart/prompts/acceptance_criteria.py:79`, `src/kodezart/prompts/evaluation.py:54`, `src/kodezart/prompts/iteration_feedback.py:45`. Verified by `grep -c 'graceful degradation' src/kodezart/prompts/{acceptance_criteria,evaluation,iteration_feedback}.py` returning a positive count from each file.",
  "[pinned literals] The `NO-DEFER RULE` literal in `src/kodezart/prompts/ticket_generation.py::_REVIEW_PROMPT_PRELUDE` is byte-unchanged (file not touched). Verified by `grep -F 'NO-DEFER RULE' src/kodezart/prompts/ticket_generation.py` returning at least one match.",
  "[no-touch] `src/kodezart/prompts/ticket_generation.py` and `src/kodezart/prompts/iteration_feedback.py` and `src/kodezart/prompts/__init__.py` are not modified: `git diff -- src/kodezart/prompts/ticket_generation.py src/kodezart/prompts/iteration_feedback.py src/kodezart/prompts/__init__.py` is empty.",
  "[no-touch] `src/kodezart/chains/ralph_workflow.py`, `src/kodezart/chains/ralph_loop.py`, `src/kodezart/core/constants.py`, `src/kodezart/types/domain/agent.py`, `src/kodezart/types/domain/workflow.py`, `src/kodezart/types/domain/consolidation.py`, `src/kodezart/adapters/git_artifact_persister.py`, and `.gitignore` are byte-unchanged.",
  "[imports] Neither edited file gains or loses an import line. `acceptance_criteria.py` still has zero imports; `evaluation.py` still has exactly one import (`from kodezart.types.domain.consolidation import ChangesetDigest`). Verified by `git diff -U0 src/kodezart/prompts/acceptance_criteria.py src/kodezart/prompts/evaluation.py | grep -E '^[+-]\\s*(import |from )'` returning empty.",
  "[no new public symbols] Neither edited file adds or removes a module-level name. The set of names returned by `dir(module)` filtered to non-underscore-prefixed names is byte-identical pre- and post-edit (currently: `{build_prompt}` for both modules).",
  "[no new helpers / constants] No new function, class, decorator, or module-level constant is added in either file. Diff hunks land exclusively inside the bodies of `_CRITERIA_PRELUDE` and `_EVALUATION_PRELUDE`. Verified by `git diff -U0` showing every +/- line falls between the constant's opening `\"\"\"\\` and closing `\"\"\"` markers.",
  "[formatting convention] All new content uses the file's existing `\\<newline>` in-string escape convention (backslash immediately before newline inside the triple-quoted constant). No new triple-quoted nesting is introduced; no implicit string concatenation (adjacent `\"...\" \"...\"` pairs) is introduced; no new f-string is introduced.",
  "[ruff E501] Every physical source line in both edited files is ≤ 88 characters: `awk 'length > 88' src/kodezart/prompts/acceptance_criteria.py src/kodezart/prompts/evaluation.py` returns empty.",
  "[ruff exit 0] `ruff check src/kodezart/prompts/acceptance_criteria.py src/kodezart/prompts/evaluation.py` exits 0.",
  "[mypy exit 0] `mypy --strict src/kodezart/prompts/acceptance_criteria.py src/kodezart/prompts/evaluation.py` exits 0.",
  "[no new suppressions] The count of suppression markers in `src/kodezart/prompts/` source code (NOT inside the documented enumeration strings — count by ruff exit code, not by raw grep) is unchanged. Verified by `ruff check` continuing to pass without any new `# noqa` or `# type: ignore` comment lines added.",
  "[wiring tests] `pytest tests/prompts/test_prompt_wiring.py -q` exits 0. Specifically: `test_evaluation_prompt_contains_watson_dispatch`, `test_acceptance_criteria_prompt_contains_watson_dispatch`, `test_iteration_feedback_contains_watson_dispatch`, `test_evaluation_prompt_inlines_changeset_digest`, `test_evaluation_prompt_handles_empty_changeset`, and `test_evaluation_prompt_rejects_positional_args` all pass.",
  "[ticket-gen tests] `pytest tests/chains/test_ticket_generation.py -q` exits 0; in particular `test_reviewer_prompt_has_sherlock_framing` (lines 332-336) passes, confirming `ticket_generation.py::_REVIEW_PROMPT_PRELUDE` retains `You are Sherlock`, `WATSON 1: ALIGNMENT`, `WATSON 4: OFFICIAL DOCS`, `Medium articles`, and `NO-DEFER RULE`.",
  "[adapter tests] `pytest tests/adapters/test_git_artifact_persister.py -q` exits 0; `.kodezart/` artifact lifecycle (create + clean) is unaffected.",
  "[full suite] `pytest -q` exits 0 with no regressions.",
  "[render-time mutation: acceptance_criteria] Calling `kodezart.prompts.acceptance_criteria.build_prompt('test task')` returns a string that: contains `the changeset under review`; contains all three of `pull-request body`, `CI / check-run status`, `merge / branch state`; contains `.kodezart/ticket.json` and `.kodezart/criteria.json`; contains the literal `exactly N files modified`; contains every Python-only suppression token in the canonical set; does NOT contain `never by reading source code and nodding`; does NOT contain `type checker passes in its strictest`; does NOT contain `linter reports zero violations`; does NOT contain `all existing and new tests pass`.",
  "[render-time mutation: evaluation] Calling `kodezart.prompts.evaluation.build_prompt(criteria=['x'], changeset=ChangesetDigest(file_paths=[], commit_subjects=[], commit_count=0))` returns a string that: contains every Python-only suppression token in the canonical set; contains `the changeset under review`; does NOT contain `reason about whether the changes pass`; does NOT contain any of `git diff`, `git log`, `--name-only`, `--format=%s`; still contains `graceful degradation` (preserved at Watson 2 line 54).",
  "[anti-pattern: no fabricated success] No new criterion in `acceptance_criteria.py` claims `exits 0`, `runs successfully`, `CI succeeds`, `build succeeds`, or `deploys successfully` as a positive runtime claim. Verified by `grep -nE 'exits 0|runs success|CI success|build success' src/kodezart/prompts/acceptance_criteria.py` returning no matches inside the rewritten Watson blocks.",
  "[anti-pattern: no silent error swallowing] No new directive instructs the Watson to `skip on error`, `ignore unparseable diff`, `continue if grep fails`, or similar. Verified by reading the rewritten Watson 3 in both files.",
  "[anti-pattern: fail-loud] The SYNTHESIS mutual-satisfiability self-check routes a NO answer back through one additional drafter pass with the unsatisfiable conjunction inlined as feedback — NOT silently dropped, NOT defaulted-to-pass.",
  "[anti-pattern: no graceful-degradation slip] No new language in the rewritten blocks softens criteria with `best-effort`, `if possible`, `where feasible`, `should attempt`, or `may degrade`, EXCEPT the explicitly disclosed SYNTHESIS (iii) best-effort sentence which self-labels as a deliberate trade-off referencing the no-new-graph-node constraint.",
  "[anti-pattern: no mocks in production] No `stub`, `fake`, `mock`, or `return True/False` placeholder appears in either edited file (test doubles belong in tests/ which is untouched).",
  "[anti-pattern: no YAGNI violations] No new module under `src/kodezart/`, no new graph node, no new schema field on `CriterionResult` or `AcceptanceCriteriaOutput` or `WorkflowState` or `RalphLoopState`, no extracted `SUPPRESSION_TOKENS` constant, no new `StandingRule` model, no new state field, no new round-trip — all blocked by user's edit-set scope.",
  "[callsite compatibility] `chains/ralph_loop.py:206` (`evaluation_prompt.build_prompt(criteria=..., changeset=...)`) and `chains/ralph_workflow.py:684` (`evaluation_prompt.build_prompt(criteria=..., changeset=...)`) continue to call `build_prompt` with keyword-only args unchanged. Both files are byte-unchanged.",
  "[in-loop graderability] Every emitted criterion (under the rewritten prompts) is falsifiable from in-loop evidence collectable via `EVAL_TOOLS = [Read, Glob, Grep, Bash]` (verified at `src/kodezart/core/constants.py:4-5`) against the changeset under review at evaluation time. This is the structural invariant the SYNTHESIS insertion (i) makes explicit."
]