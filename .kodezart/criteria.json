[
  "1. The bare `raise RuntimeError(...)` at `src/kodezart/adapters/git_change_persister.py:83-88` is gone — divergence no longer raises by default. `grep -n 'raise RuntimeError' src/kodezart/adapters/git_change_persister.py` returns matches only inside the `if remote_tip is None:` invariant guard and inside the backup-push-failure path.",
  "2. When `persist(...)` detects divergence and recovery succeeds, the returned `PersistResult.source is PersistSource.DIVERGENCE_REPLAY` (identity check, not string equality). Asserted by all three new adapter tests and the rewritten integration test.",
  "3. `PersistSource.DIVERGENCE_REPLAY = \"divergence_replay\"` is added in-place to the `StrEnum` in `src/kodezart/types/domain/persist.py`, matching the snake_case value pattern of the existing two members. `grep -n DIVERGENCE_REPLAY src/kodezart/types/domain/persist.py` returns exactly one definition; `grep -rn DIVERGENCE_REPLAY src/` shows no re-exports from any other module.",
  "4. The backup ref name is constructed via `str(BackupBranchName(source_branch=branch, job_id_prefix=backup_ref_id_prefix))` — no f-string `f\"{branch}-backup-...\"` concatenation. `grep -n '\"-backup-\"' src/kodezart/adapters/git_change_persister.py` returns zero hits; `grep -rn '\"-backup-\"' src/` returns hits only in `src/kodezart/types/domain/branch.py`.",
  "5. The backup-push to the configured remote is the FIRST `self._git.*` call in `_recover_from_divergence` — before any `reset_hard` or `commit_tree`. Asserted by `test_persist_diverged_backup_push_failure_re_raises_without_mutation`: when `push_error` is set, `[c for c in fake_git.calls if c[0]=='push']` equals exactly `[('push', cwd, 'feat-backup-deadbeef')]` and `not any(c[0] in {'reset_hard','commit_tree'} for c in fake_git.calls)`.",
  "6. When the backup push raises, `_recover_from_divergence` emits `await self._log.aerror(\"divergence_backup_push_failed\", ...)` with structured kwargs (`backup=`, `head_sha=`, `remote_tip=`, `branch=`, `error=`), then re-raises a `RuntimeError` matching `/diverged/` and referencing both `head_sha` and the configured remote/branch. State is unmutated.",
  "7. [sherlock] The recovery helper performs operations in this exact order: `self._git.push(workspace_path, backup_name)` → `self._git.head_commit_message(workspace_path)` (capture `head_message_divergent`) → `self._git.tree_of(workspace_path, head_sha)` (capture `head_tree`) → `self._git.reset_hard(workspace_path, remote_tip)`. The tree+message capture happens AFTER backup push (so backup is reachable) and BEFORE reset (so capture is defensive against worktree GC). Verify by reading the helper.",
  "8. Exactly one `reset_hard(remote_tip)` is invoked on every recovery path (never two; no separate bare-cache ref reset). On the tree-differ branch a second `reset_hard(replay_sha)` is allowed before the final push, but no third reset.",
  "9. On the tree-equal branch (`head_tree == remote_tip_tree`): `PersistResult.commit_sha == remote_tip`; `PersistResult.message` is read via `self._git.head_commit_message(workspace_path)` AFTER `reset_hard(remote_tip)` (so it describes `remote_tip`, fixing the prior-draft invariant violation); `commit_tree` is NOT invoked. Asserted by `test_persist_diverged_tree_equal_skips_replay`: `not any(c[0]=='commit_tree' for c in fake_git.calls)`.",
  "10. On the tree-differ branch: `commit_tree` is invoked with `parent=remote_tip` and `message=head_message_divergent` (captured BEFORE reset); then `reset_hard(replay_sha)` and `push(branch)` follow in order. `PersistResult.commit_sha == replay_sha` and `PersistResult.message == head_message_divergent`. The ordered fake-call sequence asserted in `test_persist_diverged_tree_differ_replays_in_order` matches the exact 5-tuple list pinned in the ticket.",
  "11. [sherlock] `PersistResult.message` describes `PersistResult.commit_sha` on BOTH recovery branches — the tree-equal test reads remote-tip's message; the tree-differ test verifies the divergent HEAD's message IS the replay commit's message (they describe the same SHA by construction). No PersistResult is ever constructed where `message` describes a commit OTHER than `commit_sha`.",
  "12. No `--force`, `-f`, or `--force-with-lease` appears anywhere in the recovery path. `grep -rn -- '--force\\|--force-with-lease' src/kodezart/adapters/git_change_persister.py src/kodezart/adapters/subprocess_git_service.py` returns zero matches in code introduced by this PR. The final `push(branch)` is fast-forward by construction (replay's parent IS `remote_tip`).",
  "13. The divergence branch's precondition uses `if remote_tip is None: raise RuntimeError(...)`, NOT `assert remote_tip is not None`. `grep -n 'assert remote_tip' src/kodezart/adapters/git_change_persister.py` returns zero matches; the `if/raise` block is present (asserts are stripped under `-O`).",
  "14. Three new methods are added to `class GitService(Protocol):` in `src/kodezart/core/protocols.py` (single canonical declaration each): `reset_hard(cwd: str, ref: str) -> None`, `tree_of(cwd: str, ref: str) -> str`, `commit_tree(cwd: str, tree: str, parent: str, message: str, author_name: str, author_email: str) -> str`. Each carries a docstring in the existing port style referencing the underlying `git ...` command. `grep -n 'async def reset_hard\\|async def tree_of\\|async def commit_tree' src/kodezart/core/protocols.py` returns exactly 3 hits.",
  "15. Every concrete `GitService` implementation in the repo implements the three new methods. The set is exhaustively: `SubprocessGitService` in `src/kodezart/adapters/subprocess_git_service.py`, `FakeGitService` in `tests/fakes.py`. Subclasses (e.g. `_HasChangesRaisingGitService` in `tests/adapters/test_git_worktree_provider.py`) inherit without override. `grep -rn '^class .*GitService' src/ tests/` returns exactly the Protocol + these two classes + the subclass.",
  "16. [sherlock] The `ChangePersister.persist(...)` Protocol in `src/kodezart/core/protocols.py:188-201` declares `backup_ref_id_prefix: str` as keyword-only and REQUIRED (no `= ...` default). Verify by reading the Protocol body — the parameter signature has no default value, forcing every consumer to pass it explicitly.",
  "17. `FakeChangePersister.persist` in `tests/fakes.py` matches the Protocol exactly: keyword-only `backup_ref_id_prefix: str` with no default; the value is recorded on the `self.calls` dict entry under key `\"backup_ref_id_prefix\"`. `grep -n 'backup_ref_id_prefix' tests/fakes.py` returns at least the parameter line and the self.calls recording line.",
  "18. All `ChangePersister`/`FakeChangePersister.persist(...)` call sites pass `backup_ref_id_prefix=` as a kwarg. Post-change exhaustive set: three in `tests/adapters/test_git_change_persister.py` (lines ~76, ~130, ~181, each passing `\"deadbeef\"`), one in `tests/integration/test_workflow_e2e.py` (~line 773, passing `\"abc12345\"`), and one in `src/kodezart/services/agent_service.py` (~line 183, computed). `ArtifactPersister.persist` call sites are unrelated and untouched.",
  "19. `tests/adapters/test_git_change_persister.py::test_persist_raises_on_diverged_head` (lines 144-172) is deleted. `grep -n 'test_persist_raises_on_diverged_head\\|pytest.raises(RuntimeError, match=\"diverged\")' tests/adapters/test_git_change_persister.py` returns zero matches.",
  "20. Three new FakeGitService-driven recovery tests are added in `tests/adapters/test_git_change_persister.py`: `test_persist_diverged_backup_push_failure_re_raises_without_mutation`, `test_persist_diverged_tree_equal_skips_replay`, and `test_persist_diverged_tree_differ_replays_in_order`. The third asserts the exact ordered 5-tuple `fake_git.calls` sequence pinned in the ticket. All three use `FakeGitService` knobs (`trees=`, `commit_tree_result=`, `push_error=`) — no `unittest.mock`, no `monkeypatch` of git internals.",
  "21. `tests/integration/test_workflow_e2e.py:718-787` is rewritten as `test_git_change_persister_recovers_from_divergence_against_configured_remote` (or equivalent). The `@pytest.mark.parametrize(\"remote_name\", [\"origin\", \"upstream\"])` parametrize is preserved. The `pytest.raises(RuntimeError)` block is replaced by: `result.source is PersistSource.DIVERGENCE_REPLAY`; `result.branch == \"main\"`; `git ls-remote --heads <remote_name> main-backup-abc12345` returns a non-empty line; the remote tip of `main` on the configured remote equals `result.commit_sha`. Both `[\"origin\", \"upstream\"]` cases pass.",
  "22. Three new real-git tests are added in `tests/adapters/test_subprocess_git.py` following the existing `git_repo` + `_run_git` + `git_service` fixture pattern: `test_reset_hard_moves_head_to_ref`, `test_tree_of_returns_tree_sha_for_head_and_full_sha`, `test_commit_tree_creates_commit_with_parent_author_and_message`. The commit-tree test verifies `git log -1 --format=%P` equals the parent SHA, `%s` equals the message, `%an` equals the supplied author name, and `%ae` equals the supplied author email — proving the env-injected author identity actually rides the commit.",
  "23. `_author_env(name: str, email: str) -> dict[str, str]` is a `@staticmethod` on `SubprocessGitService` and is the SOLE construction site of the `GIT_AUTHOR_NAME/GIT_COMMITTER_NAME/GIT_AUTHOR_EMAIL/GIT_COMMITTER_EMAIL` dict. Both `commit()` and the new `commit_tree()` consume it. `grep -n 'GIT_AUTHOR_NAME' src/kodezart/adapters/subprocess_git_service.py` returns exactly one match (inside `_author_env`); `grep -n '_author_env' src/kodezart/adapters/subprocess_git_service.py` shows one definition and ≥2 call sites.",
  "24. `_run_output` is EXTENDED with `env: dict[str, str] | None = None`, merging over `os.environ` mirroring `_run`'s pattern. No sibling helper (`_run_output_env`, etc.) is introduced. `grep -nE 'async def _run_output|async def _run_output_env' src/kodezart/adapters/subprocess_git_service.py` returns exactly one match (the extended `_run_output`).",
  "25. `src/kodezart/services/agent_service.py` imports `generate_job_id` from `kodezart.domain.agent` and does NOT import stdlib `uuid`. `grep -nE '^import uuid|^from uuid' src/kodezart/services/agent_service.py` returns zero matches; `grep -n 'from kodezart.domain.agent import generate_job_id' src/kodezart/services/agent_service.py` returns one match. Services depend on the domain layer for ID generation per project layering.",
  "26. `agent_service._run_in_workspace` computes `backup_ref_id_prefix = (session_id or generate_job_id())[:8]` exactly once and forwards it to `self._persister.persist(...)`. The expression preserves the caller's `session_id=None` intent — no `if session_id is None: session_id = generate_job_id()` mutation. `grep -n 'if session_id is None' src/kodezart/services/agent_service.py` returns zero hits in this method. The slice satisfies `BackupBranchName.job_id_prefix`'s `min_length=8, max_length=8` (both branches of `or` yield ≥32 chars).",
  "27. `src/kodezart/adapters/git_change_persister.py` does NOT import `asyncio`, `subprocess`, `os`, or `pathlib` for git operations. `grep -nE '^import asyncio|^import subprocess|^from subprocess|^import os|^from pathlib' src/kodezart/adapters/git_change_persister.py` returns zero matches. `_recover_from_divergence` calls ONLY `self._git.*` and `self._log.*` — verified by reading the method body.",
  "28. `_recover_from_divergence` is a private method on `GitChangePersister` (not in `services/`, not in `chains/`, not in `domain/`). Its signature is `async def _recover_from_divergence(self, *, workspace_path: str, branch: str, head_sha: str, remote_tip: str, backup_ref_id_prefix: str) -> PersistResult` — keyword-only, matching the ticket-pinned signature exactly.",
  "29. `src/kodezart/domain/health.py` contains exactly the 11-line content pinned in the ticket: module docstring (unchanged), `from importlib.metadata import version`, `from kodezart.types.domain.health import HealthStatus`, `_APP_VERSION = version(\"kodezart\")`, and `check_health()` returning `HealthStatus(healthy=True, version=_APP_VERSION, service=\"kodezart\")`. NO `try/except PackageNotFoundError` fallback exists.",
  "30. `_APP_VERSION = \"0.1.0\"` (or any literal string) is gone from `src/kodezart/domain/health.py`. `grep -nE '_APP_VERSION = \"' src/kodezart/domain/health.py` returns zero matches; `grep -rn '\"0.1.0\"' src/` returns zero matches.",
  "31. `pyproject.toml [project].version = \"0.1.0\"` is unchanged — it remains the single source of truth. `grep -n '^version' pyproject.toml` returns `version = \"0.1.0\"`.",
  "32. `tests/api/v1/test_health.py` is unchanged and its `version == \"0.1.0\"` assertion continues to pass against the installed wheel's dist-info under `uv run pytest`. `uv run pytest tests/api/v1/test_health.py -q` exits 0.",
  "33. `check_health()`'s function body is unchanged (a single `return HealthStatus(...)` reading `_APP_VERSION` as an attribute) and the `health.py` module docstring is unchanged. The 'no I/O, no side effects' contract is preserved at call time; the module-level `version(\"kodezart\")` runs once at import.",
  "34. No file-I/O at module load anywhere in changed files. `grep -nE '^import tomllib|^[[:space:]]*with open\\(' src/kodezart/domain/health.py` returns zero matches.",
  "35. `CONTRIBUTING.md` gains exactly one new bullet under `Dev Setup` (or `Testing`) documenting that raw `pytest` invocations bypass `uv`'s auto-sync; `uv run pytest` (or `make test`) is required for `importlib.metadata.version(\"kodezart\")` to resolve. `git diff CONTRIBUTING.md` shows exactly one bullet added.",
  "36. The persister module docstring at `src/kodezart/adapters/git_change_persister.py:1-11` is REPLACED with the EXACT text pinned in the ticket — describing the three persistence paths and the 'Raises RuntimeError only when the pre-mutation backup push in the divergence-recovery path fails' clause. Verify by reading the first ~18 lines.",
  "37. The `persist` method docstring at `src/kodezart/adapters/git_change_persister.py:51-60` is REPLACED with the EXACT text pinned in the ticket — describing the four-branch decision tree (dirty / clean+equal / clean+descends / clean+diverged → recover).",
  "38. The three required structured-log events fire from `_recover_from_divergence`: `\"divergence_backup_push_failed\"` (in the backup-push exception path), `\"divergence_recovered_tree_equal\"` (in the tree-equal branch before return), and `\"divergence_recovered_replay\"` (in the tree-differ branch before return). Each is invoked with structured kwargs (`backup=`, `commit_sha=`, `branch=`, etc.) — no f-string-interpolated event names.",
  "39. [sherlock] Severity assignment matches project precedent: `divergence_backup_push_failed` uses `self._log.aerror(...)` (safety-net failure is loud); both success events (`divergence_recovered_tree_equal`, `divergence_recovered_replay`) use `self._log.ainfo(...)` (recovery succeeded is informational). Consistent with `git_artifact_persister.py:115-118` (aerror for recoverable failures) and `git_branch_merger.py` cleanup-info precedents.",
  "40. `FakeGitService.__init__` gains exactly three new keyword-only knobs: `trees: dict[str, str] | None = None`, `commit_tree_result: str = \"c\" * 40`, and `push_error: Exception | None = None`. The `push` method consumes `push_error` as a one-shot (sets `self._push_error = None` after raising) — necessary for the tree-differ replay test that pushes twice (backup + fast-forward).",
  "41. `FakeGitService` gains exactly three new methods (`reset_hard`, `tree_of`, `commit_tree`) that append `(name, cwd, ...)` tuples to `self.calls` and return deterministic strings (`tree_of` returns `self._trees.get(ref, \"t\" * 40)`; `commit_tree` returns `self._commit_tree_result`; `reset_hard` returns `None`).",
  "42. `src/kodezart/adapters/git_branch_merger.py:139-189` (`cleanup_backup_branches`) is unchanged. The new backup ref `{branch}-backup-{8hex}` flows through the existing sweep automatically because `BackupBranchName.is_backup` checks for the literal `-backup-` infix. `git diff src/kodezart/adapters/git_branch_merger.py` shows no edits in that range.",
  "43. No `match` statement or exhaustive `if/elif` chain on `PersistSource` exists that would silently drop `DIVERGENCE_REPLAY`. `grep -rn 'PersistSource\\.' src/ tests/` reveals no caller switches exhaustively on the enum that would require a new branch.",
  "44. No `# noqa`, `# type: ignore`, `pytest.skip`, `@pytest.mark.skip`, or `@pytest.mark.xfail` is added to any changed file. Suppressions and skips are forbidden by the project's no-fallback rule. `grep -rn '# noqa\\|# type: ignore\\|pytest.skip\\|@pytest.mark.skip\\|@pytest.mark.xfail' <all changed files>` returns zero matches in additions.",
  "45. No re-export shims are introduced for any new symbol. `PersistSource.DIVERGENCE_REPLAY` is imported only from `kodezart.types.domain.persist`. `grep -rn 'from kodezart\\..* import DIVERGENCE_REPLAY' src/ tests/` returns zero matches (only canonical import path is used). No `__all__` entry in any other module re-exports it.",
  "46. `make check` exits 0. The aggregate target runs the no-origin-literal verifier, `uv run ruff check src/ tests/`, `uv run mypy src/` (strict mode, with the pydantic plugin enabled), and `uv run pytest`. Each gate also passes independently: `make lint`, `make type-check`, `make test`.",
  "47. `uv run mypy --strict src/` exits 0 on all changed files. Structural-subtype check confirms `SubprocessGitService` and `FakeGitService` satisfy the extended `GitService` Protocol, and `GitChangePersister` + `FakeChangePersister` satisfy the extended `ChangePersister` Protocol (kwarg present, no default, correct type).",
  "48. No new circular imports are introduced. `uv run python -c 'import kodezart.adapters.git_change_persister, kodezart.adapters.subprocess_git_service, kodezart.services.agent_service, kodezart.domain.health, kodezart.types.domain.persist, tests.fakes'` exits 0.",
  "49. [sherlock] No new layer-direction violations. `grep -rn '^from kodezart\\.adapters\\|^import kodezart\\.adapters' src/kodezart/core src/kodezart/services src/kodezart/chains src/kodezart/handlers src/kodezart/api src/kodezart/types src/kodezart/domain` returns zero matches after this PR — adapters remain unimported by inner layers.",
  "50. [sherlock] If `_git_output` helper does not already exist in `tests/integration/test_workflow_e2e.py`, it is added once mirroring the existing `_git` helper but capturing stdout (and used by the new ls-remote assertions). If a similar helper already exists, the new code reuses it — no duplicate helper is introduced. Verify by `grep -n 'async def _git_output\\|async def _git\\b' tests/integration/test_workflow_e2e.py`.",
  "51. [sherlock] Structured-log payloads use typed kwargs only — no `**{...}` dict-literal unpacking and no second-positional dict. `grep -nE 'self\\._log\\.(ainfo|aerror|awarning|adebug)\\([^)]*\\*\\*\\{' src/kodezart/adapters/git_change_persister.py` returns zero matches; every event uses inline `key=value` kwargs."
]