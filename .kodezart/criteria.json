[
  "`src/kodezart/core/soft_failure.py` does not exist on disk and is not present in git tree (`test ! -f src/kodezart/core/soft_failure.py` AND `git ls-files src/kodezart/core/soft_failure.py` returns empty); no backwards-compatibility shim, no forwarding stub, no aliased module replaces it.",
  "`tests/core/test_soft_failure.py` does not exist on disk and is not in git tree; the file `tests/core/test_error_egress.py` exists at the same layer.",
  "`src/kodezart/core/errors.py`, `src/kodezart/core/stream_drain.py`, and `src/kodezart/core/error_egress.py` all exist as regular files in the `core/` layer (siblings of `retry.py`, `logging.py`, `checkpointer.py`, `constants.py`, `protocols.py`, `config.py`).",
  "The tightened hygiene grep returns empty: `git grep -E 'kodezart\\.core\\.soft_failure|core/soft_failure\\.py' src/ tests/` produces zero output lines.",
  "`make check` (which chains `verify-no-origin-literal && lint && type-check && test`) exits 0 on the final tree.",
  "`make format` is a fixed point on the final tree — running `uv run ruff format src/ tests/ && uv run ruff check --select I --fix src/ tests/` produces zero file changes (no unstaged diffs after the run).",
  "`NoStructuredOutputError` is defined exactly once across `src/kodezart/`, at `src/kodezart/core/errors.py` (verified: `grep -rn '^class NoStructuredOutputError' src/kodezart` returns one hit at that path).",
  "`NoStructuredOutputError` inherits directly from `Exception` — NOT from `AgentSDKError`, `TransientAPIError`, or `RateLimitError`. Peer-of-`AgentSDKError` relationship preserved.",
  "`NoStructuredOutputError.__init__` signature and the attribute set `{raise_site, rate_limit_rejected, result_event_observed, stop_reason, is_error, session_id, total_cost_usd}` are byte-identical to the original `soft_failure.py` definition (diff of class bodies shows zero substantive change).",
  "`drain` is defined exactly once across `src/kodezart/`, at `src/kodezart/core/stream_drain.py`, as `async def drain(stream: AsyncIterator[AgentEvent]) -> tuple[ResultEvent | None, bool]`. The function body contains no `yield`/`yield from` (it is a coroutine, NOT an async generator — AST inspection confirms no `Yield`/`YieldFrom` descendant).",
  "Inside `drain`, the per-event branch order is preserved: the `isinstance(event, ResultEvent)` branch precedes the `isinstance(event, RateLimitWarningEvent) and event.status == 'rejected'` branch (matches original soft_failure.py:113–116).",
  "`_redact_credentials` is defined exactly once across `src/kodezart/`, at `src/kodezart/core/error_egress.py`; the three `re.sub` calls inside its body run in the original order `_CREDENTIAL_URL_PATTERN` → `_GH_TOKEN_PATTERN` → `_GH_FINEGRAINED_PAT_PATTERN`.",
  "`build_error_event` is defined exactly once across `src/kodezart/`, at `src/kodezart/core/error_egress.py`. Its `isinstance` branch order is `NoStructuredOutputError` first, `AgentSDKError` second; it uses `isinstance` checks (NOT `getattr(exc, ...)` introspection that would return `Any` and trip `disallow_any_explicit`).",
  "Module-level constants `_REDACTION_SENTINEL: Final[str]`, `_CREDENTIAL_URL_PATTERN`, `_GH_TOKEN_PATTERN`, `_GH_FINEGRAINED_PAT_PATTERN` are defined exactly once each, all in `src/kodezart/core/error_egress.py`. `_REDACTION_SENTINEL` retains its `Final` annotation and is importable by `tests/adapters/test_claude_executors.py`.",
  "The four redaction GitHub credential patterns (the regex bodies for `x-access-token:`, `gh[posu]_`, `github_pat_`) appear in exactly one file: `src/kodezart/core/error_egress.py`. No adapter, chain, or test fixture re-defines them.",
  "Both `src/kodezart/core/errors.py` and `src/kodezart/core/error_egress.py` import `RaiseSite` only under `if TYPE_CHECKING:`. Every occurrence of `RaiseSite` in either file is either inside a TYPE_CHECKING block or inside a forward-string/PEP 526 annotation; neither file performs a runtime `from kodezart.types.domain.agent import RaiseSite`.",
  "`src/kodezart/core/stream_drain.py` does NOT import `RaiseSite` at all (neither runtime nor TYPE_CHECKING) — `grep -n 'RaiseSite' src/kodezart/core/stream_drain.py` returns empty.",
  "`RaiseSite` is defined exactly once across the codebase, as `RaiseSite = Literal[...]` at module top of `src/kodezart/types/domain/agent.py`. No parallel `Literal[...]` enumeration of raise-site identifiers appears in `core/errors.py` or `core/error_egress.py`.",
  "None of `core/errors.py`, `core/stream_drain.py`, `core/error_egress.py` imports from `kodezart.adapters.*`, `kodezart.chains.*`, `kodezart.handlers.*`, `kodezart.services.*`, or `kodezart.api.*` (`git grep -nE '^(from|import) kodezart\\.(adapters|chains|services|handlers|api)' src/kodezart/core/errors.py src/kodezart/core/stream_drain.py src/kodezart/core/error_egress.py` returns empty).",
  "`src/kodezart/domain/errors.py` is byte-unchanged (`git diff src/kodezart/domain/errors.py` is empty); its zero-`kodezart.*`-imports posture is preserved (`grep -E '^(from|import) kodezart' src/kodezart/domain/errors.py` returns empty).",
  "`core/error_egress.py` imports `NoStructuredOutputError` from `core/errors` (sibling, permitted) and `AgentSDKError` from `domain/errors` (downward, permitted); no other sibling `core/*` import appears in it.",
  "`src/kodezart/adapters/claude_agent_executor.py` and `src/kodezart/adapters/claude_client_executor.py` each contain exactly the single-line import `from kodezart.core.error_egress import _redact_credentials` (no other variant; the old soft_failure import is gone).",
  "`src/kodezart/adapters/git_change_persister.py`, `src/kodezart/chains/ralph_loop.py`, and `src/kodezart/chains/ralph_workflow.py` each contain `from kodezart.core.errors import NoStructuredOutputError` AND `from kodezart.core.stream_drain import drain`, in that alphabetical order (`errors` before `stream_drain`).",
  "`src/kodezart/chains/ticket_generation.py` contains three import lines in `make format`-canonical alphabetical order: `from kodezart.core.error_egress import build_error_event`, then `from kodezart.core.errors import NoStructuredOutputError`, then `from kodezart.core.stream_drain import drain` (underscore precedes `s` in ASCII, so `error_egress` < `errors`).",
  "`src/kodezart/handlers/agent_handler.py` and `src/kodezart/services/agent_service.py` each contain exactly `from kodezart.core.error_egress import build_error_event`.",
  "The 7 test files (`tests/adapters/test_claude_executors.py`, `tests/api/v1/test_agent.py`, `tests/chains/test_ralph_loop.py` at lines 423 and 790, `tests/chains/test_ralph_workflow.py` at lines 588 and 2740, `tests/chains/test_ticket_generation.py`, `tests/core/test_retry.py`) each import from the correct new module: `_REDACTION_SENTINEL` from `kodezart.core.error_egress`, `NoStructuredOutputError` from `kodezart.core.errors`.",
  "`tests/core/test_error_egress.py` opens with exactly two `kodezart`-prefixed import lines in alphabetical order: `from kodezart.core.error_egress import (_REDACTION_SENTINEL, _redact_credentials, build_error_event,)` first, then `from kodezart.core.errors import NoStructuredOutputError` second.",
  "The 9 test function bodies in `tests/core/test_error_egress.py` (`test_redact_credentials_redacts_embedded_url_token`, `test_redact_credentials_redacts_bare_classic_tokens`, `test_redact_credentials_redacts_fine_grained_pat`, `test_redact_credentials_preserves_non_secret_text`, `test_redact_credentials_is_idempotent`, `test_build_error_event_redacts_token_in_error_field`, `test_build_error_event_redacts_token_in_stderr_tail_field`, `test_build_error_event_no_structured_output_carries_raise_site`, `test_build_error_event_preserves_non_secret_message`) and the module-level fixtures `_FAKE_GHP_BODY`, `_FAKE_PAT_BODY`, `_FAKE_URL` are byte-identical to those in the deleted `tests/core/test_soft_failure.py`. The fixture-construction S105/S106-avoidance comment at lines 22-25 is preserved verbatim. The only diffs against the deleted file are (a) the module docstring at line 1 (file-rename consequence), (b) the import block at lines 14-19.",
  "The local variable `soft_failure = NoStructuredOutputError(...)` at `tests/api/v1/test_agent.py:337` and its consumer at line 344 (`FakeRaisingExecutor(soft_failure)`) are UNTOUCHED. Only line 334's import statement is rewritten. Verified by `git grep -n 'soft_failure = NoStructuredOutputError' tests/api/v1/test_agent.py` still matching.",
  "The three prose comments in `src/kodezart/types/domain/agent.py` (lines 22, 23, 237) are rewritten to reference `core/errors.py` (where `NoStructuredOutputError` now lives) and `core/errors.NoStructuredOutputError`. The `RaiseSite = Literal[...]` definition at lines 27-36 and the `ErrorEvent` Pydantic class at lines 224-254 are byte-unchanged. `git grep -n 'soft_failure' src/kodezart/types/domain/agent.py` returns empty.",
  "`src/kodezart/core/__init__.py` is byte-unchanged (`git diff src/kodezart/core/__init__.py` is empty); no aggregate re-export of the three new modules at the package level. The single-line docstring `\"\"\"Core infrastructure: config, logging, protocols.\"\"\"` is preserved.",
  "No consumer imports a moved symbol from a module that merely re-exports it. Specifically: (a) `git grep -nE 'from kodezart\\.core\\.error_egress import [^()]*NoStructuredOutputError' src/ tests/` returns empty; (b) `git grep -nE 'from kodezart\\.core\\.errors import [^()]*(drain|build_error_event|_redact_credentials)' src/ tests/` returns empty; (c) `git grep -nE 'from kodezart\\.core\\.stream_drain import [^()]*(NoStructuredOutputError|build_error_event|_redact_credentials)' src/ tests/` returns empty.",
  "Each of `core/errors.py`, `core/stream_drain.py`, `core/error_egress.py` opens with a single-line module docstring on line 1 (matching the sibling convention at `core/retry.py:1`, `core/logging.py:1`, `core/__init__.py:1`). Verification: `awk 'NR==1' src/kodezart/core/errors.py src/kodezart/core/stream_drain.py src/kodezart/core/error_egress.py` shows one self-terminated `\"\"\"…\"\"\"` per file.",
  "No `# noqa`, `# type: ignore`, `# ruff: noqa`, or rule-suppression comment is introduced in the three new src modules or in `tests/core/test_error_egress.py` (`grep -nE '# (noqa|type: ignore|ruff:)' src/kodezart/core/errors.py src/kodezart/core/stream_drain.py src/kodezart/core/error_egress.py tests/core/test_error_egress.py` returns empty).",
  "No `from __future__ import annotations` is added to any of `core/errors.py`, `core/stream_drain.py`, `core/error_egress.py` (matches the current soft_failure.py posture).",
  "No `__all__` declaration appears in any of the three new modules (`grep -n '^__all__' src/kodezart/core/errors.py src/kodezart/core/stream_drain.py src/kodezart/core/error_egress.py` returns empty).",
  "Symbol set in each new module is exactly the listed surface: `core/errors.py` top-level defs = `{NoStructuredOutputError}`; `core/stream_drain.py` top-level defs = `{drain}`; `core/error_egress.py` top-level defs/constants = `{_REDACTION_SENTINEL, _CREDENTIAL_URL_PATTERN, _GH_TOKEN_PATTERN, _GH_FINEGRAINED_PAT_PATTERN, _redact_credentials, build_error_event}`. No \"just in case\" additions, no extra exception classes, no helper functions, no new Protocols.",
  "Symbol names are unchanged across the move: `_redact_credentials` and `_REDACTION_SENTINEL` retain their leading underscores; the three pattern constant names are unchanged. No public-rename refactor is folded in.",
  "`Makefile`, `pyproject.toml`, `.github/workflows/check.yml`, and any other build/CI config file are byte-unchanged (`git diff` against each is empty). No new package dependency is added to `pyproject.toml`.",
  "Runtime import smoke test passes: `uv run python -c \"from kodezart.core.errors import NoStructuredOutputError; from kodezart.core.stream_drain import drain; from kodezart.core.error_egress import _REDACTION_SENTINEL, _CREDENTIAL_URL_PATTERN, _GH_TOKEN_PATTERN, _GH_FINEGRAINED_PAT_PATTERN, _redact_credentials, build_error_event; print('OK')\"` exits 0 and prints `OK`.",
  "Runtime negative smoke test passes: `uv run python -c \"from kodezart.core import soft_failure\"` raises `ModuleNotFoundError` (the old module is unreachable — no fallback / no shim).",
  "`tests/core/test_retry.py` still passes — verifies the retry-policy contract `should_retry(NoStructuredOutputError(...)) is False` survives the move (`uv run pytest tests/core/test_retry.py` exits 0).",
  "`tests/core/test_error_egress.py::test_build_error_event_no_structured_output_carries_raise_site` passes — `build_error_event(NoStructuredOutputError(...))` still produces an `ErrorEvent` with the primitive `raise_site` and `ResultEvent`-derived fields carried through (`uv run pytest tests/core/test_error_egress.py -v` exits 0 with 9 tests passing).",
  "The whole pytest suite passes with no regressions and no new skips: `uv run pytest` exits 0 with the same passing-test count as the pre-refactor baseline plus the 9 renamed tests."
]