[
  "[behavioral] In `src/kodezart/chains/ralph_workflow.py::_fix_code_node`, the production body invokes `await self._run_quality_gate(...)` exactly once. Verified by `grep -nE '_run_quality_gate|stream_workflow' src/kodezart/chains/ralph_workflow.py` showing the `stream_workflow` symbol no longer appears anywhere in the file body, and `_run_quality_gate` appears at exactly two call sites (`_run_ralph_loop_node` and `_fix_code_node`).",
  "[behavioral] The new `_run_quality_gate(...)` call inside `_fix_code_node` passes exactly these 10 keyword arguments and no others: `prompt`, `repo_path`, `repo_url`, `feature_branch`, `ralph_branch`, `base_branch`, `permission_mode`, `allowed_tools`, `acceptance_criteria`, `cache_key`. Verified by inspecting the diff: no `branch_name=`, no `create_branch=`, no `**kwargs`, no positional args.",
  "[behavioral] In `_fix_code_node`, the `_run_quality_gate(...)` call uses `feature_branch=state[\"feature_branch\"]`, `ralph_branch=fix_branch`, AND `base_branch=state[\"feature_branch\"]` (the feature branch, NOT `ctx.base_branch`/`\"main\"`). The fix-round inner evaluator's diff base must therefore be the merged feature tip. Verified by inspecting the call site source.",
  "[behavioral] The `_run_quality_gate(...)` return value in `_fix_code_node` is NOT bound to a name (the `WorkflowIterationEvent` is intentionally discarded). Verified by `grep -nE 'await self\\._run_quality_gate' src/kodezart/chains/ralph_workflow.py` showing the fix-node call is a bare-`await` statement with no `= await` or `last_iteration_event = await` binding.",
  "[behavioral] Inside `_fix_code_node`, BOTH return dicts (the DIVERGENT/SOURCE_MISSING failure branch and the FAST_FORWARDED/ALREADY_INTEGRATED success branch) are byte-identical to their pre-change form. Neither return dict contains the key `\"accepted\"`. Verified by `git diff src/kodezart/chains/ralph_workflow.py` showing the two `return {...}` literals unchanged, AND by `grep -nA 15 'def _fix_code_node' src/kodezart/chains/ralph_workflow.py | grep -c '\"accepted\"'` returning 0.",
  "[behavioral] In `_fix_code_node`, the `merger.consolidate(...)` call and the subsequent `WorkflowConsolidationEvent` writer emission remain in place (otherwise fix-branch commits would never land on the feature branch). Verified by `grep -nE 'self\\._merger\\.consolidate|WorkflowConsolidationEvent' src/kodezart/chains/ralph_workflow.py` showing both still present inside `_fix_code_node`.",
  "[behavioral] In `_fix_code_node`, the pre-call invariants are preserved unchanged: the docstring, `ctx = ExecutionContext.from_configurable(config)`, `writer = get_stream_writer()`, `fix_branch = generate_ralph_branch_name(state['feature_branch'])`, the `ticket is None` and `feature_tip_sha is None` precondition raises, `pre_fix_head_sha = state['feature_tip_sha']`, and the `fix_prompt` assembly are all byte-identical to pre-change.",
  "[behavioral] After the change, downstream SSE consumers receive at least one `WorkflowIterationEvent` per fix round (in addition to assistant/tool events), because `_run_quality_gate` calls `writer(event)` on every inner event. Verified by collecting `[e async for e in engine.run(...)]` in the new test and asserting at least two `WorkflowIterationEvent` instances appear in the stream (pre-merge + fix round).",
  "[no-overwrite-invariant] When the pre-merge quality gate yields `accepted=True` and the fix-path quality gate yields `accepted=False`, the terminal `WorkflowCompleteEvent.accepted` is `True` (the pre-merge verdict is preserved). Verified by the new test asserting `complete_events[0].accepted is True` after scripting the gate with `[passing, failing]` evaluations and observing the workflow run end-to-end.",
  "[no-overwrite-invariant] `state[\"accepted\"]` is written exactly once across the workflow, by `_run_ralph_loop_node` (the pre-merge node). Verified by `grep -nE 'state\\[\"accepted\"\\][[:space:]]*=|\"accepted\":' src/kodezart/chains/ralph_workflow.py` showing the only write site is inside `_run_ralph_loop_node`'s return dict — `_fix_code_node` introduces zero new write sites for this key.",
  "[no-overwrite-invariant] `_route_after_fix` (note: NOT `_route_after_fix_code` as the prompt prose says) remains byte-identical and routes only on `state[\"merge_error\"]` and `state[\"pr_url\"]`, never reading `state[\"accepted\"]`. Verified by `git diff src/kodezart/chains/ralph_workflow.py` showing zero changes inside `_route_after_fix`'s body.",
  "[no-overwrite-invariant] `_complete_node` continues to read `state[\"accepted\"]` for both `WorkflowCompleteEvent.accepted = state[\"accepted\"]` and the `cleanup_backup_branches` gate (`if state[\"accepted\"] and state[\"merged\"]:`). Verified by `git diff src/kodezart/chains/ralph_workflow.py` showing zero changes inside `_complete_node`.",
  "[test] A new test-local class `_SequentialQualityGate` is added to `tests/chains/test_ralph_workflow.py`, mirroring the `_SequentialReviewExecutor` precedent (stateful list of scripted results + `.calls: list[dict[str, object]]` recorder + single-purpose). Verified by `grep -nE 'class _SequentialQualityGate' tests/chains/test_ralph_workflow.py` returning exactly one definition, and `grep -rn '_SequentialQualityGate' tests/` returning at most one definition and one instantiation site (the new test).",
  "[test] `_SequentialQualityGate.run` is `async def`, accepts keyword-only the 10 `QualityGate.run` protocol parameters (`prompt`, `repo_path`, `repo_url`, `feature_branch`, `ralph_branch`, `base_branch`, `permission_mode`, `allowed_tools`, `acceptance_criteria`, `cache_key`), declares no `**kwargs` catchall, records all 10 kwargs into `self.calls`, pops the next scripted `AcceptanceCriteriaOutput`, and yields one `WorkflowIterationEvent` per call. Verified by reading the class definition.",
  "[test] `_SequentialQualityGate` is NOT promoted into `tests/fakes.py` and `FakeQualityGate` in `tests/fakes.py` is NOT extended or otherwise modified. Verified by `git diff tests/fakes.py` being empty.",
  "[test] Exactly ONE new async test function is added to `tests/chains/test_ralph_workflow.py`, placed between `test_fix_code_node_source_missing_routes_terminally` and `test_review_uses_review_base_sha_and_review_head_sha_not_branch_refs`. Verified by `grep -n 'def test_' tests/chains/test_ralph_workflow.py` ordering.",
  "[test] The new test uses direct `RalphWorkflowEngine(...)` construction (NOT the `_make_engine_with_executor` factory) so the test holds direct references to both the `_SequentialQualityGate` and `_SequentialReviewExecutor` instances. Verified by reading the new test source: it constructs the engine in-test, mirroring the `test_workflow_review_fails_triggers_fix` precedent.",
  "[test] The new test scripts the gate with two evaluations: `_SequentialQualityGate(evaluations=[make_passing_evaluation(), make_failing_evaluation()])` so the pre-merge call returns `accepted=True` (lets the merge proceed) and the fix-path call returns `accepted=False` (the would-be-regression case). Both are required to make the no-overwrite invariant observable.",
  "[test] The new test wires `FakeBranchMerger(consolidation_outcomes=[ConsolidationOutcome(status=ConsolidationStatus.FAST_FORWARDED, feature_tip_sha='a'*40), ConsolidationOutcome(status=ConsolidationStatus.FAST_FORWARDED, feature_tip_sha='c'*40)])` (explicit, two outcomes) matching the `test_fix_code_node_divergent_routes_*` family construction precedent.",
  "[test] The new test wires `_SequentialReviewExecutor(review_results=[failing_review, passing_review])` to drive the review-fail-then-pass topology that triggers `_fix_code_node` (otherwise the fix path is never entered).",
  "[test] The new test asserts `len(gate.calls) == 2` — proving exactly two `_run_quality_gate` invocations across the workflow (pre-merge loop + fix path).",
  "[test] The new test asserts fix-path kwargs on `gate.calls[1]`: `fix_call['base_branch'] == fix_call['feature_branch']`; `fix_call['base_branch'].startswith('kodezart/')`; `fix_call['acceptance_criteria'] == ['Tests pass', 'No lint errors']`; `fix_call['ralph_branch'].startswith(fix_call['feature_branch'])`; `'-ralph-' in fix_call['ralph_branch']`. Each assertion is a separate statement.",
  "[test] The new test asserts (via `gate.calls[0]['base_branch'] == 'main'`) that the FIRST gate call (pre-merge) uses `ctx.base_branch == 'main'`, while the SECOND gate call uses `state['feature_branch']` — proving the two call sites pass different bases.",
  "[test] The new test asserts `complete_events = [e for e in events if isinstance(e, WorkflowCompleteEvent)]; assert len(complete_events) == 1; assert complete_events[0].accepted is True` — proving the fix gate's scripted `accepted=False` did NOT overwrite the pre-merge `accepted=True`.",
  "[test] The new test does NOT use `monkeypatch.setattr`, `unittest.mock.patch`, or any private-attribute reassignment on `RalphWorkflowEngine` internals. Verified by `grep -nE 'monkeypatch|mock\\.patch|unittest\\.mock' <new-test-block>` returning zero hits within the new test function body.",
  "[test] The new test imports use only symbols already imported at the top of `tests/chains/test_ralph_workflow.py` (`AsyncGenerator` from `collections.abc`, `WorkflowIterationEvent`, `AcceptanceCriteriaOutput`, `AgentEvent`, `ConsolidationOutcome`, `ConsolidationStatus`, `WorkflowCompleteEvent`, `FakePRCreator`, `FakeCIMonitor`, `FakeBranchMerger`, `FakeTicketGenerator`, `FakeGitService`, `FakeRepoCache`, `FakeWorkspaceProvider`, `FakeChangePersister`, `AgentService`, `RalphWorkflowEngine`, `_SequentialReviewExecutor`, `make_passing_evaluation`, `make_failing_evaluation`). No new top-level imports added unless strictly required.",
  "[regression] `test_workflow_review_fails_triggers_fix` in `tests/chains/test_ralph_workflow.py` passes byte-identical to pre-change source. Under the new routing the existing default `FakeQualityGate(make_passing_evaluation())` it uses is invoked twice (pre-merge + fix), both passing, preserving the original review-fail-then-pass observation. Verified by running pytest with `-k test_workflow_review_fails_triggers_fix -v` and checking the test file's git diff has zero edits inside that test body.",
  "[regression] All four `test_fix_code_node_*_routes_*` tests (`divergent_routes_to_comment_failure_when_pr_url_set`, `divergent_routes_to_complete_when_no_pr_url`, `already_integrated_does_not_raise_advances_fix_round`, `source_missing_routes_terminally`) pass byte-identical to pre-change source. None reads `state['accepted']` for routing; all depend only on merger-outcome scripting which is unchanged. Verified by running pytest filtered to these names and by checking their diff is empty.",
  "[regression] [sherlock] `test_workflow_ci_fails_then_passes_after_fix` (currently asserts at lines ~2013–2016 that `executor.calls` contains a fix prompt with `\"## CI Failures\\nCI failed: lint\"`) is EITHER updated to read the fix prompt from `gate.calls[1]['prompt']` (because under the new routing the fix prompt is dispatched through the quality gate, not the executor) OR equivalently kept observable. The test must continue to assert the CI-summary content survives in the fix prompt. Verified by running pytest with `-k test_workflow_ci_fails_then_passes_after_fix -v` and confirming the assertion still pins the `\"## CI Failures\"` substring against the fix-round prompt, regardless of dispatch channel. (This existing test is the ONE blast-radius regression Watson 5 surfaced — it is in-scope because it covers the same code path being changed.)",
  "[regression] All other existing tests in `tests/chains/test_ralph_workflow.py` and across the entire test suite pass unchanged. Verified by `make test` exiting 0 with the same passing test count plus 1 (the new test).",
  "[anti-pattern] NO graceful-degradation event-swallowing: the `async for _event in self._service.stream_workflow(...): pass` pattern is GONE from `_fix_code_node`. Verified by `grep -nE 'async for[^:]*stream_workflow|: pass[[:space:]]*#[[:space:]]*consume' src/kodezart/chains/ralph_workflow.py` returning zero hits.",
  "[anti-pattern] NO fabricated success signals: no hardcoded `accepted=True` literal appears in any production return dict touched by this change. The terminal `WorkflowCompleteEvent.accepted` value flows from real quality-gate verification recorded in `state['accepted']`. Verified by grepping the diff for `\"accepted\":[[:space:]]*True` or `accepted=True` literal-assignment patterns in production code — zero hits.",
  "[anti-pattern] NO silent error swallowing: the `_run_quality_gate` helper raises `RuntimeError(\"Ralph loop completed without emitting an iteration event.\")` if the inner gate yields zero iteration events, and the fix-node call does NOT wrap that in `try/except`. Verified by `grep -nB 1 -A 3 'await self\\._run_quality_gate' src/kodezart/chains/ralph_workflow.py` showing both call sites are bare awaits with no surrounding try blocks.",
  "[anti-pattern] NO fallback defaults masking broken inputs: the fix-node passes `state['acceptance_criteria']` and `state['feature_branch']` directly with NO `or []`, `or \"\"`, `or None`, or `dict.get` defaulting. Verified by inspection of the new call site.",
  "[anti-pattern] NO mocks or hardcoded fakes appear in production code. `_SequentialQualityGate` is exclusively defined inside `tests/chains/test_ralph_workflow.py`. Verified by `grep -rn '_SequentialQualityGate\\|_SequentialReviewExecutor\\|FakeQualityGate' src/` returning zero hits.",
  "[anti-pattern] NO preconditions are added or relocated to business logic. The existing `ticket is None` / `feature_tip_sha is None` precondition raises at the top of `_fix_code_node` are unchanged. No precondition is introduced inside the new `_run_quality_gate` call or inside `merger.consolidate`. Verified by `git diff` of the precondition block being empty.",
  "[anti-pattern] NO YAGNI introductions. Zero new production files. Zero new test files. Zero new public functions/methods on `RalphWorkflowEngine`. Zero new fields on `WorkflowState`, `ExecutionContext`, or any event type. Zero new parameters on `_run_quality_gate`, `_fix_code_node`, or `_route_after_fix`. `_SequentialQualityGate` has exactly one consumer (the single new test). Verified by `git status` showing changes only in `src/kodezart/chains/ralph_workflow.py` and `tests/chains/test_ralph_workflow.py`, and by `grep -rn _SequentialQualityGate` showing exactly two hits (definition + one instantiation).",
  "[anti-pattern] The dropped kwargs `branch_name` and `create_branch` are NOT silently re-introduced via a `**kwargs` catchall on `_run_quality_gate`. Verified by reading the `_run_quality_gate` signature: 10 keyword-only parameters, no `**kwargs`.",
  "[anti-pattern] NO untyped cross-component string-literal duplication is introduced. The new `_run_quality_gate` call uses TypedDict access (`state['feature_branch']`, `state['acceptance_criteria']`) and `ctx` attribute access — no raw `'main'`/`'origin'`/`'feature_branch'`-as-protocol-literal strings. Verified by `make verify-no-origin-literal` exiting 0 and by inspecting the diff for new string-literal contracts.",
  "[architecture] The production code change is contained ENTIRELY within `src/kodezart/chains/ralph_workflow.py::_fix_code_node`. No other production file under `src/` is modified. Verified by `git diff --stat src/` showing exactly one file changed (`src/kodezart/chains/ralph_workflow.py`).",
  "[architecture] The diff to `src/kodezart/chains/ralph_workflow.py` introduces NO new imports — neither from `kodezart.adapters.*`, nor from `kodezart.services.*`, nor any infrastructure SDK (`subprocess`, `httpx`, `claude_agent_sdk`, etc.). Verified by `git diff src/kodezart/chains/ralph_workflow.py | grep -E '^\\+\\s*(import|from)'` returning zero hits.",
  "[architecture] The graph topology in `RalphWorkflowEngine._build_graph` (every `add_node`, `add_edge`, `add_conditional_edges` call) is byte-identical to pre-change. The `_max_fix_rounds` default and its comparison in `_route_after_review` are unchanged. Verified by `git diff` of the `_build_graph` body and `_route_after_review` body being empty.",
  "[architecture] `WorkflowState` (TypedDict in `src/kodezart/types/domain/workflow.py`) is NOT modified — no new field added. Verified by `git diff src/kodezart/types/domain/workflow.py` being empty.",
  "[architecture] `QualityGate` protocol in `src/kodezart/core/protocols.py` is NOT modified — no new method, no new parameter, no signature change. Verified by `git diff src/kodezart/core/protocols.py` being empty.",
  "[architecture] `_run_quality_gate` helper method in `src/kodezart/chains/ralph_workflow.py` has its signature byte-identical to pre-change (10 keyword-only params, returns `WorkflowIterationEvent`, raises `RuntimeError` on missing terminal event). Verified by `git diff` of the helper body being limited to whitespace at most.",
  "[architecture] `_SequentialQualityGate` is structurally substitutable for the `QualityGate` protocol — at the constructor boundary, the engine accepts it where it expects a `QualityGate`. The substitution is via the existing `quality_gate=` constructor parameter, NOT via private attribute or method patching.",
  "[tooling] `make check` exits 0 after the change. This runs (in order): `verify-no-origin-literal`, `lint`, `type-check`, `test`. Verified by running `make check` and confirming exit code 0.",
  "[tooling] `make lint` (`uv run ruff check src/ tests/`) reports zero violations on all changed files. No `# noqa`, no `# type: ignore`, no per-file ignore additions in production code. The full rule set `E, W, F, I, B, C4, UP, N, ANN, S, A, ARG, RUF` is active. Verified by `uv run ruff check src/kodezart/chains/ralph_workflow.py tests/chains/test_ralph_workflow.py` exiting 0.",
  "[tooling] `make type-check` (`uv run mypy src/`) passes with zero errors. The project's mypy config is `strict = true` + `disallow_any_explicit = true` + pydantic plugin with `init_forbid_extra = true`. The change introduces no explicit `Any`, no untyped function, no untyped call. Verified by `uv run mypy src/kodezart/chains/ralph_workflow.py` exiting 0.",
  "[tooling] `make test` (`uv run pytest`) passes with zero failures. The pre-existing test count + 1 (the new test) all pass. The new async test relies on `asyncio_mode = 'auto'` and carries NO `@pytest.mark.asyncio` decorator. Verified by `grep -A 2 'def test_fix_code_node_invokes_quality_gate_with_feature_base_branch' tests/chains/test_ralph_workflow.py` showing no marker decorator above the function.",
  "[tooling] `make verify-no-origin-literal` exits 0 — the change introduces no `\"origin\"`/`\"origin/\"` string literals into `src/kodezart`.",
  "[boundary-typing] [sherlock] All data crossing the `_fix_code_node` → `_run_quality_gate` → `_SequentialQualityGate` boundary flows through typed domain models. The fake yields a real `WorkflowIterationEvent` (Pydantic model from `src/kodezart/types/domain/agent.py`) — NOT a raw dict. The recorded `gate.calls[i]` dicts are test-internal observation only; production data crossing the protocol boundary remains fully typed. Verified by reading `_SequentialQualityGate.run`: its `yield` target is `WorkflowIterationEvent(...)`.",
  "[boundary-typing] [sherlock] The new test's assertions on `WorkflowCompleteEvent.accepted` use attribute access (`complete_events[0].accepted`) on a Pydantic-typed event filtered via `isinstance(e, WorkflowCompleteEvent)` — NOT dict-key probing. Verified by reading the new test's assertion block.",
  "[fix-prompt-observability] [sherlock] The fix-prompt content carrying review feedback (and CI summary, when present) is observable downstream of the routing change via `gate.calls[1]['prompt']`. The new test asserts the fix-path prompt contains the review-failure content the executor would previously have seen. Verified by `assert '## Review Failures' in str(gate.calls[1]['prompt'])` (or equivalent substring assertion proving the routed-through-gate path preserves the fix-prompt assembly).",
  "[architecture] [sherlock] After the change, `self._service.stream_workflow` has exactly ONE production call site (inside `_run_ralph_loop_node` if any — verify by grep) OR zero call sites in `ralph_workflow.py` (if the helper is the sole user). Either way, `_fix_code_node` no longer carries an independent `stream_workflow` integration. Verified by `grep -nE 'self\\._service\\.stream_workflow' src/kodezart/chains/ralph_workflow.py` and reading each remaining hit to confirm it is NOT in `_fix_code_node`."
]