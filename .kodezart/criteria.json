[
  "[obs/architecture] A new file `src/kodezart/core/soft_failure.py` exists and is the SOLE location of `RaiseSite`, `SoftFailureError`, and `drain`. Verify: `find src/kodezart -name '*soft_failure*'` returns exactly `src/kodezart/core/soft_failure.py` (no `chains/_soft_failure.py`, no `adapters/_soft_failure.py`).",
  "[obs/architecture] `core/soft_failure.py` exports `RaiseSite = Literal[\"ticket_creator\", \"ticket_reviewer\", \"branch_name\", \"acceptance_criteria\", \"ralph_evaluator\", \"post_merge_review\", \"pr_description\", \"commit_message\"]` — exactly eight literals, in any order. Verify: `python -c \"from kodezart.core.soft_failure import RaiseSite; import typing; print(sorted(typing.get_args(RaiseSite)))\"` outputs the sorted list of those eight strings.",
  "[obs/architecture] `core/soft_failure.py` defines `class SoftFailureError(Exception)` that is NOT a subclass of `AgentSDKError`, `TransientAPIError`, or `RateLimitError`. Verify: `python -c \"from kodezart.core.soft_failure import SoftFailureError; from kodezart.domain.errors import AgentSDKError, TransientAPIError, RateLimitError; assert not issubclass(SoftFailureError, (AgentSDKError, TransientAPIError, RateLimitError))\"` exits 0.",
  "[obs/architecture] `SoftFailureError.__init__` is keyword-only for metadata and stores ONLY primitive scalars plus a `RaiseSite` literal — no `ResultEvent`, `RateLimitWarningEvent`, or other event-object reference is retained as an instance attribute after construction. Verify: instantiate `SoftFailureError(\"m\", raise_site=\"ticket_creator\", result_event=ResultEvent(...rejected=True), rate_limit_rejected=True)`; `vars(exc)` contains only `raise_site`, `rate_limit_rejected`, `result_event_observed`, `stop_reason`, `is_error`, `session_id`, `total_cost_usd` — no `result_event` attribute holding the object.",
  "[obs/architecture] `core/soft_failure.py` defines `async def drain(stream: AsyncIterator[AgentEvent]) -> tuple[ResultEvent | None, bool]` and the function body contains NO `yield` statement (it is a coroutine, not an async generator). Verify: `python -c \"import ast, pathlib; tree=ast.parse(pathlib.Path('src/kodezart/core/soft_failure.py').read_text()); fn=[n for n in ast.walk(tree) if isinstance(n, ast.AsyncFunctionDef) and n.name=='drain'][0]; assert not any(isinstance(x,(ast.Yield,ast.YieldFrom)) for x in ast.walk(fn))\"` exits 0. [sherlock]",
  "[obs/architecture] `core/soft_failure.py` imports nothing from `kodezart.adapters.*`, `kodezart.chains.*`, `kodezart.services.*`, `kodezart.handlers.*`, or `claude_agent_sdk`. Verify: `grep -E '^from kodezart\\.(adapters|chains|services|handlers)|^import claude_agent_sdk' src/kodezart/core/soft_failure.py` returns no lines.",
  "[obs/architecture] `src/kodezart/domain/errors.py` MUST NOT import from `kodezart.types.domain.*` or any other `kodezart.*` module. Verify: `grep -E '^from kodezart|^import kodezart' src/kodezart/domain/errors.py` returns no lines.",
  "[obs/architecture] `domain/errors.py::AgentSDKError.__init__` accepts keyword-only `exit_code: int | None = None` and `stderr_tail: str | None = None` and stores them as instance attributes. Verify: `python -c \"from kodezart.domain.errors import AgentSDKError; e=AgentSDKError('m', error_kind='ProcessError', exit_code=137, stderr_tail='tail'); assert e.exit_code==137 and e.stderr_tail=='tail'\"` exits 0.",
  "[obs/wire] `types/domain/agent.py::ErrorEvent` adds exactly these top-level fields, each `T | None = None`: `error_kind: str | None`, `cause_class: str | None`, `stop_reason: str | None`, `raise_site: RaiseSite | None`, `rate_limit_rejected: bool | None`, `exit_code: int | None`, `stderr_tail: str | None`. NO `details: dict[str, object]` field is added anywhere on `ErrorEvent`. Verify: `python -c \"from kodezart.types.domain.agent import ErrorEvent; schema=ErrorEvent.model_json_schema(); props=schema['properties']; assert {'error','error_kind','cause_class','stop_reason','raise_site','rate_limit_rejected','exit_code','stderr_tail'}.issubset(set(props)); assert 'details' not in props\"` exits 0.",
  "[obs/wire] `ErrorEvent` is locked with `model_config = ConfigDict(extra='forbid')` (either declared inline or strictly inherited). Verify: `python -c \"from pydantic import ValidationError; from kodezart.types.domain.agent import ErrorEvent; import pytest; raised=False\\ntry: ErrorEvent(error='x', made_up_field=1)\\nexcept ValidationError: raised=True\\nassert raised\"` exits 0.",
  "[obs/wire] `ErrorEvent.raise_site` is typed as `RaiseSite | None`, not `str | None`. Verify: `python -c \"from kodezart.types.domain.agent import ErrorEvent; import typing; hints=typing.get_type_hints(ErrorEvent); rs=hints['raise_site']; assert 'RaiseSite' in str(rs) or 'Literal' in str(rs)\"` exits 0.",
  "[obs/raise sites] All EIGHT soft-failure raise sites route through `await drain(stream)` and `raise SoftFailureError(...)`. Verify: `grep -rn 'from kodezart.core.soft_failure import' src/kodezart/chains src/kodezart/adapters/git_change_persister.py` lists exactly these files: `chains/ticket_generation.py`, `chains/ralph_loop.py`, `chains/ralph_workflow.py`, `adapters/git_change_persister.py`. AND `grep -rn 'raise SoftFailureError(' src/kodezart/` returns exactly 8 lines.",
  "[obs/raise sites] Each `RaiseSite` literal value appears at exactly ONE raise site in production code (no value reused, no value missing). Verify: for each `v in {ticket_creator, ticket_reviewer, branch_name, acceptance_criteria, ralph_evaluator, post_merge_review, pr_description, commit_message}`, `grep -rn -F 'raise_site=\"'$v'\"' src/kodezart/` returns exactly 1 line.",
  "[obs/raise sites] The 8th raise site at `src/kodezart/chains/ralph_loop.py` (evaluator node, near line 226-228) is included — it raises `SoftFailureError(..., raise_site=\"ralph_evaluator\", ...)`. Verify: `grep -n 'raise_site=\"ralph_evaluator\"' src/kodezart/chains/ralph_loop.py` returns at least 1 line. [sherlock — Watson 2 confirmed via direct git show; missing this site would be a critical observability gap]",
  "[obs/raise sites] No bare `raise RuntimeError(` remains at the lines/contexts the ticket targets — i.e., none of the 8 sites still raise `RuntimeError` for the soft-failure precondition `result_event is None or result_event.structured_output is None`. Verify: `grep -n 'raise RuntimeError(\"Agent did not produce structured output\\|raise RuntimeError(\"Evaluator produced no structured output\\|raise RuntimeError(\"Creator produced no structured output\\|raise RuntimeError(\"Reviewer produced no structured output' src/kodezart/chains src/kodezart/adapters/git_change_persister.py` returns no lines.",
  "[obs/raise sites] The manual `for event in stream: if isinstance(event, ResultEvent): result_event = event` accumulator is collapsed by `drain` at each of the 8 sites. Verify: total count of `isinstance(event, ResultEvent)` in the 8 affected files DROPS by at least 8 vs. main. Concretely: `grep -c 'isinstance(event, ResultEvent)' src/kodezart/chains/ticket_generation.py src/kodezart/chains/ralph_loop.py src/kodezart/chains/ralph_workflow.py src/kodezart/adapters/git_change_persister.py | awk -F: '{s+=$2} END {print s}'` produces a number at least 8 lower than the pre-PR count (the `_execute_node` accumulator at `ralph_loop.py:176` capturing `commit_sha` is preserved — it is NOT a soft-failure site).",
  "[obs/handler] `handlers/agent_handler.py::stream_query` (~L53-58) and `stream_workflow` (~L79-84) both: (a) call `await self._log.aexception(\"stream_failed\", error=str(exc), error_kind=type(exc).__name__, ..., exc_info=sys.exc_info())` passing `exc_info` EXPLICITLY; (b) build `ErrorEvent` via explicit isinstance branches on `SoftFailureError` and `AgentSDKError` — NO `getattr(exc, ...)` access. Verify: `grep -n 'exc_info=sys.exc_info()' src/kodezart/handlers/agent_handler.py` returns at least 2 lines; `grep -n 'getattr(exc' src/kodezart/handlers/agent_handler.py` returns no lines.",
  "[obs/handler] When `SoftFailureError` is caught the emitted `ErrorEvent` carries `raise_site`, `stop_reason`, `rate_limit_rejected`; when `AgentSDKError` is caught it carries `exit_code`, `stderr_tail`. Verify by running the wire-shape tests in `tests/api/v1/test_agent.py` (`test_error_event_carries_exception_class_on_runtime_path`, `test_error_event_carries_soft_failure_payload`) — both pass.",
  "[obs/handler] The `ErrorEvent.error` string field MUST NOT contain a Python traceback. Tracebacks go on the log record via `exc_info`, not on the wire. Verify: in the wire-shape tests, `assert 'Traceback (most recent call last)' not in event['error']` and `assert '\\n  File \"' not in event['error']`. [sherlock — ticket pins it; no Watson made it a standalone criterion]",
  "[obs/adapter] `adapters/claude_client_executor.py` defines a module-level `_STDERR_TAIL_BYTES: int = 4096` with a comment justifying the cap as a wire-shape invariant tied to upstream SSE framing limits. Verify: `grep -n '_STDERR_TAIL_BYTES' src/kodezart/adapters/claude_client_executor.py` returns the definition line; `grep -rn '_STDERR_TAIL_BYTES' src/kodezart/` returns exactly two paths (definition + slice site) and neither is in `core/config.py`, `core/constants.py`, or `domain/`.",
  "[obs/adapter] The `ProcessError` re-raise in `claude_client_executor.py` is None-safe: `stderr_tail = exc.stderr[:_STDERR_TAIL_BYTES] if exc.stderr is not None else None`. Verify: `tests/adapters/test_claude_executors.py::test_process_error_with_none_stderr_does_not_crash` passes, and `test_process_error_round_trips_exit_code_and_stderr_on_re_raise` asserts `exc.exit_code == 137`, `\"<known-tail>\" in exc.stderr_tail`, AND `len(exc.stderr_tail) <= 4096`.",
  "[obs/adapter] `adapters/claude_agent_executor.py` is unchanged except for a single TODO comment above the L60-87 except block. Verify: `git diff main...HEAD -- src/kodezart/adapters/claude_agent_executor.py` shows only a comment-line addition (no behavior change).",
  "[cfg/inheritance] Commits 19aca0d and 2e7cfa8 are present in this PR's history (rebased from sibling branch `kodezart/extract-git-remote-to-config-04317d19-ralph-56a0c08e`). Verify: `git log --oneline main..HEAD --format='%s'` contains both subject lines `refactor: extract git remote name to KODEZART_GIT_REMOTE config` and `test(e2e): cover git_remote threading in workflow argv and errors`. [sherlock — inheritance gate, no Watson verified the actual log presence]",
  "[cfg/inheritance] After rebase, `core/config.py` declares `git_remote: str = Field(default=\"origin\", ...)`. Verify: `python -c \"from kodezart.core.config import AppConfig; assert AppConfig().git_remote == 'origin'\"` exits 0; AND `KODEZART_GIT_REMOTE=upstream python -c \"from kodezart.core.config import AppConfig; assert AppConfig().git_remote == 'upstream'\"` exits 0.",
  "[cfg/inheritance] No `_REMOTE = \"origin\"` module constant survives in `src/kodezart/`. Verify: `grep -rn '^_REMOTE\\s*=\\s*\"origin\"' src/kodezart/` returns no lines.",
  "[cfg/inheritance] `SubprocessGitService`, `GitChangePersister`, `GitBranchMerger` constructors all accept keyword-only `remote: str`; `RalphWorkflowEngine` accepts keyword-only `git_remote: str`. Verify: `python -c \"import inspect; from kodezart.adapters.subprocess_git_service import SubprocessGitService; from kodezart.adapters.git_change_persister import GitChangePersister; from kodezart.adapters.git_branch_merger import GitBranchMerger; from kodezart.chains.ralph_workflow import RalphWorkflowEngine; assert 'remote' in inspect.signature(SubprocessGitService.__init__).parameters; assert 'remote' in inspect.signature(GitChangePersister.__init__).parameters; assert 'remote' in inspect.signature(GitBranchMerger.__init__).parameters; assert 'git_remote' in inspect.signature(RalphWorkflowEngine.__init__).parameters\"` exits 0.",
  "[cfg/inheritance] `Makefile` target `verify-no-origin-literal` is wired into the `check` target. Verify: `grep -E 'verify-no-origin-literal' Makefile` returns at least 2 lines (the target definition AND its appearance as a dependency of `check`), and `make verify-no-origin-literal` exits 0 on the PR tree.",
  "[cfg/inheritance] No production code under `src/kodezart/` (excluding `core/config.py`) contains the literal string `\"origin\"` or `\"origin/\"`. Verify: `make verify-no-origin-literal` exits 0; equivalently `grep -rn -E '\"origin(/|$|[^_])' src/kodezart/ --exclude-dir=__pycache__ | grep -v 'src/kodezart/core/config.py'` returns no lines.",
  "[anc/refspec] `SubprocessGitService.fetch` argv extends to `[\"git\", \"fetch\", self._remote, f\"+refs/heads/*:refs/remotes/{self._remote}/*\"]`. Verify: `grep -n 'refs/remotes/{self._remote}/\\*' src/kodezart/adapters/subprocess_git_service.py` returns at least 1 line, AND `grep -n '\"git\", \"fetch\"' src/kodezart/adapters/subprocess_git_service.py` shows the 4-element argv form (not the bare 3-element form).",
  "[anc/refspec] `SubprocessGitService.clone_bare` body and `git_branch_merger.consolidate` body and `git_branch_merger.py` lines 99-103 (`is_ancestor` call site) are UNCHANGED in this PR's net diff. Verify: `git diff main...HEAD -- src/kodezart/adapters/git_branch_merger.py | grep -E '^[+-].*is_ancestor'` returns no lines (the `is_ancestor` call site is untouched in the net diff after CFG inheritance).",
  "[anc/refspec] The fetch refspec uses `self._remote` interpolation — NEVER a literal `\"origin\"`. Verify: `grep -n '\"origin\"' src/kodezart/adapters/subprocess_git_service.py` returns no lines (only `self._remote`).",
  "[anc/test] A new test in `tests/adapters/test_subprocess_git.py` (`test_fetch_populates_remote_tracking_refs` or equivalent) creates a real upstream repo, calls `clone_bare` then `fetch`, and asserts `git for-each-ref refs/remotes/origin/` returns at least one ref. Verify: the test exists and passes — `pytest tests/adapters/test_subprocess_git.py-k fetch_populates -v` exits 0 with the test reported PASSED.",
  "[anc/test] A new integration test in `tests/adapters/test_git_branch_merger.py` exercises `consolidate()` against a REAL bare clone (NOT fakes): creates bare upstream, pushes a source branch, calls `consolidate(...)`, asserts the outcome is in `{ConsolidationStatus.FAST_FORWARDED, ConsolidationStatus.ALREADY_INTEGRATED}` and NO `RuntimeError` matching `\"Not a valid object name origin/\"` is raised. Verify: the test exists, uses subprocess git (no FakeGitService), and passes. [sherlock — ticket pins REAL bare clone; no Watson called out the fakes-prohibition gate]",
  "[xfacet/refspec] In `tests/integration/test_workflow_e2e.py`, the `KODEZART_GIT_REMOTE=upstream` argv test asserts BOTH the remote-name slot AND the refspec namespace as a single joint argv: it asserts `[\"git\", \"fetch\", \"upstream\", \"+refs/heads/*:refs/remotes/upstream/*\"]` (or equivalent ordered subseq) appears in captured argvs — NOT just `\"upstream\" in argv`. Verify: pytest runs this test and passes; manual read of the assertion confirms BOTH tokens are checked together.",
  "[xfacet/structured-payload] In `tests/integration/test_workflow_e2e.py`, a new test `test_stream_failed_carries_structured_payload_on_consolidate_failure` drives the workflow through `AgentHandler.stream_workflow`, fault-injects an `is_ancestor` failure with the known exit-128 string, captures the SSE `ErrorEvent`, and asserts `error_kind == \"RuntimeError\"` AND the literal substring `\"exit 128\"` appears in `event[\"error\"]`. NO new `ConsolidationStatus` value is added to re-classify exit-128. Verify: `pytest tests/integration/test_workflow_e2e.py -k stream_failed_carries_structured -v` exits 0 PASSED; AND `grep -E 'ConsolidationStatus\\.' src/kodezart/types/domain/consolidation.py | sort -u` lists exactly the four existing values.",
  "[tests/fixtures] `tests/conftest.py` defines shared fixtures `subprocess_git_service` and `git_branch_merger` that absorb the inherited explicit `remote=\"origin\"` kwarg sites. The previously file-local fixtures in `tests/adapters/test_subprocess_git.py`, `test_git_branch_merger.py`, `test_git_change_persister.py`, `test_git_worktree_provider.py`, `test_git_artifact_persister.py` are REMOVED in the same PR diff (no duplicates remain). Verify: `grep -rn '@pytest.fixture' tests/adapters/test_subprocess_git.py tests/adapters/test_git_branch_merger.py tests/adapters/test_git_change_persister.py tests/adapters/test_git_worktree_provider.py tests/adapters/test_git_artifact_persister.py | grep -E 'def (subprocess_git_service|git_branch_merger)' ` returns no lines, AND `grep -n 'def subprocess_git_service\\|def git_branch_merger' tests/conftest.py` returns 2 lines.",
  "[tests/retry] `tests/core/test_retry.py` includes `test_soft_failure_error_does_not_satisfy_should_retry` asserting `should_retry(SoftFailureError(\"x\", raise_site=\"ticket_creator\", result_event=None)) is False`, and `core/retry.py` is UNCHANGED. Verify: `git diff main...HEAD -- src/kodezart/core/retry.py` shows no production changes; `pytest tests/core/test_retry.py -k soft_failure -v` exits 0 PASSED.",
  "[tests/chains] `tests/chains/test_ticket_generation.py`, `test_ralph_loop.py`, `test_ralph_workflow.py` are updated so that every `pytest.raises(RuntimeError, ...)` site targeting a soft-failure raise (no-structured-output / SOURCE_MISSING) now uses `pytest.raises(SoftFailureError, ...)` with the matching `raise_site` literal assertion. Verify: `pytest tests/chains/ -v` exits 0 PASSED across all updated tests; `grep -n 'pytest.raises(RuntimeError, match=\"Agent did not produce structured\\|pytest.raises(RuntimeError, match=\"no structured output\\|pytest.raises(RuntimeError, match=\"SOURCE_MISSING' tests/chains/` returns no lines.",
  "[tooling/mypy] `mypy --strict` passes with zero errors on all changed files, including `core/soft_failure.py`, `types/domain/agent.py`, `domain/errors.py`, `handlers/agent_handler.py`, `adapters/claude_client_executor.py`, `adapters/subprocess_git_service.py`, `chains/ralph_loop.py`, `chains/ralph_workflow.py`, `chains/ticket_generation.py`, `adapters/git_change_persister.py`. Verify: `make type-check` exits 0. AND no `getattr(exc, ...)` introspection pattern is introduced (returns Any, would violate `disallow_any_explicit`). Verify: `grep -n 'getattr(exc' src/kodezart/handlers/ src/kodezart/chains/ src/kodezart/adapters/` returns no NEW lines vs. main.",
  "[tooling/ruff] `make lint` (or `ruff check src/ tests/`) exits 0 with zero violations on all changed files. NO `# noqa`/`# type: ignore` suppressions are introduced (verify the linter is not disabled or suppressed on production-code lines). Verify: `make lint` exits 0; `git diff main...HEAD -- 'src/kodezart/**'` shows no new `# noqa` or `# type: ignore` comments outside test files.",
  "[tooling/sdk pin] `pyproject.toml` declares `claude-agent-sdk~=0.1.69` (NOT `>=0.1.69`). Verify: `grep -E 'claude-agent-sdk' pyproject.toml` shows `~=0.1.69`; `python -c \"import claude_agent_sdk; help(claude_agent_sdk.ProcessError)\"` confirms the verified ProcessError signature.",
  "[tooling/ci-gate] `.github/workflows/ci.yml` defines a `verify-parent-pr` job that runs `gh pr view 15 --json mergeable -q .mergeable` and fails the job unless the output equals `MERGEABLE`. The job is configured as a required check on this PR (or required by branch protection). Verify: `grep -n 'verify-parent-pr\\|gh pr view 15' .github/workflows/ci.yml` returns lines; `gh pr checks <this-PR>` lists `verify-parent-pr` as a required check.",
  "[archi/full make-check] `make check` (= lint + type-check + test + verify-no-origin-literal) exits 0 on a fresh checkout of this PR. Verify: `make check` exits 0.",
  "[archi/no shims] No backwards-compat re-export shims are introduced. If a symbol moves location, all consumers update their imports. Verify: `grep -rn 'try:.*from kodezart' src/kodezart/ | grep -i 'except ImportError'` returns no lines; no `__init__.py` is modified to re-export `SoftFailureError`, `RaiseSite`, or `drain` from a non-canonical path; consumers import directly from `kodezart.core.soft_failure`.",
  "[archi/single PR] Exactly one PR contains all three facets (OBS + inherited CFG + ANC); no intermediate merge to main. Verify: `git log --oneline --merges main..HEAD` returns no merge commits; `gh pr list --state merged --search 'OBS observability widening'` returns no separate PR.",
  "[anti-pattern/peer not subclass] `SoftFailureError` is a PEER of `AgentSDKError`, not a subclass of `TransientAPIError`/`RateLimitError`/`AgentSDKError`. Verified by the retry test (`tests/core/test_retry.py::test_soft_failure_error_does_not_satisfy_should_retry`).",
  "[anti-pattern/no value object] No `GitRemote` value object, enum, or helper module is introduced. `AppConfig.git_remote` is a plain `str`. Verify: `find src/kodezart -iname '*remote*'` returns no new files; `grep -rn 'class GitRemote\\|class Remote(' src/kodezart/` returns no lines.",
  "[anti-pattern/no new ConsolidationStatus] No new value is added to the `ConsolidationStatus` enum to capture exit-128. The subprocess error string propagates verbatim through `RuntimeError`. Verify: the four-status enum body in `types/domain/consolidation.py` (`ALREADY_INTEGRATED`, `FAST_FORWARDED`, `DIVERGENT`, `SOURCE_MISSING`) is unchanged in the PR diff.",
  "[anti-pattern/no clone-layout switch] `clone_bare` is NOT switched to `git clone --mirror` and the bare-repo cache layout is unchanged. Verify: `git diff main...HEAD -- src/kodezart/adapters/subprocess_git_service.py` shows no edits to `clone_bare` body and no `--mirror` flag introduced; `grep -n '\\-\\-mirror' src/kodezart/adapters/subprocess_git_service.py` returns no lines.",
  "[anti-pattern/no config knob] `_STDERR_TAIL_BYTES` is NOT exposed as an `AppConfig` field. Verify: `grep -E 'stderr_tail|STDERR_TAIL' src/kodezart/core/config.py` returns no lines.",
  "[anti-pattern/no mocks in production] No `unittest.mock`, `pytest.MonkeyPatch`, or test-double class is referenced from any module under `src/kodezart/`. Verify: `grep -rnE 'from unittest(\\.mock)?|import unittest\\.mock|MagicMock|AsyncMock' src/kodezart/` returns no lines.",
  "[anti-pattern/no silent swallowing] Every modified `except` block either re-raises or logs at `error`/`exception` level before yielding an `ErrorEvent`. No `except Exception: pass` or `except Exception: return None` is introduced. Verify: `git diff main...HEAD -- 'src/kodezart/**' | grep -B1 -A3 '^+.*except '` shows every new `except` block followed by an `await self._log.aexception(` or a `raise` (or both)."
]