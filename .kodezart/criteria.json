[
  "Running `pytest tests/adapters/test_subprocess_git.py::test_is_ancestor_returns_false_when_not_ancestor` exits 0 (PASSED) on hosts where `init.defaultBranch` is unset, set to `master`, or set to `main` — host-independent.",
  "Running `pytest tests/adapters/test_subprocess_git.py::test_diff_summary_returns_changeset_digest` exits 0 (PASSED) under the same host-variance set as above.",
  "Running `pytest tests/adapters/test_subprocess_git.py::test_is_ancestor_raises_on_unknown_ref` continues to exit 0 (PASSED) — the `RuntimeError`-on-unknown-ref contract is preserved.",
  "The assertion at `tests/adapters/test_subprocess_git.py:224` still reads `is False` (not weakened to `is None`, `is not True`, or removed); the membership assertions at lines 313-315 remain unchanged.",
  "Running `pytest tests/adapters/test_subprocess_git.py -v` shows every test in the file passing — no test added, no test removed, no test renamed, no test marked skip/skipif/xfail by this change.",
  "Running `make check` (= `make lint type-check test`) exits 0 on an ubuntu-24.04 environment that has no global `init.defaultBranch` configured.",
  "`uv run ruff check src/ tests/` reports zero violations across the diff; no `# noqa`, `# ruff:`, `# type: ignore`, or any other suppression directive is added.",
  "`uv run mypy src/` continues to exit 0 with no new errors (production code is untouched; `mypy_path = src` means tests are out of scope by design, which the change preserves).",
  "`git diff --name-only` against the merge-base lists exactly one path: `tests/adapters/test_subprocess_git.py`. No file under `src/kodezart/`, no other file under `tests/`, no `pyproject.toml`, no `Makefile`, no `.github/workflows/`, no `__init__.py`, and no `tests/conftest.py` is modified.",
  "The diff to `tests/adapters/test_subprocess_git.py` deletes exactly one line (the bare `await _run_git([\"git\", \"init\"], cwd=repo)`) and adds at most three lines: the replacement `await _run_git([\"git\", \"init\", \"-b\", \"main\"], cwd=repo)` and a one-line inline comment immediately above it explaining the pin (e.g., `# pin initial branch so 'main' is a valid local ref independent of host gitconfig`).",
  "The modification lives entirely inside the `git_repo` async fixture body (originally lines 11-19); no test function, no module-level code, and no helper (`_run_git`) is touched.",
  "The new argv list reads exactly `[\"git\", \"init\", \"-b\", \"main\"]` — short-form `-b`, matching verbatim the three established precedents at `tests/adapters/test_git_branch_merger.py:50`, `tests/adapters/test_git_worktree_provider.py:195`, and `tests/integration/test_workflow_e2e.py:60`.",
  "The seven other `git init*` invocations in the file remain byte-identical: `git init --bare` at lines 88, 95, 141, 239, 255, 290 (remote stand-ins, local refs DB irrelevant) and the non-bare `git init` at line 267 (`test_remote_branch_sha_raises_on_unexpected_exit`, which intentionally does not depend on local `main`).",
  "The other `\"main\"` literals in the file are not modified: push refspecs `HEAD:refs/heads/main` at lines 242 and 293, and `ls-remote --heads origin main` invocations at lines 245, 257, and 297 — these operate against the remote refs database and are independent of the local initial branch.",
  "No new `import` or `from … import …` line is added (`git diff tests/adapters/test_subprocess_git.py | grep -E '^\\+(import|from)'` returns empty); no `__init__.py` is modified; no alias or re-export shim is introduced.",
  "No new helper function, fixture, parametrized fixture, `conftest.py` entry, repo-creation utility, module-level constant (e.g., `DEFAULT_BRANCH`), or class appears in the diff. The `+` side contains zero `def `, `@pytest.fixture`, or `class ` lines.",
  "`tests/conftest.py`'s `_git_test_identity` autouse session fixture (lines 15-25) is unchanged — `GIT_AUTHOR_*` / `GIT_COMMITTER_*` env-var setup continues unmodified.",
  "`src/kodezart/adapters/subprocess_git_service.py` is byte-identical to pre-fix: `SubprocessGitService.is_ancestor`, `SubprocessGitService.diff_summary`, `_run_with_exit_codes`, and `_run_output` are unchanged; no `try`/`except` is added; no production-side tolerance of unknown refs is introduced; the `RuntimeError` re-raise on exit 128 remains intact.",
  "No `pytest.mark.skip`, `pytest.mark.skipif`, `pytest.mark.xfail`, or test rename is applied to `test_is_ancestor_returns_false_when_not_ancestor`, `test_diff_summary_returns_changeset_digest`, `test_is_ancestor_raises_on_unknown_ref`, or any other test in the file.",
  "No `try`/`except RuntimeError` or `try`/`except Exception` block is added inside the `git_repo` fixture or inside either previously-failing test thatwould swallow or mask a real failure.",
  "No `git config init.defaultBranch` invocation appears anywhere in the diff (neither global, nor system, nor local, nor before, nor after `git init`); the post-`init` form is a no-op anti-pattern and must not appear.",
  "The diff contains no `git config --global`, no `GIT_CONFIG_GLOBAL=` assignment, no `git config --system`, and no other mutation of user- or host-level git configuration. The pin is local to the one argv list.",
  "`pytest --collect-only tests/adapters/test_subprocess_git.py` returns the same set of test node-ids before and after the fix — no compensating new test is added to mask the original failure.",
  "[sherlock] Inside a repository created by the `git_repo` fixture, `git symbolic-ref --short HEAD` returns `main` on any host irrespective of the host's `init.defaultBranch` value — combines Watson 1's HEAD-state observation with Watson 5's environment-invariance principle.",
  "[sherlock] No typed domain model, shared constant, or cross-component convention is introduced for the literal `\"main\"`; the string remains a test-local fixture/argument value. Production code stays agnostic to branch names (it accepts `base_branch` as a parameter). Synthesizes Watson 2 (F1/F2), Watson 4 (h), and Watson 5 (d).",
  "[sherlock] Causal-responsibility check: reverting only the addition of the `\"-b\", \"main\"` tokens (keeping all other diff lines) causes `test_is_ancestor_returns_false_when_not_ancestor` and `test_diff_summary_returns_changeset_digest` to fail with the documented `RuntimeError` from `_run_with_exit_codes` / `_run_output` — proving the two tokens are the load-bearing change, not a coincidental side effect of comment placement or whitespace.",
  "[sherlock] Targeted CI-equivalence check: running `HOME=$(mktemp -d) GIT_CONFIG_GLOBAL=/dev/null uv run pytest tests/adapters/test_subprocess_git.py -v` (the closest reproducible local stand-in for the ubuntu-24.04 runner's missing `init.defaultBranch`) shows all tests in the file passing — Watson 3 reported a baseline of '229 passed' on the developer machine but did not test under the actual failure condition; this criterion closes that gap."
]