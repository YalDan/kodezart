[
  "[behavioral] Running the workflow with `consolidate` returning FAST_FORWARDED emits — in this order on the SSE stream — `WorkflowConsolidationEvent(type='workflow_consolidation', status='fast_forwarded', featureBranch=..., sourceBranch=..., featureTipSha=<40 hex>)` followed by `WorkflowReviewEvent`, with no `phase` key on the consolidation event.",
  "[behavioral] Running the workflow with `consolidate` returning ALREADY_INTEGRATED emits `WorkflowConsolidationEvent(status='already_integrated', ...)` and the workflow proceeds to `review_against_ticket` without raising and without emitting any error field on `WorkflowCompleteEvent`.",
  "[behavioral] Running the workflow with `consolidate` returning DIVERGENT emits `WorkflowConsolidationEvent(status='divergent', ...)`, then `WorkflowCompleteEvent(merged=False, error=<string containing 'diverged'>)`, with no exception propagated to the SSE stream and no `try/except` mediating the path.",
  "[behavioral] Invoking `consolidate` from `_merge_to_feature_node` with a source branch that does not exist on the remote raises a `RuntimeError` (programming-error contract: the ralph branch must have been pushed by the loop) — verified by `pytest tests/chains/test_ralph_workflow.py::test_merge_to_feature_source_missing_raises`.",
  "[behavioral] Invoking `consolidate` from `_fix_code_node` for a fix branch that is already an ancestor of the feature tip returns ALREADY_INTEGRATED and does NOT raise; the fix node advances `fix_rounds_used` by 1 and routes via `_route_after_fix`.",
  "[behavioral] After a successful workflow, querying `git ls-remote origin` returns zero refs matching the glob `*-backup-*`; equivalently, `git branch -r | grep -- '-backup-'` produces zero lines.",
  "[behavioral] `GitService.diff_summary(cwd, base_ref, head_ref)` returns a `ChangesetDigest` whose `commit_count == 0`, `file_paths == []`, `commit_subjects == []` when `base_ref == head_ref`; the prompt body rendered with this digest contains the literal escape clause 'No commits between the base and head refs; the previous verdict\\'s failures persist unchanged.'.",
  "[behavioral] `GitService.remote_branch_sha(cwd, remote, branch)` returns `None` exactly when the underlying `git ls-remote --exit-code --heads <remote> refs/heads/<branch>` exits with code 2, and returns the parsed SHA exactly when the command exits 0; any other exit code raises.",
  "[behavioral] `GitService.is_ancestor(cwd, a, b)` returns True iff `git merge-base --is-ancestor a b` exits 0, False iff exit 1; any other exit code raises (e.g., unknown ref).",
  "[behavioral] `GitChangePersister.persist` returns `PersistResult(source=PersistSource.WORKING_TREE_COMMIT, ...)` on the dirty-tree path; returns `PersistResult(source=PersistSource.AGENT_DIRECT_COMMIT, message=<real `git log -1 --format=%B HEAD` output>)` on the clean-tree-HEAD-ahead-of-remote path; returns `None` when HEAD equals remote tip; raises `RuntimeError` matching /diverged/ when HEAD and remote tip have diverged.",
  "[behavioral] `WorkflowIterationEvent.commit_sha` carries the per-iter commit SHA: across a two-iteration run where iter 1 commits and iter 2 does not, the second `WorkflowIterationEvent.commit_sha` is `None` and is NOT the SHA from iter 1.",
  "[behavioral] `WorkflowCompleteEvent.final_commit_sha` (wire key `finalCommitSha`, camelCase) is populated from `state['feature_tip_sha']` and is unchanged in wire shape; a Pydantic serialization assertion in tests pins `finalCommitSha` as the JSON key.",
  "[architecture] `src/kodezart/types/domain/consolidation.py` is a new module under `types/domain/` (not `adapters/`, `chains/`, `core/`, or `services/`) and imports only stdlib (`enum`, `typing`) plus `kodezart.types.base` for `CamelCaseModel` — verified by `grep '^from kodezart' src/kodezart/types/domain/consolidation.py` returning matches only under `kodezart.types.*`.",
  "[architecture] `ConsolidationStatus` is declared as `class ConsolidationStatus(StrEnum)` with exactly four lowercase_with_underscores members `ALREADY_INTEGRATED='already_integrated'`, `FAST_FORWARDED='fast_forwarded'`, `DIVERGENT='divergent'`, `SOURCE_MISSING='source_missing'`; no fifth member.",
  "[architecture] `ConsolidationOutcome` is a frozen `CamelCaseModel` with exactly two fields: `status: ConsolidationStatus` and `feature_tip_sha: str` (Field with min/max length 40). It contains no `ahead_count`, no `behind_count`, no `source_tip_sha`, no `error`, no `phase` field — `grep -E 'ahead_count|behind_count|source_tip_sha|phase' src/kodezart/types/domain/consolidation.py` returns zero matches.",
  "[architecture] `ChangesetDigest` is a frozen `CamelCaseModel` with exactly `file_paths: list[str]`, `commit_subjects: list[str]`, `commit_count: int (ge=0)`, and an `is_empty` property; no `raw_diff`, no `insertions`/`deletions` field unless the prompt body actually renders it — verified by inspecting both the model and the prompt template.",
  "[architecture] `PersistSource(StrEnum)` is declared in `src/kodezart/types/domain/persist.py` with exactly two members: `WORKING_TREE_COMMIT='working_tree_commit'` and `AGENT_DIRECT_COMMIT='agent_direct_commit'`.",
  "[architecture] `PersistResult.source: PersistSource` is a required field with NO default; every construction site in src/ and tests/ passes `source=...` explicitly — confirmed by running `mypy --strict` over the changed files producing zero errors.",
  "[architecture] `WorkflowConsolidationEvent` lives in `src/kodezart/types/domain/agent.py` next to other `Workflow*Event` classes, subclasses `AgentEvent`, declares `type: Literal['workflow_consolidation'] = 'workflow_consolidation'`, and contains fields `status: ConsolidationStatus`, `feature_branch: str (min_length=1)`, `source_branch: str (min_length=1)`, `feature_tip_sha: str (min_length=40, max_length=40)`. No `phase` field exists.",
  "[architecture] `RalphLoopState` in `src/kodezart/types/domain/workflow.py` declares `iteration_commit_sha: NotRequired[str | None]` using `from typing import NotRequired` (PEP 655, available on the 3.12 target); the symbol `last_commit_sha` does NOT appear in this TypedDict.",
  "[architecture] `WorkflowState` declares `feature_tip_sha: str | None`, `review_base_sha: str | None`, `review_head_sha: str | None`, and does NOT declare `last_commit_sha`, `last_consolidation`, `ralph_tip_sha`, `ahead_count`, `behind_count`, or `source_tip_sha`.",
  "[architecture] `GitService` protocol in `src/kodezart/core/protocols.py` adds exactly three new async methods — `is_ancestor`, `remote_branch_sha`, `diff_summary` — and adds NO `rev_list_count` and NO `diff_name_only` (YAGNI rejection).",
  "[architecture] `BranchMerger` protocol in `src/kodezart/core/protocols.py` exposes exactly `consolidate(...) -> ConsolidationOutcome` and `cleanup_backup_branches(...)`; both `merge_and_push` and `cleanup_source` are absent from the protocol surface — `grep -E 'merge_and_push|def cleanup_source' src/kodezart/core/protocols.py` returns zero matches.",
  "[architecture] The decision tree of `consolidate` is implemented entirely inside `src/kodezart/adapters/git_branch_merger.py`; `_cleanup_source_internal` is a private (leading-underscore) method invoked only from inside `consolidate` on the FAST_FORWARDED branch — no chain/workflow file calls `_cleanup_source_internal` or any cleanup method on the merger conditioned on `outcome.status`.",
  "[architecture] `src/kodezart/chains/ralph_loop.py` and `src/kodezart/chains/ralph_workflow.py` annotate the new `git` and `cache` parameters with the Protocol types `GitService` and `RepoCache` (imported from `kodezart.core.protocols`), not with concrete adapter classes — verified by `grep -E 'SubprocessGitService|LocalBareRepoCache|GitBranchMerger' src/kodezart/chains/` returning zero matches.",
  "[architecture] No file under `src/kodezart/adapters/` imports from `kodezart.chains`, `kodezart.services`, or `kodezart.handlers` — `grep -rE 'from kodezart\\.(chains|services|handlers)' src/kodezart/adapters/` returns zero matches.",
  "[anti-pattern] Zero `try:`/`except` blocks surround any `self._merger.consolidate(...)` call in `src/kodezart/chains/ralph_workflow.py` — verified by reading `_merge_to_feature_node` and `_fix_code_node` and confirming the consolidate call is at the top level of the node body, not inside a `try` block.",
  "[anti-pattern] The string `<agent-direct>` does not appear anywhere in `src/` or `tests/` — `grep -r 'agent-direct' src/ tests/` returns zero matches.",
  "[anti-pattern] The strings `'post_loop'` and `'post_fix'` do not appear in any TypedDict, event payload, routing dict, or production source file — `grep -rE \"'(post_loop|post_fix)'|\\\"(post_loop|post_fix)\\\"\" src/` returns zero matches.",
  "[anti-pattern] The method `GitWorktreeProvider._backup_if_needed` is GONE: `hasattr(GitWorktreeProvider, '_backup_if_needed')` returns False at runtime, asserted by `tests/adapters/test_git_worktree_provider.py::test_backup_if_needed_method_is_gone`.",
  "[anti-pattern] The string `kodezart: emergency backup of in-progress work` does not appear anywhere in `src/kodezart/` — `grep -r 'emergency backup' src/kodezart/` returns zero matches.",
  "[anti-pattern] `git_worktree_provider.py` `release()` performs no `git push` to any backup-style ref on the dirty-release path; on dirty release it emits a structured warning log `workspace_release_unclean` via `self._log.awarning(...)` and returns without further side effects — verified by `tests/adapters/test_git_worktree_provider.py` inverting the prior backup-on-dirty-release test.",
  "[anti-pattern] `BackupBranchName(...)` is no longer constructed in `src/kodezart/adapters/git_worktree_provider.py` — `grep 'BackupBranchName(' src/kodezart/adapters/git_worktree_provider.py` returns zero matches.",
  "[anti-pattern] The symbol `last_commit_sha` does not appear in any file under `src/kodezart/` after the change — `grep -r 'last_commit_sha' src/kodezart/` returns zero matches.",
  "[anti-pattern] No `WorkflowConsolidationEvent` field, no `ConsolidationOutcome` field, and no routing dict in `chains/ralph_workflow.py` references a `ConsolidationStatus` value as a raw string literal — `grep -nE \"'already_integrated'|'fast_forwarded'|'divergent'|'source_missing'\" src/kodezart/chains/ src/kodezart/adapters/` returns matches ONLY inside `src/kodezart/types/domain/consolidation.py` (the enum members themselves) and inside SSE snapshot test fixtures.",
  "[anti-pattern] No `_fix_code_node` or `_merge_to_feature_node` body branches on `pr_url is None` to skip emitting `WorkflowConsolidationEvent`; the WorkflowConsolidationEvent is emitted unconditionally after every `consolidate` call. Precondition routing lives only in `_route_after_fix` / `_route_after_merge` router functions.",
  "[anti-pattern] No `Fake*` or `Mock*` class is defined anywhere under `src/kodezart/` — `grep -rE '^class (Fake|Mock)' src/kodezart/` returns zero matches.",
  "[anti-pattern] No `import *` statement exists anywhere in `src/kodezart/` — `grep -rE '^from .* import \\*' src/kodezart/` returns zero matches.",
  "[sherlock-anti-pattern] The string `--porcelain` appears exactly once in `src/kodezart/`, inside `SubprocessGitService.has_changes` only; it does NOT appear in `git_change_persister.py`, `git_branch_merger.py`, or anywhere else — `grep -r '\\-\\-porcelain' src/kodezart/adapters/` returns exactly one file (`subprocess_git_service.py`) and the new clean-tree path of `git_change_persister.py` uses `current_sha` + `remote_branch_sha` + `is_ancestor` only (committed-history primitives).",
  "[architecture] `SubprocessGitService._run_with_exit_codes(cmd, cwd, allowed: frozenset[int], env=None) -> tuple[int, str]` exists as a private method in `subprocess_git_service.py`, returns `(exit_code, stdout)` when exit ∈ allowed, raises otherwise; `is_ancestor` calls it with `allowed={0, 1}` and `remote_branch_sha` calls it with `allowed={0, 2}`; the existing `_run` and `_run_output` continue to raise on any non-zero exit.",
  "[architecture] `SubprocessGitService.remote_branch_sha` invokes `git ls-remote --exit-code --heads <remote> refs/heads/<branch>` and does NOT invoke `git fetch` before it — verified by `tests/adapters/test_subprocess_git.py::test_remote_branch_sha_does_not_invoke_fetch`.",
  "[architecture] `evaluation.build_prompt` has the keyword-only signature `def build_prompt(*, criteria: list[str], changeset: ChangesetDigest) -> str`; calling positionally raises `TypeError` — verified by `tests/prompts/test_prompt_wiring.py::test_evaluation_prompt_rejects_positional_args`.",
  "[architecture] The rendered output of `evaluation.build_prompt` contains the literal commit subjects and file paths from the `ChangesetDigest`, and contains NO shell command substrings (`git diff`, `git log`, `--name-only`, `--format=%s`) — `assert 'git diff' not in rendered` and `assert 'git log' not in rendered` in the new test.",
  "[tooling] Running `mypy --strict` from the project root over every file in the change set produces zero errors. The exact file list comprises the 14 src/ paths and 11 tests/ paths enumerated in the plan (src/kodezart/types/domain/{consolidation.py,persist.py,agent.py,workflow.py}, src/kodezart/core/protocols.py, src/kodezart/adapters/{subprocess_git_service.py,git_branch_merger.py,git_change_persister.py,git_worktree_provider.py}, src/kodezart/prompts/evaluation.py, src/kodezart/chains/{ralph_loop.py,ralph_workflow.py}, src/kodezart/main.py, tests/fakes.py, plus all 10 modified test files).",
  "[tooling] Running `ruff check --select=E,W,F,I,B,C4,UP,N,ANN,S,A,ARG,RUF --ignore=S101` over the same file set produces zero violations. No `# noqa`, no `# type: ignore`, no per-file ignore additions are introduced.",
  "[tooling] Running `ruff format --check` over the same file set succeeds with no formatting drift (line-length 88, double quotes).",
  "[tooling] Running `pytest tests/` from the project root completes with all collected tests passing; no test is skipped (the `live` marker is unaffected by this change).",
  "[integration-sanity] No source file in `src/kodezart/` or `tests/` references `merge_and_push` or `cleanup_source` — `grep -rE 'merge_and_push|cleanup_source' src/ tests/` returns zero matches.",
  "[integration-sanity] No source file in `src/kodezart/` references `WorkflowState['last_commit_sha']` or `state['last_commit_sha']` — `grep -rE \"last_commit_sha\" src/kodezart/` returns zero matches.",
  "[integration-sanity] `tests/fakes.py` `FakeBranchMerger`: the `fail` and `fail_cleanup` ctor parameters are removed; the new `consolidation_outcomes: list[ConsolidationOutcome] | None = None` parameter exists; default (empty/absent queue) yields a FAST_FORWARDED outcome with a deterministic 40-char SHA so existing default-construct call sites (e.g., `tests/api/v1/test_agent.py:177`) continue to pass.",
  "[integration-sanity] `tests/fakes.py` `FakeGitService` implements `is_ancestor`, `remote_branch_sha`, and `diff_summary`, each appending to `self.calls`; new keyword-only ctor params `remote_branch_shas`, `ancestor_pairs`, `diff_digests` exist with safe defaults so pre-existing constructions are not broken.",
  "[integration-sanity] `tests/chains/test_ralph_workflow.py:194-202` (and the equivalent at lines ~620 and ~2026-2028) updates the assertion `len(merger.calls) == 3` to `len(merger.calls) == 2` (one `consolidate`, one `cleanup_backup_branches`); the kwargs read off `merger.calls[0]` reflects the `consolidate` signature (`repo_path`, `repo_url`, `base_branch`, `feature_branch`, `source_branch`, `cache_key`).",
  "[integration-sanity] `tests/chains/test_ralph_workflow.py::test_workflow_merge_failure_reports_error` (lines 205-232) is reshaped from `FakeBranchMerger(fail=RuntimeError(...))` to `FakeBranchMerger(consolidation_outcomes=[ConsolidationOutcome(status=ConsolidationStatus.DIVERGENT, feature_tip_sha='0'*40)])` and continues to assert `'diverged' in complete_events[0].error`.",
  "[sherlock-integration-sanity] `tests/chains/test_ralph_workflow.py::test_workflow_cleanup_failure_reports_error` (currently relies on `fail_cleanup=` to simulate a `cleanup_source` failure) is removed or repurposed: since `cleanup_source` is absorbed into `consolidate` as a private, never-raising helper, there is no externally observable cleanup-source-failure surface; if the test remains it must instead assert that the internal cleanup failure on FAST_FORWARDED is logged (warning) and does NOT change `outcome.status` from FAST_FORWARDED.",
  "[integration-sanity] `tests/chains/test_shorthand_url_resolution.py:87-89` updates the merger call count from 3 to 2 and reads `merger.calls[0]['repo_url']` off the `consolidate` call dict (filtered by `method == 'consolidate'` if needed).",
  "[integration-sanity] `tests/chains/test_ralph_loop.py:413-431` migrates the `build_prompt` spy from `def spy(criteria: list[str]) -> str` to `def spy(*, criteria: list[str], changeset: ChangesetDigest) -> str` and calls `original(criteria=criteria, changeset=changeset)`.",
  "[integration-sanity] `tests/chains/test_ralph_loop.py` adds these new tests: `test_execute_node_writes_iteration_commit_sha_none_on_no_change_iter`, `test_execute_node_with_commit_returns_iteration_commit_sha_set`, `test_evaluate_node_emits_workflowiteration_with_per_iter_commit_sha`, `test_evaluate_node_calls_git_diff_summary_with_base_and_ralph_branch`, `test_evaluate_node_passes_changeset_to_build_prompt`. The per-iter test drives iter 1 with a SHA and iter 2 with None and asserts iter 2's `WorkflowIterationEvent.commit_sha is None`.",
  "[integration-sanity] `tests/chains/test_ralph_workflow.py` adds: `test_merge_to_feature_divergent_routes_to_complete_with_merge_error`, `test_merge_to_feature_source_missing_raises`, `test_merge_to_feature_already_integrated_proceeds_to_review`, `test_fix_code_node_divergent_routes_to_comment_failure_when_pr_url_set`, `test_fix_code_node_divergent_routes_to_complete_when_no_pr_url`, `test_fix_code_node_already_integrated_does_not_raise_advances_fix_round`, `test_fix_code_node_source_missing_routes_terminally`, `test_review_uses_review_base_sha_and_review_head_sha_not_branch_refs`, `test_complete_event_final_commit_sha_sources_from_feature_tip_sha`, `test_review_against_ticket_passes_changeset_digest_to_build_prompt`.",
  "[integration-sanity] `tests/adapters/test_git_branch_merger.py` renames `test_merge_and_push_creates_feature_branch` to `test_consolidate_fast_forwarded_creates_feature_branch` and adds `test_consolidate_already_integrated_returns_no_push`, `test_consolidate_divergent_does_not_raise`, `test_consolidate_source_missing_returns_status_without_acquiring_worktree`; the SOURCE_MISSING test asserts no `_workspace.acquire` call recorded.",
  "[integration-sanity] `tests/adapters/test_git_change_persister.py` adds `test_persist_returns_working_tree_commit_source`, `test_persist_returns_agent_direct_commit_source_with_real_head_message` (asserting `message` equals the actual HEAD commit message, NOT the literal string `<agent-direct>`), `test_persist_raises_on_diverged_head` (`pytest.raises(RuntimeError, match='diverged')`), and `test_persist_returns_none_when_remote_in_sync`.",
  "[integration-sanity] `tests/adapters/test_subprocess_git.py` adds real-git-in-tmp_path tests: `test_is_ancestor_returns_true_when_descendant`, `test_is_ancestor_returns_false_when_not_ancestor`, `test_is_ancestor_raises_on_unknown_ref`, `test_remote_branch_sha_returns_sha_when_present`, `test_remote_branch_sha_returns_none_when_absent`, `test_remote_branch_sha_raises_on_unexpected_exit`, `test_remote_branch_sha_does_not_invoke_fetch`, `test_diff_summary_returns_changeset_digest`, `test_diff_summary_empty_when_refs_equal`.",
  "[integration-sanity] `tests/adapters/test_git_worktree_provider.py` adds `test_backup_if_needed_method_is_gone` asserting `not hasattr(GitWorktreeProvider, '_backup_if_needed')`, and inverts any prior `test_release_backs_up_uncommitted_changes`-style test so that on dirty release: (a) the `workspace_release_unclean` warning is emitted, (b) no `git.push` call records any ref matching `*-backup-*`.",
  "[integration-sanity] `tests/prompts/test_prompt_wiring.py` migrates the positional call at line 15 to keyword-only and adds `test_evaluation_prompt_inlines_changeset_digest`, `test_evaluation_prompt_handles_empty_changeset`, `test_evaluation_prompt_rejects_positional_args`. NO new file `tests/prompts/test_evaluation.py` is created (DRY).",
  "[integration-sanity] `tests/integration/test_workflow_e2e.py` migrates every reference: `merge_and_push` → `consolidate`, `cleanup_source` → (removed assertion), `last_commit_sha` → `feature_tip_sha`, `fail=RuntimeError(...)` → `consolidation_outcomes=[ConsolidationOutcome(status=DIVERGENT, ...)]`, positional `build_prompt(...)` → keyword-only `build_prompt(criteria=..., changeset=...)`. No test is skipped or weakened.",
  "[sherlock-coordination] `tests/chains/test_ralph_workflow.py::test_workflow_consolidation_event_emitted_post_loop` is a SNAPSHOT test that pins the exact camelCase wire shape of `WorkflowConsolidationEvent` JSON: `{'type': 'workflow_consolidation', 'status': 'fast_forwarded', 'featureBranch': <str>, 'sourceBranch': <str>, 'featureTipSha': <40-hex>}` with `extra='forbid'` semantics — this is the explicit coordination artifact for SSE consumers (per the Pydantic v2 discriminated-union note in the plan).",
  "[sherlock-coordination] A serialization assertion exists (in `tests/api/v1/test_agent.py` or `tests/chains/test_ralph_workflow.py`) confirming `WorkflowCompleteEvent` still emits the wire key `finalCommitSha` (camelCase) — guarding against an accidental rename to `featureTipSha` when the internal source field changes.",
  "[integration-sanity] `src/kodezart/main.py` `lifespan` adds `git=git, cache=cache` kwargs to both `RalphLoop(...)` (line 85-91) and `RalphWorkflowEngine(...)` (line 100-113); both `git` and `cache` are already in scope at line 54 and 56 respectively. No new concrete adapter is constructed.",
  "[YAGNI] Every newly added field, parameter, method, or class in this change set has at least one direct consumer in the change set: `is_ancestor` is called from `GitBranchMerger.consolidate` and `GitChangePersister.persist`; `remote_branch_sha` is called from both; `diff_summary` is called from `RalphLoop._evaluate_node` and `RalphWorkflowEngine._review_against_ticket_node`; `_run_with_exit_codes` is called from `is_ancestor` and `remote_branch_sha`; `_log_release_state` is called from `release()`; `PersistResult.source` is constructed by both persister paths and read by at least the SSE/agent-service consumer that newly distinguishes the two; `iteration_commit_sha` is written by `_execute_node` and read by `_evaluate_node`; `review_base_sha`/`review_head_sha` are written by `_merge_to_feature_node`/`_fix_code_node` and read by `_review_against_ticket_node`; `git`/`cache` engine params are read by `_evaluate_node` and `_review_against_ticket_node` respectively. Any orphaned addition fails this criterion.",
  "[architecture] `src/kodezart/types/domain/agent.py` imports `ConsolidationStatus` from `kodezart.types.domain.consolidation` — no string-literal duplication of the four status values inside `agent.py`.",
  "[architecture] No `__init__.py` under `src/kodezart/types/domain/` is modified to aggregate exports; consumers import the new symbols directly from `kodezart.types.domain.consolidation` and `kodezart.types.domain.persist` — `grep -rE 'from kodezart\\.types\\.domain\\.(consolidation|persist) import' src/ tests/` returns matches only as direct module imports.",
  "[architecture] `src/kodezart/adapters/git_worktree_provider.py` class docstring records the deliberate trade-off: 'Recovery-via-backup-branch is intentionally disabled; re-introducing it must be via the typed state contract' (or equivalent verbiage).",
  "[LangGraph invariant] `_execute_node` in `ralph_loop.py` ALWAYS includes the key `iteration_commit_sha` in its return dict (value is `None` on no-change iters, a 40-char SHA otherwise) — never omitted; `_evaluate_node` reads it via `state.get('iteration_commit_sha')`.",
  "[LangGraph invariant] `_merge_to_feature_node` returns explicit values for `merged`, `merge_error`, `feature_tip_sha`, and (on FAST_FORWARDED/ALREADY_INTEGRATED) `review_base_sha`, `review_head_sha`; on DIVERGENT it returns `{'merged': False, 'merge_error': <str>, 'feature_tip_sha': outcome.feature_tip_sha}` and routes through `_route_after_merge` to `complete` so the error surfaces in `WorkflowCompleteEvent.error`.",
  "[LangGraph invariant] `_fix_code_node` returns `feature_tip_sha`, `review_base_sha`, `review_head_sha`, `merge_error`, `merged`, `fix_rounds_used` keys for every status; on DIVERGENT/SOURCE_MISSING it sets `feature_tip_sha=pre_fix_head_sha` (preserves), `merged=False`, `review_base_sha=None`, `review_head_sha=None`, and increments `fix_rounds_used`.",
  "[LangGraph invariant] `graph.add_edge('fix_code', 'review_against_ticket')` is replaced by `graph.add_conditional_edges('fix_code', self._route_after_fix, {...})` whose router returns `'review_against_ticket'` when `merge_error is None`, `'comment_failure'` when `pr_url is not None` AND `_pr_creator is not None`, else `'complete'`.",
  "[behavioral] In `_review_against_ticket_node`, when `review_base_sha == review_head_sha` (ALREADY_INTEGRATED fix path), `diff_summary` returns an empty `ChangesetDigest` and the evaluator prompt renders the empty-changeset escape clause; the workflow continues without raising.",
  "[behavioral] In `_review_against_ticket_node`, if either `state['review_base_sha']` or `state['review_head_sha']` is `None`, the node raises (programming error) — verified by an explicit assert and a test.",
  "[architecture] `WorkflowIterationEvent`, `WorkflowReviewEvent`, `WorkflowPREvent`, `WorkflowCIEvent`, and `WorkflowCompleteEvent` wire shapes are unchanged (no field additions, no field renames at the Python level beyond what alias_generator emits) — Pydantic snapshot tests pin each.",
  "[anti-pattern] `_complete_node` populates `final_commit_sha=state['feature_tip_sha']`; the assignment is the only source for the field and there is no fallback to a stale `state['last_commit_sha']` (the latter key does not exist).",
  "[architecture] `GitChangePersister.persist` docstring is expanded to document the new conceptual scope ('ensure canonical ref equals workspace HEAD') and the two return-source paths."
]