[
  "Running `make check` (which executes `verify-no-origin-literal lint type-check test` per Makefile) exits 0 on a CI host without a global `init.defaultBranch=main` git config — for example `HOME=$(mktemp -d) -- git config --global --unset-all init.defaultBranch || true && make check` exits 0.",
  "Running `pytest 'tests/integration/test_workflow_e2e.py::test_git_change_persister_divergence_error_references_configured_remote[origin]' 'tests/integration/test_workflow_e2e.py::test_git_change_persister_divergence_error_references_configured_remote[upstream]' -q` exits 0 in an environment where `git config --global --get init.defaultBranch` returns empty.",
  "Inspecting `tests/integration/test_workflow_e2e.py::_init_repo_with_remote` shows the bare-repo creation line invokes `git init --bare -b main` (or equivalent argv passing `-b main` / `--initial-branch=main`) — `grep -n 'git', 'init', '--bare'` shows the bare invocation paired with `-b`/`--initial-branch` `main`.",
  "No conftest fixture, no `pytest.ini` change, no global `git config init.defaultBranch` write, and no other CI-lifecycle hook is introduced to mask the CI portability bug — `git diff main..HEAD -- 'tests/**/conftest.py' 'pytest.ini' 'pyproject.toml'` shows no additions related to default-branch handling or `init.defaultBranch`.",
  "The class `SoftFailureError` no longer exists anywhere — `grep -RIn 'SoftFailureError' src/ tests/` returns ZERO matches (covers the class definition, all 8 raise sites, the `build_error_event` isinstance branch, all test file references, every docstring/comment mention in `core/soft_failure.py` and `types/domain/agent.py`, and the wire-shape literal assertion in `tests/api/v1/test_agent.py`).",
  "The class `NoStructuredOutputError` is defined exactly once — `grep -RIn '^class NoStructuredOutputError' src/` returns exactly one match in `src/kodezart/core/soft_failure.py`.",
  "Running `python -c 'from kodezart.core.soft_failure import SoftFailureError'` exits non-zero with `ImportError`.",
  "Running `python -c 'from kodezart.core.soft_failure import NoStructuredOutputError; assert NoStructuredOutputError.__bases__ == (Exception,)'` exits 0 — the hierarchy is unchanged (peer of `AgentSDKError`, NOT a subclass).",
  "`grep -RIn 'raise NoStructuredOutputError(' src/` returns exactly 8 matches at the eight specified raise sites: 4 in `src/kodezart/chains/ralph_workflow.py` (`branch_name`, `acceptance_criteria`, `post_merge_review`, `pr_description` raise sites), 1 in `src/kodezart/chains/ralph_loop.py` (`ralph_evaluator`), 2 in `src/kodezart/chains/ticket_generation.py` (`ticket_creator`, `ticket_reviewer`), 1 in `src/kodezart/adapters/git_change_persister.py` (`commit_message`).",
  "The eight `raise_site=` literal values at the 8 `raise NoStructuredOutputError(...)` call sites are members of the `RaiseSite = Literal[...]` definition in `src/kodezart/types/domain/agent.py:27-36` — no new literal is invented and the existing eight literals remain the single authoritative enumeration.",
  "The `isinstance` dispatch in `build_error_event` is updated to `isinstance(exc, NoStructuredOutputError)` — `grep -n 'isinstance(exc, NoStructuredOutputError)' src/kodezart/core/soft_failure.py` returns at least one line; `grep -n 'isinstance(exc, SoftFailureError)' src/` returns zero lines.",
  "After raising and rendering a `NoStructuredOutputError`, the emitted `ErrorEvent.error_kind` field on the wire equals the string `\"NoStructuredOutputError\"` — confirmed by the renamed wire-shape test `test_error_event_carries_no_structured_output_payload` at `tests/api/v1/test_agent.py` passing with that exact literal assertion.",
  "NO backwards-compat alias for the renamed exception exists anywhere — `grep -RIn 'SoftFailureError\\s*=\\s*NoStructuredOutputError\\|NoStructuredOutputError\\s+as\\s+SoftFailureError' src/ tests/` returns ZERO matches; no `__all__` entry preserves the old name; no try/except ImportError shim is introduced.",
  "Running `python -c 'from kodezart.core.soft_failure import RaiseSite'` exits non-zero with `ImportError` — the dead `from kodezart.types.domain.agent import RaiseSite as RaiseSite` re-export is gone.",
  "`grep -n 'import RaiseSite as RaiseSite' src/kodezart/core/soft_failure.py` returns zero lines; `RaiseSite` is imported via a plain (non-aliased) entry folded into the existing multi-import block from `kodezart.types.domain.agent`.",
  "The module docstring of `src/kodezart/core/soft_failure.py` no longer claims downstream consumers import `RaiseSite` from this module — `grep -nE 'Re-exported so existing consumers|continue to import RaiseSite' src/kodezart/core/soft_failure.py` returns zero lines.",
  "Module docstring scope creep is avoided: the only changes to `core/soft_failure.py`'s module docstring are (a) removal of the inaccurate `RaiseSite` re-export claim and (b) renaming `SoftFailureError` → `NoStructuredOutputError` in any prose references. The peer-of-AgentSDKError positioning, primitive-only invariant explanation, `core/` layering rationale, and `drain` coroutine explanation remain verbatim — verifiable by inspecting the diff and counting unchanged sentences.",
  "`grep -RIn 'from kodezart\\.core\\.soft_failure import.*RaiseSite' src/ tests/` returns ZERO matches — confirms no consumer was relying on the dead re-export.",
  "[sherlock] The wrongly-attributed comment block at `core/soft_failure.py:40-43` claiming `handlers/agent_handler.py` is a `RaiseSite` consumer is deleted — `grep -n 'handlers/agent_handler' src/kodezart/core/soft_failure.py` returns zero lines, and verifying `grep -n 'RaiseSite' src/kodezart/handlers/agent_handler.py` returns zero lines confirms the original comment was stale.",
  "`STDERR_TAIL_BYTES: int = 4096` is defined exactly once in the codebase, in `src/kodezart/core/constants.py` — `grep -RIn '^STDERR_TAIL_BYTES\\s*' src/` returns exactly one assignment line in `core/constants.py`.",
  "`grep -RIn '_STDERR_TAIL_BYTES' src/ tests/` returns ZERO matches — the leading-underscore form is fully retired (including the stale comment at `tests/adapters/test_claude_executors.py:101` which previously referenced `_STDERR_TAIL_BYTES = 4096`).",
  "Running `python -c 'from kodezart.core.constants import STDERR_TAIL_BYTES; assert STDERR_TAIL_BYTES == 4096'` exits 0.",
  "Running `python -c 'from kodezart.adapters.claude_client_executor import _STDERR_TAIL_BYTES'` exits non-zero with `ImportError`.",
  "The 5-line wire-shape-invariant rationale comment for `STDERR_TAIL_BYTES` travels verbatim from `adapters/claude_client_executor.py` to `core/constants.py` immediately above the constant declaration — verifiable by `grep -B6 'STDERR_TAIL_BYTES: int = 4096' src/kodezart/core/constants.py` showing the comment block referencing Cloudflare/nginx `proxy_buffer_size` ~8KB framing limits.",
  "`STDERR_TAIL_BYTES` follows the destination module's established uppercase-no-prefix convention — `grep -E '^[A-Z_]+\\s*[:=]' src/kodezart/core/constants.py` shows all constants are uppercase with no leading underscore (matching `EVAL_PERMISSION_MODE`, `EVAL_TOOLS`, `EVAL_TOOLS_WITH_AGENT`, `TICKET_TOOLS`).",
  "`core/constants.py` remains a pure-leaf constants module with no first-party imports — `grep -nE '^from kodezart|^import kodezart' src/kodezart/core/constants.py` returns zero lines.",
  "`src/kodezart/adapters/claude_client_executor.py` imports `STDERR_TAIL_BYTES` from `kodezart.core.constants` — `grep -n 'from kodezart.core.constants import STDERR_TAIL_BYTES' src/kodezart/adapters/claude_client_executor.py` returns at least one line.",
  "The 4096-byte slice in `claude_client_executor.py` is computed on the redacted stderr local, NOT on `exc.stderr` directly — `grep -nB2 'STDERR_TAIL_BYTES\\]' src/kodezart/adapters/claude_client_executor.py` shows the slice operates on `stderr_redacted` (or equivalent local already produced by `_redact_credentials`), not on `exc.stderr`. This enforces the redact-before-slice ordering so a token straddling the 4096-byte boundary cannot survive partially exposed in `AgentSDKError.stderr_tail`.",
  "`_redact_credentials` is defined exactly once in the codebase — `grep -RIn '^def _redact_credentials' src/` returns exactly one match in `src/kodezart/core/soft_failure.py`.",
  "`_REDACTION_SENTINEL: Final[str] = \"***REDACTED***\"` is defined exactly once — `grep -RIn '^_REDACTION_SENTINEL\\s*' src/` returns exactly one assignment line in `src/kodezart/core/soft_failure.py`.",
  "Running `python -c 'from kodezart.core.soft_failure import _REDACTION_SENTINEL; assert _REDACTION_SENTINEL == \"***REDACTED***\"'` exits 0.",
  "The literal `\"***REDACTED***\"` is referenced as the constant `_REDACTION_SENTINEL` everywhere downstream of its definition — `grep -RIn '\"\\*\\*\\*REDACTED\\*\\*\\*\"' src/ tests/` returns exactly one line (the constant definition in `core/soft_failure.py`); test files import `_REDACTION_SENTINEL` from `kodezart.core.soft_failure` and never inline the literal.",
  "No `core/redaction.py` (or any standalone redaction module) exists — `ls src/kodezart/core/redaction.py` exits non-zero; `find src/kodezart -name 'redaction*.py'` returns nothing.",
  "The `_redact_credentials` helper has the signature `def _redact_credentials(s: str) -> str` (single signature, no `@overload` pair) — `grep -nA3 'def _redact_credentials' src/kodezart/core/soft_failure.py` shows `(s: str) -> str` and the function above is not decorated with `@overload`; `grep -nB3 'def _redact_credentials' src/kodezart/core/soft_failure.py` shows no `@overload` decorator immediately preceding.",
  "None-guards live inline at the two call sites in `build_error_event`, NOT inside `_redact_credentials` — `grep -nA12 'return ErrorEvent' src/kodezart/core/soft_failure.py` shows `stderr_tail=_redact_credentials(stderr_tail) if stderr_tail is not None else None` (or equivalent inline guard) at the field-content position.",
  "The three compiled regex patterns are annotated as `re.Pattern[str]` (mypy strict + `disallow_any_explicit` requires explicit module-level annotation) — `grep -nE '_CREDENTIAL_URL_PATTERN|_GH_TOKEN_PATTERN|_GH_FINEGRAINED_PAT_PATTERN' src/kodezart/core/soft_failure.py` shows each followed by `: re.Pattern[str]`.",
  "The token-prefix regex patterns scope tightly to the published GitHub format spec with documented length lower bounds and NO finite upper bounds: classic prefixes `ghp_/gho_/ghs_/ghu_` with body `[A-Za-z0-9]{36,}` (no `{36,N}`), fine-grained PAT prefix `github_pat_` with body `[A-Za-z0-9_]{20,}` (no `{20,N}`) — `grep -nE '\\\\{36,\\\\}|\\\\{20,\\\\}' src/kodezart/core/soft_failure.py` shows the open-ended `{N,}` form, and `grep -nE '\\\\{36,[0-9]+\\\\}|\\\\{20,[0-9]+\\\\}' src/kodezart/core/soft_failure.py` returns zero matches (no `{36,255}` upper bound).",
  "The trailing edge of `[A-Za-z0-9_]+` in the fine-grained PAT pattern does NOT use a `\\b` word-boundary (per Python 3.12 re docs `\\b` between `_` and `\\w` is a no-op) — `grep -nE '\\[A-Za-z0-9_\\]\\{20,\\}\\\\b' src/kodezart/core/soft_failure.py` returns zero matches.",
  "`build_error_event` applies `_redact_credentials` to BOTH leak-carrying ErrorEvent field contents — `grep -nC2 'error=_redact_credentials' src/kodezart/core/soft_failure.py` shows the `error=` kwarg wrapped, and `grep -nC2 'stderr_tail=' src/kodezart/core/soft_failure.py` shows `stderr_tail` similarly wrapped with the inline None-guard.",
  "`adapters/claude_client_executor.py:79-96` computes the redacted stderr exactly ONCE into a local (`stderr_redacted` or equivalent) and reuses it for BOTH the `awarning(..., stderr=...)` kwarg AND the `STDERR_TAIL_BYTES` slice — `grep -c '_redact_credentials(exc.stderr)' src/kodezart/adapters/claude_client_executor.py` returns at most 1 (no double regex evaluation in the same except block).",
  "`adapters/claude_agent_executor.py:69-73` applies `_redact_credentials` symmetrically to the parallel `awarning(\"claude_sdk_process_error\", ..., stderr=...)` call — `grep -nA5 'claude_sdk_process_error' src/kodezart/adapters/claude_agent_executor.py` shows the `stderr=` kwarg value passes through `_redact_credentials` (with inline None-guard); the unwired-by-default executor is scrubbed for future-wired safety, consistent with the existing in-file TODO at lines 56-63 that defers detail-preservation (no new `stderr_tail` slice is introduced here, only the log-call redaction).",
  "Both Claude adapter files import `_redact_credentials` from `core/soft_failure` — `grep -n 'from kodezart.core.soft_failure import.*_redact_credentials' src/kodezart/adapters/claude_client_executor.py src/kodezart/adapters/claude_agent_executor.py` returns exactly two lines.",
  "[sherlock] The chain caller of `build_error_event` at `chains/ticket_generation.py:114` (which the ticket's enumeration overlooks) automatically benefits from the redaction-at-source — verified by the complete caller set: `grep -RIn 'build_error_event(' src/` returns exactly four call lines spread across `services/agent_service.py`, `handlers/agent_handler.py` (twice), and `chains/ticket_generation.py`; each yields an `ErrorEvent` whose `error` field has been routed through `_redact_credentials`.",
  "[sherlock] The PR body explicitly documents the choke-point-vs-origin distinction: `subprocess_git_service.py` `_run`/`_run_output`/`_run_with_exit_codes` at lines 325-329, 347-350, 376-383 (the LEAK ORIGIN) embed raw `stderr.decode().strip()` into `RuntimeError` messages and are NOT modified by this ticket; the design redacts at egress (`build_error_event`) instead. The PR description / commit message names this so a follow-up author understands the choke point is downstream of the leak — verified by inspecting the PR body for the keyword `subprocess_git_service` or `LEAK ORIGIN` paired with `egress` or `convergence`.",
  "[sherlock] No third tokenized-URL construction site exists in the codebase — `grep -RIn 'x-access-token' src/` returns exactly two lines: the construction at `adapters/github_token_auth.py:12` plus its docstring at line 11; redacting at `build_error_event` + the two adapter `awarning` calls is therefore complete coverage for the token-URL form.",
  "An `import re` is added at the top of `core/soft_failure.py` adjacent to the existing `from collections.abc import AsyncIterator`, sorted into the stdlib block per ruff `I` (isort) selector — `ruff check src/kodezart/core/soft_failure.py --select I` exits 0.",
  "The new `from kodezart.core.constants import STDERR_TAIL_BYTES` and `from kodezart.core.soft_failure import _redact_credentials` imports in both adapter files are sorted into the existing first-party-imports block per ruff `I` — `ruff check src/kodezart/adapters/claude_client_executor.py src/kodezart/adapters/claude_agent_executor.py --select I` exits 0.",
  "The new test file `tests/core/test_soft_failure.py` exists and contains the documented set of tests including: redaction of embedded tokenized URL, parametrized redaction of classic prefixes `ghp_/gho_/ghs_/ghu_`, redaction of fine-grained `github_pat_` PAT, preservation of non-secret prose (`ghp_short` below the 36-char floor, branch name `feature/ghp-thing`, plain `hello world`), idempotency `_redact(_redact(s)) == _redact(s)`, `build_error_event` redacts both `error` AND `stderr_tail` field contents, and one representative `NoStructuredOutputError` raise-site smoke test with `error_kind == \"NoStructuredOutputError\"`.",
  "Running `pytest tests/core/test_soft_failure.py -q` exits 0 with all tests passing.",
  "`tests/adapters/test_claude_executors.py` gains two new tests for `ClaudeClientExecutor` only (NOT `ClaudeAgentExecutor`, consistent with the in-file TODO at `claude_agent_executor.py:56-63` arguing CI-cost economy on the unwired path): (a) using `structlog.testing.capture_logs()` to assert the `claude_sdk_process_error` log event's `stderr` field equals the redacted form and the 40-char token body is absent; (b) asserting `AgentSDKError.stderr_tail` contains `_REDACTION_SENTINEL` and the 40-char token body is absent.",
  "Running `pytest tests/adapters/test_claude_executors.py -q` exits 0 with all tests (existing + new) passing.",
  "The test bidirectional-assertion invariant: every redaction test asserts BOTH that `_REDACTION_SENTINEL` IS present in the redacted output AND that the secret-body literal (the 40-char `ghp_AAA...` body, the `_FAKE_GHP_BODY` string, or equivalent) is NOT present — `grep -nE 'assert.*not in|assert.*absent' tests/core/test_soft_failure.py tests/adapters/test_claude_executors.py` returns lines confirming the negative-assertion side of each redaction test.",
  "Test fixtures in `tests/core/test_soft_failure.py` and the two new tests in `tests/adapters/test_claude_executors.py` avoid the ruff S105/S106 trigger: no variable named `token`/`password`/`secret`/`api_key` is assigned a string literal — `grep -nE '^\\s*(token|password|secret|api_key)\\s*=\\s*[\"\\x27]' tests/core/test_soft_failure.py tests/adapters/test_claude_executors.py` returns zero lines. Fixture variables use names like `creds_url`, `tokenized_url`, `fake_url`, `gh_body`, `_FAKE_GHP_BODY`, `pat_body`.",
  "Test fixtures import `_REDACTION_SENTINEL` from `kodezart.core.soft_failure` as the single source of truth — `grep -RIn 'from kodezart.core.soft_failure import.*_REDACTION_SENTINEL' tests/` returns at least one line; no test inlines the literal `\"***REDACTED***\"` (covered by the source-of-truth criterion above).",
  "`tests/core/test_retry.py` renames the import (`SoftFailureError` → `NoStructuredOutputError`) and the test function `test_soft_failure_error_does_not_satisfy_should_retry` → `test_no_structured_output_error_does_not_satisfy_should_retry`. The renamed test passes — `pytest tests/core/test_retry.py::test_no_structured_output_error_does_not_satisfy_should_retry -q` exits 0 and confirms `should_retry(NoStructuredOutputError(...))` returns False (retry semantics survive the rename).",
  "`tests/chains/test_ticket_generation.py`, `tests/chains/test_ralph_loop.py`, `tests/chains/test_ralph_workflow.py`, `tests/api/v1/test_agent.py` are all updated for the rename — `grep -RIn 'SoftFailureError\\|soft_failure_' tests/chains/test_ticket_generation.py tests/chains/test_ralph_loop.py tests/chains/test_ralph_workflow.py tests/api/v1/test_agent.py` returns zero matches; any test function whose name contained `_soft_failure` is renamed to use the `_no_structured_output_error` equivalent.",
  "An inline `# TODO` comment is added in `src/kodezart/chains/ralph_workflow.py::_complete_node` immediately after the `merged=state[\"merged\"],` line inside the `WorkflowCompleteEvent(...)` constructor — the comment text references both `reason` and `WorkflowCompleteEvent`. Verifiable by `grep -nA1 'merged=state\\[\"merged\"\\]' src/kodezart/chains/ralph_workflow.py` showing the TODO on the following lines containing both keywords.",
  "The `WorkflowCompleteEvent` pydantic model is UNCHANGED — `grep -nA40 '^class WorkflowCompleteEvent' src/kodezart/types/domain/agent.py` shows no `reason: ` field added; the schema-stability criterion is preserved (deliverable 6 is comment-only).",
  "The `ErrorEvent` schema is UNCHANGED — `grep -nA30 '^class ErrorEvent' src/kodezart/types/domain/agent.py` shows the existing field types `error: str`, `stderr_tail: str | None = None`, plus `extra=\"forbid\"`; only field CONTENTS are scrubbed by `_redact_credentials`, not field shapes.",
  "Running `mypy --strict src/kodezart/core/soft_failure.py src/kodezart/core/constants.py src/kodezart/adapters/claude_client_executor.py src/kodezart/adapters/claude_agent_executor.py src/kodezart/chains/ralph_workflow.py src/kodezart/chains/ralph_loop.py src/kodezart/chains/ticket_generation.py src/kodezart/adapters/git_change_persister.py` exits 0 with no errors under `disallow_any_explicit=true` and `no_implicit_reexport=true`.",
  "Running `ruff check src/kodezart/core/soft_failure.py src/kodezart/core/constants.py src/kodezart/adapters/claude_client_executor.py src/kodezart/adapters/claude_agent_executor.py src/kodezart/chains/ralph_workflow.py src/kodezart/chains/ralph_loop.py src/kodezart/chains/ticket_generation.py src/kodezart/adapters/git_change_persister.py tests/core/test_soft_failure.py tests/adapters/test_claude_executors.py tests/integration/test_workflow_e2e.py tests/chains/test_ralph_workflow.py tests/chains/test_ralph_loop.py tests/chains/test_ticket_generation.py tests/core/test_retry.py tests/api/v1/test_agent.py` exits 0 under the active ruff selection `[E,W,F,I,B,C4,UP,N,ANN,S,A,ARG,RUF]`.",
  "NO new `# noqa`, `# type: ignore`, `# mypy: ignore-errors`, or `# ruff: noqa` is introduced in any file touched by this change — `git diff main..HEAD -- '*.py' | grep -E '^\\+.*(#\\s*noqa|#\\s*type:\\s*ignore|#\\s*mypy:|#\\s*ruff:)'` returns zero new lines. The bare `# TODO:` in `_complete_node` is acceptable (ruff `FIX*`/`TD*` are NOT in the active selection).",
  "NO widening of `[tool.ruff.lint.per-file-ignores]` — `git diff main..HEAD -- pyproject.toml | grep -E '^\\+.*per-file-ignores'` returns zero new entries; specifically `S105`/`S106` are NOT added to the tests/** ignores list.",
  "Running `make verify-no-origin-literal` exits 0 — the redaction strings (`x-access-token`, `ghp_`, `gho_`, `ghs_`, `ghu_`, `github_pat_`, `***REDACTED***`) do not collide with the literal-`\"origin\"` pattern that target enforces.",
  "Running the full suite `pytest -q` exits 0 with the same or higher passing-test count than the pre-change baseline — no regressions.",
  "No layer violation introduced: `grep -nE '^from kodezart\\.(adapters|chains|services|handlers|api)|^import claude_agent_sdk' src/kodezart/core/soft_failure.py src/kodezart/core/constants.py` returns zero lines (core remains pure of adapter/chain/service/handler imports).",
  "`adapters → chains` direction remains forbidden — `grep -RnE '^from kodezart\\.chains|^import kodezart\\.chains' src/kodezart/adapters/` returns zero lines (regression guard).",
  "`core → adapters` direction remains forbidden — `grep -RnE '^from kodezart\\.adapters|^import kodezart\\.adapters' src/kodezart/core/` returns zero lines (regression guard).",
  "[sherlock] No new public re-export is added for `_redact_credentials` — `grep -n '^__all__' src/kodezart/core/soft_failure.py` returns zero lines; `grep -n '_redact_credentials' src/kodezart/__init__.py src/kodezart/core/__init__.py 2>/dev/null || true` shows no top-level re-export. The cross-layer underscore-prefixed import (the first such in this codebase, since the cited `_validate_permission_mode` precedent is intra-adapter) is acceptable because it is fully-qualified and not propagated as a public symbol.",
  "`_redact_credentials` operates only on plain `str` — `grep -nA3 'def _redact_credentials' src/kodezart/core/soft_failure.py` shows no `ProcessError`, `ClaudeSDKError`, `Optional[str]`, `str | None`, or pydantic `BaseModel` in the signature (None-handling is the caller's responsibility).",
  "No try/except suppression is introduced around `_redact_credentials` — `grep -nB1 -A3 '_redact_credentials' src/kodezart/` does not show any line being wrapped in `try:`/`except: pass`. The exception still raises; only the field contents are scrubbed at the egress point.",
  "No mock, hardcoded credential, or test-double construction appears in `src/kodezart/` — `grep -RIn 'ghp_[A-Za-z0-9]\\{20,\\}\\|github_pat_[A-Za-z0-9_]\\{15,\\}' src/` returns matches ONLY inside the regex pattern strings of `core/soft_failure.py` (i.e., as part of the regex source, not as data).",
  "Existing tests that previously asserted on the old class name update consistently: `tests/api/v1/test_agent.py` (around line 360) asserts `error_event.error_kind == \"NoStructuredOutputError\"` — `grep -n 'error_kind.*\"NoStructuredOutputError\"' tests/api/v1/test_agent.py` returns at least one line; `grep -n 'error_kind.*\"SoftFailureError\"' tests/` returns zero lines.",
  "The renamed class follows the project's exception-naming convention (PascalCase + `Error` suffix) consistent with `WorkspaceError`, `TransientAPIError`, `RateLimitError`, `AgentSDKError` — verified by reading the class definition line.",
  "The commit / PR is rebased on `kodezart/fix-pipeline-obs-git-remote-is-ancestor-1c5f230d-ralph-27fc4305 @ 80aa43d` (the stated branch base) — the observability-widening / git-remote-config / is-ancestor-refspec work already on the branch is NOT re-opened or duplicated by this change.",
  "[sherlock] The stale comment line at `tests/adapters/test_claude_executors.py` that previously referenced `_STDERR_TAIL_BYTES = 4096` is either updated to reference the new name `STDERR_TAIL_BYTES` (no leading underscore) or removed — `grep -n 'STDERR_TAIL_BYTES' tests/adapters/test_claude_executors.py` shows no lingering underscore-prefixed form. This is a comment-rot guard that no single Watson found but that emerges from cross-referencing the rename's full reference set."
]