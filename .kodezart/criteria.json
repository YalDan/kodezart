[
  "`pyproject.toml` — the `claude-agent-sdk` dependency line uses the compatible-release `~=` operator (not `>=`, `<`, `==`, `!=`, or a bare version) and its version specifier is not `0.1.69`.",
  "`pyproject.toml` — no other dependency pin line is modified; the only edit under the `[project.dependencies]` / `dependencies` array is to the `claude-agent-sdk` entry.",
  "`pyproject.toml` — the rationale comment block immediately above the `claude-agent-sdk` pin contains no CVE, GHSA, or `mcp` remediation motive.",
  "`pyproject.toml` — the rationale comment above the `claude-agent-sdk` pin does not still describe `<0.2.0` as active cap policy; any occurrence of that upper bound is framed as an explicit acknowledgment that this bump reverses the prior policy.",
  "`pyproject.toml` — the rationale comment above the `claude-agent-sdk` pin records at least one concrete observation about the SDK's public surface at the resolved version (the `ProcessError` constructor shape, the `ClaudeAgentOptions` field set the adapters use, or the top-level re-export status of `RateLimitEvent`/`StreamEvent`), not a placeholder template.",
  "`pyproject.toml` — the rationale comment above the `claude-agent-sdk` pin contains no unresolved placeholder tokens such as `<LATEST>`, `<TODO>`, `<FILL_IN>`, `<VERSION>`, or `TBD` left literally in the text.",
  "`uv.lock` — the `[[package]]` block whose `name = \"claude-agent-sdk\"` carries a `version = ...` value that is not `0.1.69`, matching the floor implied by the `pyproject.toml` pin.",
  "`uv.lock` — the project's own dependency-metadata specifier for `claude-agent-sdk` still uses the `~=` operator (not a hand-inserted `>=`/`<` combo).",
  "`src/kodezart/adapters/_sdk_mapping.py` — the 14 `isinstance(...)` dispatch sites in the message-mapping block are unchanged in count, branch order, and referenced symbol names.",
  "`src/kodezart/adapters/_sdk_mapping.py` — the file is in exactly one of two coherent states: (a) it still contains a single `from claude_agent_sdk.types import RateLimitEvent, StreamEvent` line and no top-level import of those two symbols from `claude_agent_sdk`; OR (b) both `RateLimitEvent` and `StreamEvent` are imported from `claude_agent_sdk` at the top level and the `claude_agent_sdk.types` import for these two symbols is absent. No half-migrated state (only one of the two symbols moved) is acceptable.",
  "`src/kodezart/adapters/_sdk_mapping.py` — no `try: ... except ImportError: ...` block or other conditional runtime import is introduced for the SDK symbols.",
  "`src/kodezart/adapters/_sdk_mapping.py` — no `__all__` list is introduced in this file and no `from claude_agent_sdk import X as X` re-export shim is added; if the flatten happens, the single consumer's import path moves rather than an alias being created.",
  "`src/kodezart/core/config.py` — the `model` field remains declared as `str | None` with `Field(default=None, ...)`; the `None` default is preserved (no source-side mitigation for a potentially changed bundled-CLI default).",
  "`src/kodezart/main.py` — the wiring that constructs `ClaudeClientExecutor` still passes `model=config.model` (or the equivalent unwrapped `config.model` argument); the composition-root wiring is unchanged.",
  "`src/kodezart/adapters/claude_agent_executor.py` — no functional edits: imports, the `stream(...)` method, the `ClaudeAgentOptions(...)` construction, and the exception handler (including the deferred `stderr_tail` TODO) are unchanged.",
  "`src/kodezart/adapters/claude_client_executor.py` — the `ProcessError` handler continues to read `exc.exit_code` and `exc.stderr`, redact-before-slice to `STDERR_TAIL_BYTES`, and re-raise as `AgentSDKError`; the `ClaudeAgentOptions(...)` construction still passes exactly the six kwargs `cwd`, `permission_mode`, `allowed_tools`, `resume`, `output_format`, `model` and no new ones.",
  "`src/kodezart/domain/errors.py` — `AgentSDKError`'s constructor signature and attribute shape are unchanged; no SDK-typed exception object is added as a field, and `exit_code` / `stderr_tail` remain primitive scalars.",
  "`src/kodezart/types/domain/agent.py` — no `AgentEvent` subtype has fields added, removed, or reshaped; in particular no field is added to make a bundled-CLI probe convenient.",
  "`src/kodezart/core/protocols.py` — the `AgentExecutor` protocol signature is unchanged.",
  "`Makefile` — no edits to the `check:` target or any of its recipe steps (`verify-no-origin-literal lint type-check test`).",
  "`tests/adapters/test_claude_executors.py` — no `def test_` declaration is removed, no `assert ` line is removed, and the existing `ProcessError(\"...\", exit_code=..., stderr=...)` constructor sites plus `AgentSDKError.exit_code` / `.stderr_tail` round-trip assertions remain intact.",
  "`tests/conftest.py` — the `pytest_collection_modifyitems` live-skip hook is unchanged; no changes on the lines that add `pytest.mark.skip` to `live`-marked items when `-m live` is not selected.",
  "`src/kodezart/agents/__init__.py` — unchanged; the SDK reference in its docstring remains docstring-only, and no active `claude_agent_sdk` import is introduced here.",
  "`src/kodezart/adapters/_permission_modes.py` — unchanged; no new permission-mode literals, alias reshuffling, or validator changes.",
  "No file under `src/kodezart/` outside `src/kodezart/adapters/` contains any `from claude_agent_sdk` or `import claude_agent_sdk` statement — the adapter layer remains the sole holder of SDK-typed dependencies.",
  "No file anywhere in the repository imports from a `claude_agent_sdk._*` (underscore-prefixed) submodule or attribute — no `_find_cli`, `_internal`, `_transport`, `SubprocessCLITransport`, or any other private SDK path is referenced.",
  "No file in the repository contains an untyped SDK dict-key read of the form `SystemMessage.data[\"model\"]`, `message.data[\"model\"]`, or any equivalent string-key access against SDK `Message` / `AssistantMessage` / `SystemMessage` / `ResultMessage` objects.",
  "No file with a name matching `PULL_REQUEST_DESCRIPTION*`, `PR_BODY*`, `PULL_REQUEST_TEMPLATE*` (or the lowercase `pull_request_template*`) exists at the repository root or under `.github/`; the PR body remains transient and is not committed.",
  "No new `# type: ignore` and no new `# type: ignore[<code>]` bracketed-code suppression is introduced on any changed line.",
  "No new linter suppression is introduced on any changed line — no new `# noqa`, `# noqa: <code>`, `# ruff: noqa`, or `# ruff: noqa: <code>` directive.",
  "No new `pytest.mark.skip`, `pytest.mark.skipif`, `pytest.skip(...)` call, or `pytest.importorskip(...)` call is introduced on any changed line.",
  "No Pydantic `model_config` has its `extra` value flipped from `forbid` or `ignore` to `allow` on any changed line.",
  "No new file is created under `src/kodezart/` — no facade, shim, SDK-version-compat helper, probe script, or new module is added anywhere under the production source tree.",
  "No new `AppConfig` field is introduced, and no new operator-facing model-override documentation file (`docs/*.md`, `README*` addition, or similar) is added as part of this change.",
  "No transient artifact from the bundled-CLI probe (script `.py`, `.sh`, transcript `.md`, or captured-output file) is committed anywhere under version control at the repository root, under `.github/`, under `src/`, under `tests/`, or under `scripts/`."
]