{
  "additionalProperties": false,
  "description": "Structured output for a generated implementation ticket.\n\nThe downstream generate_criteria agent has its own job \u2014 it derives\nobservable acceptance criteria from the full formatted ticket.",
  "properties": {
    "context": {
      "description": "The repository facts the implementation rests on, including the documentation sources consulted.",
      "title": "Context",
      "type": "string"
    },
    "openQuestions": {
      "description": "Questions the ticket could not settle from the repository.",
      "items": {
        "type": "string"
      },
      "title": "Openquestions",
      "type": "array"
    },
    "outOfScope": {
      "description": "Work this ticket deliberately excludes.",
      "items": {
        "type": "string"
      },
      "title": "Outofscope",
      "type": "array"
    },
    "references": {
      "description": "Locations in the repository the implementer starts from.",
      "items": {
        "additionalProperties": false,
        "description": "A reference to a specific location in the codebase.",
        "properties": {
          "location": {
            "description": "Where in the repository this points: a file path, optionally with a line range or a symbol name.",
            "title": "Location",
            "type": "string"
          },
          "note": {
            "description": "What the implementer needs to know about this location.",
            "title": "Note",
            "type": "string"
          }
        },
        "required": [
          "location",
          "note"
        ],
        "title": "CodeReference",
        "type": "object"
      },
      "title": "References",
      "type": "array"
    },
    "requiredChanges": {
      "description": "Every file the change must touch, one entry per file.",
      "items": {
        "additionalProperties": false,
        "description": "A single file-level change required to implement the ticket.",
        "properties": {
          "changeType": {
            "description": "Whether the file is created, modified or deleted.",
            "enum": [
              "create",
              "modify",
              "delete"
            ],
            "title": "Changetype",
            "type": "string"
          },
          "description": {
            "description": "What changes in this file, stated as an observable outcome.",
            "title": "Description",
            "type": "string"
          },
          "filePath": {
            "description": "Repository-relative path of the file this change touches.",
            "title": "Filepath",
            "type": "string"
          },
          "rationale": {
            "description": "Why this change is required by the task, not merely useful.",
            "title": "Rationale",
            "type": "string"
          }
        },
        "required": [
          "filePath",
          "changeType",
          "description",
          "rationale"
        ],
        "title": "FileChange",
        "type": "object"
      },
      "title": "Requiredchanges",
      "type": "array"
    },
    "summary": {
      "description": "What the change accomplishes, for a reader with no other context.",
      "title": "Summary",
      "type": "string"
    },
    "title": {
      "description": "One line naming what the ticket delivers.",
      "title": "Title",
      "type": "string"
    }
  },
  "required": [
    "title",
    "summary",
    "context",
    "requiredChanges"
  ],
  "title": "TicketDraftOutput",
  "type": "object"
}
