{
 ".env.example": "text: CONTENT differs: recomposed-only 83 lines, donor-only 47 lines; e.g. ['# SEPARATE axis from KODEZART_MODEL: the set decides what is said, the model', '#KODEZART_MODEL=claude-opus-5'] / ['# SEPARATE axis from KODEZART_AGENT__MODEL: the set decides what is said, the model', '#KODEZART_AGENT__MODEL=claude-opus-5']",
 "CHANGELOG.md": "text: same non-blank line multiset (ordering/blank drift only)",
 "README.md": "text: same non-blank line multiset (ordering/blank drift only)",
 "docs/api.md": "text: same non-blank line multiset (ordering/blank drift only)",
 "docs/architecture.md": "text: CONTENT differs: recomposed-only 0 lines, donor-only 11 lines; e.g. [] / ['The authored Ralph evaluator observes native SDK session openings at its', 'actual dispatch boundary. A typed `node_session_started` stream occurrence']",
 "docs/configuration.md": "text: CONTENT differs: recomposed-only 0 lines, donor-only 9 lines; e.g. [] / ['Tracker deployments require a complete `[run_event_states]` table. Its keys', 'are the single `RunEventKind` vocabulary; startup names every missing or']",
 "docs/cutover_mapping.md": "text: same non-blank line multiset (ordering/blank drift only)",
 "docs/migration-v0.2-to-v0.3.md": "text: same non-blank line multiset (ordering/blank drift only)",
 "docs/operation.example.toml": "text: CONTENT differs: recomposed-only 2 lines, donor-only 39 lines; e.g. ['# collide with a principal.', '# the knowledge server to the scheduled passes (KODEZART_KNOWLEDGE_SESSION_GRANTS'] / ['# collide with a principal. The tracker credential must be attributed to one', '# of these identities; boot refuses otherwise.']",
 "docs/parity_checklist.md": "text: same non-blank line multiset (ordering/blank drift only)",
 "src/kodezart/adapters/git/change_persister.py": "py: same statement multiset (placement/whitespace drift only)",
 "src/kodezart/adapters/linear/tracker.py": "py: CONTENT differs: recomposed-only 1 stmts, donor-only 1 stmts; e.g. Expr(value=Constant(value=\"Linear tracker adapter \u2014 a programmatic MCP client behind ``TrackerPort``.\\n\\nEvery read and write on the deterministic path is a named tool call with\\nno model in the loop.  The adapter owns everything vendor-shaped: the\\nidentifier translation, queue-state-as-label mecha",
 "src/kodezart/composition/engine.py": "py: same statement multiset (placement/whitespace drift only)",
 "src/kodezart/composition/jobs.py": "py: same statement multiset (placement/whitespace drift only)",
 "src/kodezart/composition/passes.py": "py: same statement multiset (placement/whitespace drift only)",
 "src/kodezart/config/app.py": "py: same statement multiset (placement/whitespace drift only)",
 "src/kodezart/core/errors.py": "py: same statement multiset (placement/whitespace drift only)",
 "src/kodezart/core/protocols.py": "py: same statement multiset (placement/whitespace drift only)",
 "src/kodezart/domain/errors.py": "py: same statement multiset (placement/whitespace drift only)",
 "src/kodezart/domain/fire_spec.py": "py: same statement multiset (placement/whitespace drift only)",
 "src/kodezart/domain/organize.py": "py: same statement multiset (placement/whitespace drift only)",
 "src/kodezart/main.py": "py: same statement multiset (placement/whitespace drift only)",
 "src/kodezart/services/agent_service.py": "py: same statement multiset (placement/whitespace drift only)",
 "src/kodezart/types/domain/agent.py": "py: same statement multiset (placement/whitespace drift only)",
 "src/kodezart/types/domain/audit.py": "py: same statement multiset (placement/whitespace drift only)",
 "src/kodezart/types/domain/dispatch.py": "py: CONTENT differs: recomposed-only 1 stmts, donor-only 1 stmts; e.g. ClassDef(name='DispatchReport', bases=[Name(id='DispatchModel', ctx=Load())], keywords=[], body=[Expr(value=Constant(value='The outcome of exactly one dispatch pass.\\n\\n    ``tied_candidates`` is non-empty only when the ranking reached the\\n    random tie-break, and then it carries the whole tied se",
 "src/kodezart/types/domain/fire_spec.py": "py: same statement multiset (placement/whitespace drift only)",
 "src/kodezart/types/domain/native_execution.py": "py: same statement multiset (placement/whitespace drift only)",
 "src/kodezart/types/domain/operation.py": "py: same statement multiset (placement/whitespace drift only)",
 "src/kodezart/types/domain/organize.py": "py: same statement multiset (placement/whitespace drift only)",
 "src/kodezart/types/domain/prompts.py": "py: same statement multiset (placement/whitespace drift only)",
 "src/kodezart/types/domain/session.py": "py: same statement multiset (placement/whitespace drift only)",
 "src/kodezart/types/domain/tracker.py": "py: CONTENT differs: recomposed-only 1 stmts, donor-only 1 stmts; e.g. Expr(value=Constant(value=\"Vendor-neutral tracker vocabulary.\\n\\nEvery type here is domain language.  No vendor concept \u2014 no cycles, no\\ninitiatives, no vendor identifier format \u2014 appears in this module, and\\nnone may appear in ``TrackerPort``'s signatures.  Adapters own the\\ntranslation between the",
 "src/kodezart/types/domain/workflow.py": "py: same statement multiset (placement/whitespace drift only)",
 "src/kodezart/types/domain/workspace.py": "py: same statement multiset (placement/whitespace drift only)",
 "tests/chains/test_ralph_loop.py": "py: same statement multiset (placement/whitespace drift only)",
 "tests/fakes.py": "py: same statement multiset (placement/whitespace drift only)",
 "tests/integration/test_stacked_scope.py": "py: same statement multiset (placement/whitespace drift only)",
 "tests/integration/test_workflow_e2e.py": "py: same statement multiset (placement/whitespace drift only)",
 "tests/tracker/conftest.py": "py: same statement multiset (placement/whitespace drift only)",
 "tests/types/test_wire_schemas.py": "py: same statement multiset (placement/whitespace drift only)"
}